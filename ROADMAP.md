# Explorer Rebuild Roadmap

| ID | Task | Status | Notes |
| --- | --- | --- | --- |
| EX-001 | Map current Explorer data needs vs. Torii/Norito capabilities in `../i23`; document required endpoints (REST, Norito, SSE/WS). | COMPLETED | `docs/roadmap/explorer_gap_analysis.md` documents gaps; maintain as APIs evolve. |
| EX-002 | Implement Explorer-focused endpoints in Torii (accounts/domains/assets/NFTs with metadata + counters, blocks/transactions/instructions history, metrics/peers summaries). | COMPLETED | `/v2/explorer/accounts`, `/domains`, `/asset-definitions`, `/assets`, `/nfts`, `/blocks`, `/transactions` + `/transactions/{hash}`, `/instructions`, and `/instructions/{hash}/{index}` now land in Torii with tests, and the frontend normalizes the instruction payloads to the Norito `{ kind, payload }` shape. Governance metrics now fetch Torii directly and listen to `GET /v2/gov/stream` (SSE), which emits `CouncilUpdated`/`UnlockStatsUpdated` and per-id `ReferendumUpdated`/`LocksUpdated`/`TallyUpdated`/`ProposalUpdated` so the dashboard refetches without manual refresh (polling remains as fallback). |
| EX-003 | Replace the frontend data layer to call Torii directly (Norito + WS/SSE clients, env-configurable base URL, remove legacy backend proxy). | COMPLETED | Accounts/domains/assets/NFTs/blocks/transactions now hit the `/v2/explorer/*` handlers directly, asset-definition fetchers live on Torii, governance/ZK/SoraFS calls bypass the backend proxy, query serialization no longer drops zero/false values, the production `Caddyfile` only serves the SPA, all telemetry consumers share a single `useTelemetryMetrics` helper, and the telemetry/peers UI now fetches Torii’s `/v2/explorer/metrics` + `/v2/telemetry/peers-info` without the legacy `/api` proxy. |
| EX-006 | Wire Torii explorer detail endpoints (assets, NFTs, account extras) + aggregation helpers so the UI can drop the legacy backend entirely. | COMPLETED | `/v2/explorer/accounts/{id}`, `/domains/{id}`, `/asset-definitions/{id}`, `/assets/{id}`, `/nfts/{id}`, `/blocks/{id}`, `/transactions/{hash}`, and `/instructions/{hash}/{index}` now resolve straight from Torii, and the frontend consumes the Norito instruction payloads plus `/v2/explorer/transactions/stream` + `/v2/explorer/instructions/stream` SSE feeds, so the legacy backend proxy/listeners are gone. |
| EX-004 | Surface new Iroha features in the UI (governance dashboards, KAIGI relays, SoraFS state, ZK proof telemetry) with meaningful visualizations. | COMPLETED | KAIGI relays overview/detail page, `/sorafs/registry`, `/governance`, and `/zk/telemetry` are live; the ZK page now includes filtered proof SSE throughput charts plus attachment/report analytics. |
| EX-005 | Strengthen automated testing (unit + integration + e2e smoke); ensure every new function has tests and complex logic has multiple cases. | COMPLETED | Explorer frontend now has Vitest coverage for instruction table behaviors (auto-open, retries, share/copy), API query-serialization helpers, the telemetry composables (`useTelemetryMetrics`/`usePeersTelemetry`), the reusable table pagination hook (`src/shared/lib/table.ts`), both home widgets (`latest-transactions` SSE refresh + `latest-blocks` rendering/navigation), the API URL helpers (`buildToriiUrl`, `buildToriiWsUrl`, `resolveApiUrl`), and four e2e smoke passes: routing/layout, Blocks data (`tests/e2e/blocks-data.smoke.test.ts`), Accounts data (`tests/e2e/accounts-data.smoke.test.ts`), plus an SSE-focused LatestTransactions stream test (`tests/e2e/latest-transactions-stream.smoke.test.ts`) that asserts Torii pushes trigger refetches only when filters match. |
| EX-007 | Point the frontend data layer directly at Torii (`VITE_API_URL` → `/v2/explorer/*`) and move SSE/WS consumers off the legacy `/api/v2` proxy. | COMPLETED | The API client + Vite proxy now default to `/v2/explorer` (configurable via `VITE_API_URL`); telemetry + KAIGI SSE streams use `buildToriiUrl('/v2/...')`, latest transactions/instructions widgets listen directly to Torii SSE/WebSocket feeds, and the instruction schemas normalize legacy wire payloads into the Norito `{ kind, payload }` JSON so every fetcher consumes the structured Torii responses without backend fallbacks. |
| EX-008 | Enforce “Norito-only serialization” across Torii explorer handlers and shared DTOs (remove serde/non-Norito API codecs). | COMPLETED | `crates/iroha_torii/src/explorer.rs` pagination query now derives the Norito + Json codecs (defaults handled via `#[norito(default = ...)]`), so serde usage is removed from the explorer app API path. |
| EX-009 | Document the large-task decomposition policy in `AGENTS.md`. | COMPLETED | Added rule 5 instructing agents to auto-break large requests into smaller subtasks and handle them with proper engineering practices instead of rejecting work. |
| EX-010 | Critique codebase quality (architecture, testing, UX correctness) per latest request. | COMPLETED | Addressed the flagged gaps: wired SearchField routing + tests, fixed TransactionsTable hash bindings, hardened BaseButton/BaseLink, and exposed KAIGI relays via the nav. |
| EX-011 | Surface Torii's new Sumeragi telemetry endpoints (`/v2/sumeragi/status`, `/v2/sumeragi/status/sse`, `/v2/sumeragi/telemetry`) inside the Explorer telemetry views. | COMPLETED | Telemetry page now consumes the enriched Torii status payloads (view-change index/proofs, inline post counters, RBC store pressure/evictions, consensus/VRF penalty totals, settlement + Nexus fee/staking snapshots, NPoS election details, lane governance seals) alongside the existing VRF summary, QC latency, backlog, and availability collectors. Node telemetry table now shows connected peer counts, queue usage (size/capacity), block delta vs best, and renders city + country when available. Governance updates now use `GET /v2/gov/stream` (SSE) for event-driven refetches with polling fallback. Sumeragi status now falls back to 10s polling when the SSE stream is closed/unavailable (and reports `Stream: polling`). Upstream Torii fix: allow `/v2/sumeragi/status/sse` under `telemetry_profile=extended` (gate by `expensive_metrics` instead of `developer_outputs`) so Explorer can show `Stream: connected` in prod; added a regression test in `../iroha` (`crates/iroha_torii/tests/sumeragi_status_sse_profile_gate.rs`). |
| EX-012 | Modernize the UI (Sora font + vector logo), add node presets, and ship multilingual/RTL support. | COMPLETED | Header node selector lets users pick Nexus/Testus or custom Torii bases (updates explorer/Torii URLs and persists). Sora variable font from `../sora-font` is loaded globally, the header logo uses the vector `hyperledger_iroha_final.svg`, and home/KAIGI surfaces now have HDR gradients + aligned tables. Arabic/Hebrew/Urdu locales are wired with RTL direction toggling in `useApplicationLanguage`; Urdu now uses a dedicated Nastaliq/Naskh-first font stack (fallbacks to Sora/sans-serif) so localized UI text renders with better script aesthetics. Fixed RTL header dropdown anchoring so teleported dialogs (language/menu/node selector) stay aligned to the trigger side instead of flipping to the opposite edge. Language selector behavior is now idempotent on repeat clicks (second click closes reliably), dropdown click-outside handling ignores trigger clicks to prevent reopen races, and header/portal layering is elevated so governance/content text no longer paints over the dialog. Home-page RTL button/dropdown positioning now relies on logical inline-end anchors only (no extra RTL side override), preventing the language selector from clipping off-screen. The latest translation QA pass corrected locale issues across Turkish, Sinhala, Tamil, Azerbaijani, Armenian, Georgian, Khmer, Burmese, Nepali, Dhivehi, Hebrew, Arabic, and Ukrainian strings; complete JSON locale packs are now generated + wired for all configured modern non-English locales (including Amharic, Azerbaijani, Armenian, Georgian, Turkish, Sinhala, Tamil, Indonesian, Ukrainian, Urdu, Hebrew, Arabic, Dhivehi, and Dzongkha) with placeholder/schema integrity validation. Added an Old Akkadian (`akk`) locale variant with poetic cuneiform semantic approximations, language-picker support, and a cuneiform-first font fallback. Added an Ancient Egyptian (`egy`) locale variant with poetic hieroglyphic semantic approximations, language-picker support, and a hieroglyph-first font fallback, and then removed English gloss text so `egy` now renders hieroglyph-only labels. `/accounts/:id` now has non-English localized account/detail/table labels across every configured locale (including programmatic locales), enforced by a localization regression test.  Verified 2026-03-09 with `pnpm exec vitest run src/shared/ui/composables/useApplicationLanguage.test.ts src/shared/lib/localization/index.test.ts src/features/switch-lang/LangDropdown.test.ts src/widgets/header/TheHeader.test.ts src/pages/HomePage.test.ts` and `pnpm typecheck`. |
| EX-013 | Bootstrap a local explorer automatically from the localnet script and align the frontend to latest Torii explorer payloads (no legacy fallback). | COMPLETED | `scripts/sora_localnet.py` now syncs `iroha2-block-explorer-web/.env.local` (`VITE_API_URL`) and starts/stops a managed `pnpm dev` explorer process with PID/log tracking. Frontend API/schema parity updated for current Torii payloads (`/accounts` filtering path, NFT `metadata`, nullable `avg_block_time`, `Limited(...)` mintable labels) with test coverage in `src/shared/api/*.test.ts`. |
| EX-014 | Ignore generated local Explorer artifacts in git tracking. | COMPLETED | `.gitignore` now covers `output`, `test-results`, `playwright-report`, `coverage`, `.vite`, `.pnpm-store`, and local `.env*` overrides (keeping `*.example` files trackable) so local dev/test outputs stay out of git status. |
| EX-015 | Add HDR highlights for HDR-compatible displays across hero/background surfaces. | COMPLETED | Added CSS HDR overrides gated by `@media (dynamic-range: high), (video-dynamic-range: high)` + `@supports (color: color(display-p3 1 1 1))` in `src/app/App.vue`, `src/pages/HomePage.vue`, `src/shared/ui/components/BaseContentBlock.vue`, and `src/pages/KaigiRelays.vue`; SDR styling remains unchanged while HDR/P3 screens get brighter highlight gradients, stronger glow shadows, and richer status pill/detail panel accents. |
| EX-016 | Polish light mode visuals (palette contrast + surface legibility). | COMPLETED | Light theme token map now uses lighter surfaces (`surface: neutral-0`, `surface-variant: secondary-10`), stronger borders (`border-primary/secondary` moved to `secondary-20/30`), and dark-on-light `content-on-surface-variant` text. Added light-mode-specific chrome polish in `src/app/App.vue` (background gradients), `src/shared/ui/components/BaseContentBlock.vue` (card border/shadow/highlight tuning), and `src/widgets/home-page-info/HomePageInfo.vue` (stats strip gradient + typography contrast) while keeping dark mode and HDR-specific branches intact. |
| EX-017 | Add a small top spacing buffer on `/blocks` so content cards never tuck under the toolbar during movement/hover transitions. | COMPLETED | Added a page-level top margin on `src/pages/BlocksList.vue` (`margin-top: size(1)` + larger `md` buffer) to keep the blocks table card visually separated from the header toolbar. |
| EX-018 | Remove the outer container box around the home-page search section. | COMPLETED | Dropped `home-page__search` background/border/shadow/padding (including HDR overrides) in `src/pages/HomePage.vue` so only the search input itself is boxed. |
| EX-019 | Make home-page “Try a…” sample tags interactive. | COMPLETED | Converted sample chips to real buttons in `src/pages/HomePage.vue`, wired each to a route target (`blocks/tx/account/asset/nft` examples), and added regression coverage in `src/pages/HomePage.test.ts`. |
| EX-020 | Restore localization module loading when generated locale JSON files are missing. | COMPLETED | Updated `src/shared/lib/localization/index.ts` to stop importing missing locale JSON files (`si/ta/id/uk/ur/he/ar`) and use the already-defined in-code locale objects, clearing Vite import-analysis 500 errors on startup. |
| EX-021 | Handle Torii transaction `rejection_reason.json` payloads that can be either structured objects or plain strings. | COMPLETED | `DetailedTransaction` now accepts `rejection_reason.json` as either a record or string, and `src/shared/api/schemas.test.ts` covers both shapes to prevent regressions in `fetchTransaction` parsing. |
| EX-022 | Restore functional pagination controls on `/blocks`. | COMPLETED | `/blocks` now uses standard (non-reversed) pagination mode to match Torii’s 1-indexed paging semantics, and `src/pages/BlocksList.test.ts` asserts the page does not opt into reversed mode. |
| EX-023 | Decode Norito/base64 `TransactionRejectionReason` payloads into human-readable rejection messages. | COMPLETED | Transaction detail page now decodes `rejection_reason.encoded` (Norito bytes as hex) and falls back to `rejection_reason.json` (Norito archive base64), so UI shows a readable reason like “Validation failed: Instruction failed: Failed to find domain: bank2” instead of `TlJUM...`. |
| EX-024 | Fix KAIGI relays overview when telemetry counters are missing/reset; align KAIGI SSE parsing with Torii. | COMPLETED | KAIGI overview now floors registrations/reports totals using the relay list (avoids misleading all-zero snapshots), shows an Unknown status bucket when needed, parses `/v2/kaigi/relays/events` payloads correctly, and debounces overview refetch on incoming SSE events. |
| EX-025 | Restore pre-HDR light mode visuals by scoping HDR highlights to dark mode only. | COMPLETED | HDR/P3 overrides are now applied only when `html.dark` is active, so light theme stays on the SDR styling while dark theme keeps the HDR glow treatment. |
| EX-026 | Fix square-edged focus/hover highlights on rounded UI in dark mode. | COMPLETED | Removed the global `:focus-visible` `border-radius` override so focus rings keep each component’s intended rounded shape instead of forcing a 4px radius. |
| EX-027 | Add a documented nginx subpath deployment and enable subpath hosting for the Explorer build. | COMPLETED | Added `VITE_APP_BASE_PATH` support in `vite.config.mts`, switched router history to `import.meta.env.BASE_URL`, documented local+nginx deployment in `README.md`, and added `ops/nginx/explorer.subpath.locations.conf` serving the app at `/explorer-iroha2/` with `/v2/*` proxied to Torii (`127.0.0.1:29080`). |
| EX-028 | Center the home-page search icon vertically in the main search bubble. | COMPLETED | Adjusted the `lg` `SearchField` icon rendering so the magnifier sits in the exact vertical center (`src/features/search/SearchField.vue`). |
| EX-029 | Resolve merge conflicts from the `origin/i23` sync. | COMPLETED | Reconciled duplicate roadmap IDs by splitting the conflicting `EX-027` rows into `EX-027` + `EX-028` and removed merge markers from `ROADMAP.md`. |
| EX-030 | Remove legacy codec wording and field naming from Explorer UI/API mappings; keep Norito-only terminology. | COMPLETED | Instruction/rejection schemas now use Norito `encoded` bytes end-to-end (`Instruction.box.encoded`, `rejection_reason.encoded`), instruction detail UI + copy actions use `encoded` naming, localization keys/texts were renamed to `instructionEncodedPayload`/`copyEncodedPayload`, and roadmap/docs/agent guidance were updated to remove legacy codec terminology. |
| EX-031 | Remove lingering placeholder UI text from the Explorer frontend/localization packs. | COMPLETED | Removed unresolved localization placeholder artifacts (`__…__`/`PH0__`) in `dv`/`dz` locale entries, replaced them with concrete values, and cleared remaining TODO placeholder comments in UI/theme files. Verified with `pnpm typecheck` and targeted localization tests (`pnpm exec vitest run src/shared/lib/localization/index.test.ts`). |
| EX-032 | Prevent UI jumps/flashes on background data refresh (polling/SSE) by keeping previous content rendered while refetching. | COMPLETED | `setupAsyncData` now preserves the last fulfilled value via `useStaleState` and ensures watchers are attached before the initial run; `BaseTable` no longer blanks rows during refresh (shows a small overlay spinner instead), LatestTransactions keeps rendering existing rows while a refetch is pending, and Governance dashboard avoids blanking on auto-refresh by treating loading as “initial-only” when stale data is available. Added tests: `src/shared/utils/setup-async-data.test.ts`, `src/shared/ui/components/BaseTable.test.ts`. |
| EX-033 | Add an Econometrics page with per-asset metrics (distribution, concentration, activity). | COMPLETED | Added `/econometrics` page with per-asset input + query param (`?asset=...`). Snapshot metrics computed from `/v2/explorer/assets?definition=...`: holders, supply, coverage, Gini, HHI, effective holders, top-1/5/10, median/p90/p99, plus top-holders table. Activity section scans a recent sample of Transfer instructions to estimate turnover/velocity when payloads include an `object` and (optionally) amount fields. Added `src/shared/lib/econometrics.ts` + `src/shared/lib/econometrics.test.ts`. Wired nav via `src/shared/config.ts` + `src/app/router.ts` and added `econometrics.*` i18n keys to `src/shared/lib/localization/en.json`. |
| EX-034 | Expand Econometrics with entropy/Theil + Lorenz curve + supply dynamics + holder churn + multi-window velocity. | COMPLETED | Expanded `/econometrics` with Theil T + Shannon entropy (+ normalized + entropy-effective holders), Lorenz curve mini-chart, velocity windows (1h/24h/7d) with per-window coverage flags, Mint/Burn issuance windows + 30-day daily net issuance sparkline, and localStorage-based holder churn (retained/entered/exited + retention). Velocity/issuance scans detect instruction order (newest-first vs oldest-first) and guard progress updates so stale compute runs can’t clobber the UI. Added generic ISI extraction helpers in `src/shared/lib/econometrics.ts` and extended `src/shared/lib/econometrics.test.ts`. |
| EX-035 | Fix asset holders rendering when Torii returns explorer asset instances with non-legacy `AssetId` encoding (no `account@domain`). | COMPLETED | Updated `src/shared/api/schemas.ts` Asset payload to include `definition_id` + `account_id` and switched `/assets/:id`, `/accounts/:id` asset tables + `/econometrics` holders scan to rely on those fields instead of `AssetId.parse()`. Added `tests/e2e/assets-data.smoke.test.ts` regression coverage. |
| EX-036 | Stabilize e2e smoke tests under parallel Vitest runs (avoid 5s timeouts). | COMPLETED | Increased per-test timeouts to 20s in `tests/e2e/*.smoke.test.ts` to prevent flaky timeouts under load. |
| EX-037 | Expose authoritative asset-definition supply (total + governance locked/circulating) in Torii explorer payloads and wire into Econometrics. | COMPLETED | Torii explorer asset-definition DTO now includes `total_quantity` for all assets plus `locked_quantity`/`circulating_quantity` for the governance voting asset (bond escrow). Econometrics now displays these supply fields, adds supply coverage (scanned/definition), and uses circulating/definition supply as the velocity denominator when available (fallbacks to scanned holder supply). Updated `src/shared/api/schemas.ts` + tests. |
| EX-038 | Compute econometrics activity (velocity/issuance windows) server-side in Torii to avoid scan limits and payload heuristics. | COMPLETED | Torii now exposes `GET /v2/explorer/asset-definitions/{definition_id}/econometrics` returning committed-only Transfer/Mint/Burn aggregates for 1h/24h/7d windows + 30d daily net issuance; the Econometrics UI prefers this endpoint and falls back to client-side instruction scans when unavailable. |
| EX-039 | Improve Econometrics UX (asset list + click-to-compute) and expand `/assets` to also list asset instances. | COMPLETED | `/econometrics` now includes a paginated asset-definition list; clicking a row updates `?asset=...` and triggers compute immediately (no Compute button needed). `/assets` gained an “Asset holders” tab that lists asset instances (definition + domain + account + value) with an optional holder filter. Added coverage in `src/pages/Econometrics.test.ts` + `src/pages/AssetsList.test.ts`. |
| EX-040 | Compute econometrics snapshot (holders + distribution metrics) server-side in Torii (Gini/HHI/Theil/entropy/top-N/quantiles) so results aren’t biased by client scan limits. | COMPLETED | Torii now exposes `GET /v2/explorer/asset-definitions/{definition_id}/snapshot` returning holders total, total supply, top holders, Lorenz curve points, and distribution metrics computed from the world state; the Econometrics UI prefers this endpoint and falls back to client-side holder scans when unavailable. |
| EX-041 | Restore instruction/transaction detail rendering when Torii exposes Norito bytes as `scale` (instead of `encoded`). | COMPLETED | Torii returns `r#box.scale` for instructions and `rejection_reason.scale` for rejected transactions on some deployments; the explorer now accepts both, normalizes to `encoded`, and has schema regression tests to prevent silent instruction table blanking. |
| EX-042 | Add domain/owner filters to the Econometrics asset-definition list. | COMPLETED | `/econometrics` asset list now supports `domain` + `owned_by` filtering with the same labels/placeholders as `/assets`. |
| EX-043 | Avoid auto-refresh shifting paginated tables on SSE updates (transactions/instructions). | COMPLETED | The Transactions/Instructions tables now only auto-refetch from their SSE streams when the user is on the latest page (prevents “jumping” while browsing older pages). Added unit coverage for the gating logic. |
| EX-044 | Add Nakamoto coefficients + nearest-rank quantiles to asset snapshot distribution metrics. | COMPLETED | Torii snapshot distribution now includes `nakamoto_33/51/67` and uses nearest-rank quantiles for `p90/p99`; the Econometrics UI displays the Nakamoto metrics and matches quantile semantics for client-side fallback scans. |
| EX-045 | Restore polling semantics for `setupAsyncData({ interval })` (keep stale content rendered while polling). | COMPLETED | `setupAsyncData` now uses the provided `interval` as a real polling period (not just error-retry). Pages that already passed `{ interval: 5000 }` (e.g. `/blocks` list + telemetry metrics) now refresh automatically without blanking due to stale-state rendering. |
| EX-046 | Stop emitting `scale` in Torii explorer DTOs (Norito bytes are `encoded` only). | COMPLETED | Renamed `ExplorerInstructionBoxDto.scale` → `encoded` and `ExplorerTransactionRejectionDto.scale` → `encoded` in `../iroha/crates/iroha_torii/src/explorer.rs` and added unit assertions that the serialized JSON contains `encoded` and not `scale`. |
| EX-047 | Scope Econometrics local churn snapshots by selected node/network. | COMPLETED | Churn snapshots stored in `localStorage` are now keyed by the active Torii base URL so switching node presets doesn’t compare holder sets across different networks. |
| EX-048 | Enrich the Econometrics asset-definition picker list with useful columns. | COMPLETED | The `/econometrics` asset-definition list now shows instances count, definition total supply (when present), and definition owner, making it easier to pick the right asset without drilling into details. |
| EX-049 | Deduplicate default error toasts during polling/retry refresh. | COMPLETED | `setupAsyncData` now suppresses repeated identical errors until a successful fetch occurs again (prevents toast spam when a node is down). Added unit coverage. |
| EX-050 | Use stable row keys for tables (avoid DOM churn on refresh). | COMPLETED | `BaseTable` now accepts `rowKey` and keying is wired for the auto-refreshing block/transaction/instruction lists. |
| EX-051 | Avoid scroll-jumps on background refresh by pausing auto-refetch while scrolled down. | COMPLETED | Transactions/Instructions tables now defer SSE-triggered refetches when the user is scrolled down (shows inline “New data available” with Load). Blocks list polling now respects `setupAsyncData.pollWhen` to avoid shifting while reading. Added unit coverage. |
| EX-052 | Prevent SSE live-event lists from jumping while the user scrolls. | COMPLETED | KAIGI relay live-events now preserve internal scroll position on prepend. ZK proof stream adjusts window scroll to keep the viewport stable when new proofs arrive while scrolled down. Added unit coverage for both. |
| EX-053 | Add a `/blocks` “New data available” banner when auto-refresh is paused (avoid silent staleness). | COMPLETED | `/blocks` now runs a lightweight background probe while scrolled down on page 1; if the latest remote height exceeds the displayed max height, it shows an inline “New data available” + Load banner (Load scrolls to top + refetches). Added unit coverage in `src/pages/BlocksList.test.ts`. |
| EX-054 | Apply stable `rowKey` to all `BaseTable` usages (avoid DOM churn across the app). | COMPLETED | All `BaseTable` call sites now pass a stable `rowKey` (IDs/hashes/heights), including detail pages and telemetry tables, avoiding index-keyed DOM churn on refresh and pagination changes. |
| EX-055 | Translate `table.newDataAvailable` and `table.load` across all locales (and add a regression test). | COMPLETED | Added `table.newDataAvailable` + `table.load` to every shipped locale JSON and added a regression test in `src/shared/lib/localization/index.test.ts` to prevent fallback-to-English regressions. |
| EX-056 | Make `setupAsyncData({ interval, immediate: false })` still poll (timer starts even without an immediate fetch). | COMPLETED | `setupAsyncData` now always starts the interval timer when `interval` is provided; `immediate: false` only defers the first fetch. Added unit coverage in `src/shared/utils/setup-async-data.test.ts`. |
| EX-057 | Avoid index-keyed `v-for` churn in core UI components (tabs/dropdowns/nav/home stats). | COMPLETED | Replaced `:key="i"` with stable keys (`value`, `to`, `i18nKey`) in `BaseTabs`, dropdowns, the nav menu, and home stats widgets to avoid DOM reuse bugs when lists change. |
| EX-058 | Add a blocks SSE stream in Torii and use it to refresh `/blocks` without jumpy polling. | COMPLETED | Torii (`../iroha`) now exposes `GET /v2/explorer/blocks/stream` emitting committed blocks as Norito JSON via SSE. The Explorer `useBlockStream` listens via `EventSource`, and `/blocks` uses the stream to refetch when at the top or show the “New data available” banner while scrolled down (polling remains as fallback). |
| EX-059 | Use the blocks SSE stream for the home “Latest blocks” widget and suppress polling while the stream is connected. | COMPLETED | `LatestBlocks` now connects to `/v2/explorer/blocks/stream`, refetches on new block events, and disables 5s polling while `EventSource` is connected (polling remains as fallback when the stream is unsupported/closed). Added unit coverage in `src/widgets/latest-blocks/LatestBlocks.test.ts`. |
| EX-060 | Auto-detect Torii econometrics endpoints instead of requiring an explicit runtime-config flag. | COMPLETED | Econometrics now tries the Torii `/snapshot` + `/econometrics` endpoints by default and disables them only when they return `NOT_FOUND` (runtime-config can still force-disable by setting `toriiEconometricsEndpointsEnabled=false`). Updated unit coverage in `src/pages/Econometrics.test.ts`. |
| EX-061 | Make runtime config loading work under subpath deployments (`BASE_URL`). | COMPLETED | `loadRuntimeConfig()` now fetches `${import.meta.env.BASE_URL}config.json` and falls back to `/config.json` for root-hosted configs. Covered by `src/shared/runtime-config.test.ts`. |
| EX-062 | Make node switching reliable: reactive Torii base + SSE streams reconnect on node changes. | COMPLETED | `src/shared/api/index.ts` now keeps Torii base URL in reactive state (so UI hints update), and all Explorer SSE consumers (`useBlockStream`, transactions/instructions streams, sumeragi/telemetry streams) now use computed URLs so they auto-reconnect when the node changes. |
| EX-063 | Keep “New data available” banners visible while scrolled (sticky banners). | COMPLETED | Pending-refresh banners in `/blocks`, `TransactionsTable`, and `InstructionsTable` are now `position: sticky` so users see the “New data available” affordance even while reading lower rows. |
| EX-064 | Reduce redundant polling when SSE streams are connected. | COMPLETED | `LatestTransactions` now pauses its 5s polling interval while the `/v2/explorer/transactions/stream` SSE connection is `OPEN` (polling remains as fallback when the stream is closed/unavailable). |
| EX-065 | Make node switching refresh the active page without a full reload. | COMPLETED | `src/app/App.vue` now keys `RouterView` on the active Torii base URL so route components remount (and refetch) when the user changes nodes via the header selector. |
| EX-066 | Localize Econometrics navigation labels across all locales. | COMPLETED | Added `econometrics.nav/title/compute` to every shipped JSON locale and added a regression test in `src/shared/lib/localization/index.test.ts`. |
| EX-067 | Remove unused programmatic locale overrides now that JSON locale packs ship. | COMPLETED | Trimmed `src/shared/lib/localization/index.ts` to only keep programmatic poetic locales (`akk`/`egy`) and rely on JSON locale files for all other locales (reduces bundle/memory churn). |
| EX-068 | Document runtime `config.json` and provide a tracked example. | COMPLETED | Added `public/config.json.example`, documented `config.json` loading + keys in `README.md`, and gitignored `public/config.json`. |
| EX-069 | Add `/v2/telemetry/live` SSE in Torii and wire the Explorer to consume it (reduce polling + enable live peer status updates). | COMPLETED | Backend: implemented `GET /v2/telemetry/live` (Norito JSON SSE) emitting `peer_info`/`peer_status`/`network_status` plus a `first` bootstrap snapshot. Route wiring + OpenAPI + smoke coverage were revalidated in `../iroha` (including `/v2/gov/stream`) on 2026-02-18. Frontend: `useTelemetryMetrics` consumes the stream and pauses polling while connected; `/telemetry` shows stream indicators for consensus + nodes. |
| EX-070 | Decouple shared composables from the first consumer scope so SSE/polling keeps running until the last consumer unmounts. | COMPLETED | Shared telemetry/governance/sumeragi singleton composables now initialize inside detached `effectScope(true)` instances with explicit teardown, so SSE/polling survive creator unmounts and only stop after the final consumer leaves. Added multi-scope regression tests in `useTelemetryMetrics.test.ts` and `useGovernanceEvents.test.ts`, and re-ran targeted composable suites + `pnpm typecheck`. |
| EX-071 | Make Econometrics resilient when Torii snapshot/econometrics endpoints fail at runtime (network/CORS); fall back to holder scan + activity scan instead of hard failure. | COMPLETED | Reproduced on `https://explorer.soramitsu.io/econometrics?asset=PKR%23sbp`: browser CORS blocked Torii snapshot endpoint and UI failed with `Failed to fetch`. Frontend now catches snapshot fetch throws and falls back to holder scan (`src/pages/Econometrics.vue`) with regression coverage in `src/pages/Econometrics.test.ts`. SBP ingress nginx now re-adds Torii CORS headers on cached `/v2/explorer/asset-definitions/:id/{snapshot,econometrics}` locations, and CORS gate coverage was expanded in `pk-deploy/scripts/gates/check-cors-contract.sh`. |
| EX-072 | Fix Econometrics velocity/issuance table header overlap on mid-width desktop viewports. | COMPLETED | Updated `src/pages/Econometrics.vue` table grid CSS to use responsive `minmax(..., fr)` column tracks, added `min-width: 0` cell guards, and enabled header wrapping with `overflow-wrap: break-word` so labels like `Turnover (amount/supply)` and `Velocity/day (turnover/window)` no longer spill into adjacent columns (reproduced + validated at `1280x800` on `https://explorer.soramitsu.io/econometrics?asset=PKR%23sbp`). |
| EX-073 | Refresh light mode app-wide with a crisp-contrast visual system pass (tokens + shared components + key pages). | COMPLETED | Retuned light-theme token/shadow maps in `src/shared/ui/styles/theme.scss` (higher text/border contrast, cleaner surface hierarchy, reduced neumorphic haze), refined light chrome in `src/app/App.vue` + `src/widgets/header/TheHeader.vue`, and updated shared/page surfaces (`BaseContentBlock`, `BaseButton`, `SearchField`, `HomePage`, `HomePageInfo`, `KaigiRelays`) to use crisper borders, restrained shadows, and lower blur while keeping Sora + dark-mode/HDR branches intact. Verified with `pnpm typecheck` and targeted tests: `HomePage.test.ts`, `SearchField.test.ts`, `BaseTable.test.ts`, `KaigiRelayEvents.test.ts`. |
| EX-074 | Add consistent top spacing under the header for all non-home pages. | COMPLETED | Moved the top gap into `src/shared/ui/components/BasePageLayout.vue` (`padding-top: size(1)` on small screens, `size(1.5)` from `md`), then removed the `/blocks`-only top margin in `src/pages/BlocksList.vue` so `/blocks` keeps its existing visual spacing while all other non-home pages inherit the same gap. Verified with `pnpm exec vitest run src/pages/BlocksList.test.ts` and `pnpm build`. |
| EX-075 | Align `/accounts` filter row side spacing with table content in the same block. | COMPLETED | Added responsive horizontal padding on `src/pages/AccountsList.vue` for `accounts-list-page__filters` (`size(2)` default, `size(3)` on `sm`, back to `size(2)` on `md+`) to match table row side insets in the same card. Verified with `pnpm build`. |
| EX-076 | Align `/governance` body content side spacing with table/card insets. | COMPLETED | Added responsive side insets in `src/pages/GovernanceDashboard.vue` for `governance-page__panel`, `governance-page__stats`, and `governance-page__form` (`size(2)` default, `size(3)` on `sm`, back to `size(2)` on `md+`) and restored locks table row horizontal padding to the same spacing in `governance-page__locks-table .content-row`. Verified with `pnpm exec vitest run src/pages/GovernanceDashboard.test.ts` and `pnpm build`. |
| EX-077 | Add side insets for governance fallback prompt text blocks. | COMPLETED | Applied `governance-page__message` to governance fallback/prompt row-text blocks in `src/pages/GovernanceDashboard.vue` (including “Submit a referendum ID…” and “Enter a proposal ID…” prompts) and added responsive insets (`size(2)` default, `size(3)` on `sm`, back to `size(2)` on `md+`). Verified with `pnpm exec vitest run src/pages/GovernanceDashboard.test.ts` and `pnpm build`. |
| EX-078 | Make top header controls responsive so buttons never overflow outside the bar on medium-wide screens. | COMPLETED | Updated header breakpoints so desktop controls/nav no longer engage too early: `MobileMenu` now hides at `xl` (instead of `lg`), `NavigationMenu` now appears at `xl` (instead of `lg`), and `TheHeader` desktop-only controls now appear at `xl` with tighter `xl` button gap (`size(1)`, returning to `size(2)` at `xxl`). This keeps the compact menu path active until there is enough width and prevents top-bar overflow. Verified with `pnpm build`. |
| EX-079 | Prevent header button overflow at sub-xxl widths by delaying full desktop controls. | COMPLETED | Delayed full desktop header mode to `xxl`: `MobileMenu` now hides at `xxl`, while `NavigationMenu` and `TheHeader` desktop-only controls now appear at `xxl`. This keeps compact/mobile controls active through `xl` widths and prevents button overflow outside the top bar on narrower desktops. Verified with `pnpm build`. |
| EX-080 | Make header controls progressively responsive (hide only heavier buttons first, not everything). | COMPLETED | Implemented staged visibility in `src/widgets/header/TheHeader.vue`: theme toggle now appears from `lg`, language dropdown from `xl`, and node selector only from `xxl` (split into dedicated classes). Kept `MobileMenu` visible until `xxl` so hidden actions remain accessible, and kept `NavigationMenu` at `xxl` to avoid reintroducing overflow pressure. Verified with `pnpm build`. |
| EX-081 | Tune header controls to show most actions while clipping overflow safely. | COMPLETED | Updated `src/widgets/header/TheHeader.vue` so the header button row uses flex + overflow clipping (`min-width: 0; overflow: hidden`) and stages visibility as requested: theme + language from `lg`, node selector from `xxl`. This keeps most controls visible while preventing spillover outside the top bar. `NavigationMenu` remains at `xxl` and `MobileMenu` stays available until `xxl` for hidden actions. Verified with `pnpm build`. |
| EX-082 | Keep language dropdown anchored to the language button after responsive header reflow. | COMPLETED | Updated `src/features/switch-lang/LangDropdown.vue` to teleport the language list to `body` and position it with fixed coordinates derived from the trigger button (`getBoundingClientRect`), with live repositioning on open/resize/scroll and RTL-aware left/right anchoring. Updated `src/features/switch-lang/LangDropdown.test.ts` to assert teleported dropdown behavior under the new `body` anchor. Verified with `pnpm exec vitest run src/features/switch-lang/LangDropdown.test.ts` and `pnpm build`. |
| EX-083 | Keep most header buttons always rendered and clip overflow inside the top bar. | COMPLETED | Removed per-button desktop visibility wrappers in `src/widgets/header/TheHeader.vue` so node/theme/language controls stay rendered, made the header nav/buttons lanes clip overflow (`min-width: 0; overflow: hidden`) with right-aligned control clipping, and restored `NavigationMenu` visibility from `xl` while keeping overflow contained (`src/features/navigation/NavigationMenu.vue`). Verified with `pnpm build`. |
| EX-084 | Make header controls truly window-responsive (fit-to-width) rather than breakpoint-only clipping. | COMPLETED | `src/widgets/header/TheHeader.vue` now measures real header width (ResizeObserver + `requestAnimationFrame`) and applies priority fitting on every resize/locale change: start with all controls visible, hide nav if it overflows, then hide lower-priority controls (`NodeSettings` → `LangDropdown` → `ThemeSwitcher`) only when button lane still overflows. `MobileMenu` is forced visible whenever anything is collapsed via `mobile-menu_force-visible` (`src/features/mobile-menu/MobileMenu.vue`) so hidden actions remain accessible. Verified with `pnpm build`. |
| EX-085 | Keep language dropdown list anchored to the language button after responsive header relayouts. | COMPLETED | Updated `src/features/switch-lang/LangDropdown.vue` to position the teleported list with measured fixed `left/top` coordinates (viewport clamped) based on trigger + dropdown rects, and added `ResizeObserver` tracking on both trigger and dropdown so anchor position updates when layout shifts. Added regression test coverage in `src/features/switch-lang/LangDropdown.test.ts` for trigger-based placement and re-ran `pnpm exec vitest run src/features/switch-lang/LangDropdown.test.ts` plus `pnpm build`. |
| EX-086 | Re-arrange top bar to never hide controls; wrap to multiple rows when one row is too narrow. | COMPLETED | Header now keeps nav + action controls always rendered: removed desktop-only wrappers and hide breakpoints in `TheHeader`, made nav/buttons lanes flex-wrap, and kept `MobileMenu` visible at all sizes so no controls disappear. Also relaxed the node-selector button nowrap rule on small screens so long localized labels wrap instead of pushing outside the bar. Verified with `pnpm exec vitest run src/widgets/header/TheHeader.test.ts` and `pnpm build`. |
| EX-087 | Reduce avoidable top-bar line breaks by separating logo/actions from nav into a two-row responsive layout. | COMPLETED | Refactored `src/widgets/header/TheHeader.vue` to CSS grid with explicit rows: row 1 (`logo` + controls), row 2 (full-width navigation). This stops nav/control width contention that caused unnecessary third-line wrapping on medium desktop widths (e.g. `/accounts/:id`) while still keeping all controls visible. Verified with `pnpm exec vitest run src/widgets/header/TheHeader.test.ts` and `pnpm build`. |
| EX-088 | Prevent controls-lane squeezing that causes theme/language buttons to spill into an extra row on narrower viewports. | COMPLETED | Updated `src/widgets/header/TheHeader.vue` to stack header lanes by default (`logo`, then `controls`, then `nav`) and only switch to side-by-side `logo + controls` from `md` upward. This removes the narrow-width squeeze that was isolating theme/language on an extra controls row. Verified with `pnpm exec vitest run src/widgets/header/TheHeader.test.ts` and `pnpm build`. |
| EX-089 | Prevent controls from splitting into inefficient extra rows by prioritizing compact one-line packing. | COMPLETED | Updated `src/widgets/header/TheHeader.vue` so the controls lane is `nowrap` with priority sizing (node selector gets flexible width; icon controls stay fixed). Added node-selector label truncation in `src/features/node-settings/NodeSettings.vue` and delayed language button text/arrow expansion to `xl` in `src/features/switch-lang/LangDropdown.vue`. This keeps theme/language from being pushed onto their own row on medium widths. Verified with `pnpm exec vitest run src/widgets/header/TheHeader.test.ts src/features/switch-lang/LangDropdown.test.ts` and `pnpm build`. |
| EX-090 | Remove obsolete overflow (`...`) control from the non-home header now that all controls stay visible. | COMPLETED | Removed `MobileMenu` from `src/widgets/header/TheHeader.vue` so non-home pages no longer show the `...` top-bar control; retained direct controls only (`NodeSettings`, `ThemeSwitcher`, `LangDropdown`). Updated `src/widgets/header/TheHeader.test.ts` expectations accordingly. Verified with `pnpm exec vitest run src/widgets/header/TheHeader.test.ts src/features/switch-lang/LangDropdown.test.ts src/pages/HomePage.test.ts` and `pnpm build`. |
| EX-091 | Eliminate forced three-lane stacking on sub-`md` widths so header remains space-efficient (two lanes: top controls + nav). | COMPLETED | Updated `src/widgets/header/TheHeader.vue` to keep `logo + controls` side-by-side at all widths (with nav as the second row), removing the forced stacked middle controls lane. Tightened small-width packing via smaller base logo width in `src/widgets/header/HeaderLogo.vue` and reduced node-selector button padding in `src/features/node-settings/NodeSettings.vue` so node/theme/language remain in the top controls lane more reliably. Verified with `pnpm exec vitest run src/widgets/header/TheHeader.test.ts src/features/switch-lang/LangDropdown.test.ts src/pages/HomePage.test.ts` and `pnpm build`. |
| EX-092 | Fix `/accounts/:id` authority filter overflow caused by fixed transaction row heights. | COMPLETED | Scoped the account-details compact-height override in `src/pages/AccountDetails.vue` from `.transactions-table .content-row` to `.transactions-table__container .content-row`, so only transaction body rows stay compact while filter rows keep natural height. This resolves authority filter text/input overflow on account details pages. Verified with `pnpm build` and targeted regression checks (`HomePage`, `TheHeader`, `LangDropdown` tests). |
| EX-093 | Prevent `/accounts/:id` transactions panel from overflowing horizontally on narrow/intermediate widths. | COMPLETED | Updated `src/pages/AccountDetails.vue` to use container-relative widths in account layout/transactions (`100%` on small screens, `%` at `lg`) instead of viewport widths (`vw`), and removed per-row `vw` widths in `account-details__transactions-table_short .content-row` (now `width/max-width: 100%`). This keeps account transactions within the screen across breakpoint transitions. Verified with `pnpm build` and targeted regression checks (`HomePage`, `TheHeader`, `LangDropdown` tests). |
| EX-094 | Prevent node selector from over-expanding and consuming most of the header controls row. | COMPLETED | Updated `src/widgets/header/TheHeader.vue` to stop node selector flex-grow (`flex: 0 1 size(28)`), apply explicit width bounds (`min-width: size(14)`, `max-width: size(34)`, `size(38)` at `xxl`), and keep existing truncation behavior from `NodeSettings`. This prevents the node button from taking most of the controls lane while preserving compact layout. Verified with `pnpm exec vitest run src/widgets/header/TheHeader.test.ts src/features/switch-lang/LangDropdown.test.ts src/pages/HomePage.test.ts` and `pnpm build`. |
| EX-095 | Make header node selector compact by default (avoid oversized footprint in controls lane). | COMPLETED | Tightened node control sizing in `src/widgets/header/TheHeader.vue` by removing growth behavior and adding stricter max widths (`size(24)` default, `size(28)` at `xxl`) while retaining truncation. Updated `src/features/node-settings/NodeSettings.vue` so the inner button is `width: auto` with capped label width (`size(20)`) instead of forcing full-width. Verified with `pnpm exec vitest run src/widgets/header/TheHeader.test.ts src/features/switch-lang/LangDropdown.test.ts src/pages/HomePage.test.ts` and `pnpm build`. |
| EX-096 | Remove compressed-address warning copy from account details UI. | COMPLETED | Removed the rendered warning paragraph (`accounts.accountAddressCompressedWarning`) from `src/pages/AccountDetails.vue` compressed-address card and deleted the now-unused `account-details__address-warning` style block. Verified with `pnpm build`. |
| EX-097 | Fix copy-button placement for account-address and compressed fields on account details. | COMPLETED | Refactored `src/pages/AccountDetails.vue` address cards so both canonical account-address and compressed variants use the same header-aligned `CopyIcon` control (instead of mixed inline icon vs text button), keeping copy actions consistently positioned and compact across breakpoints. Also consolidated address copy handling into one function with context-aware success toasts. Verified with `pnpm build`. |
| EX-098 | Remove unnecessary left whitespace before header node control in the top controls lane. | COMPLETED | Updated `src/widgets/header/TheHeader.vue` controls container (`app-header__buttons`) to shrink-to-content (`width: fit-content; max-width: 100%`) and anchor right (`justify-self: end; margin-inline-start: auto`) instead of stretching full-width. This removes the visually wasted left gap before the node button while preserving right-edge alignment of controls. Verified with `pnpm build`. |
| EX-099 | Make dark-mode hover highlights respect rounded card geometry on account/details surfaces. | COMPLETED | Updated shared card styling in `src/shared/ui/components/BaseContentBlock.vue` by isolating blend compositing (`isolation: isolate`) and applying `border-radius: inherit` on the animated `::before` sheen layer. This prevents square-edged hover highlight artifacts in dark mode and keeps highlights clipped to rounded card corners. Verified with `pnpm build`. |
| EX-100 | Show multisig instructions with a friendly label instead of generic `Custom` in transaction instruction views. | COMPLETED | `src/shared/ui/components/InstructionsTable.vue` now detects multisig-shaped custom payloads (`Register`/`Propose`/`Approve` with account-bearing bodies) and renders a friendly `Multisig` kind label in instruction rows and detail metadata, while keeping raw API/filter kinds unchanged (`Custom` remains the filter value). Added `transactions.multisig` localization for all shipped JSON locale packs plus poetic overrides in `akk`/`egy`, and added localization regression coverage (`src/shared/lib/localization/index.test.ts`) to keep `transactions.*` key parity across locales. Existing instruction rendering coverage remains in `src/shared/ui/components/InstructionsTable.test.ts` for multisig custom + non-multisig custom paths. Verified with `pnpm exec vitest run src/shared/ui/components/InstructionsTable.test.ts src/shared/lib/localization/index.test.ts` and `pnpm build`. |
| EX-101 | Add Sora Nexus dataspaces support via Torii-first API contract and ship an Explorer `/dataspaces` page. | COMPLETED | Torii now exposes `GET /v2/nexus/dataspaces/accounts/{literal}/summary` (literal resolution + UAID/bindings/manifest/portfolio/consensus joins) with route + OpenAPI wiring in `../iroha/crates/iroha_torii`. Explorer now consumes it via `fetchNexusDataspacesAccountSummary`, includes schema coverage (`src/shared/api/schemas.test.ts`), API URL coverage (`src/shared/api/index.test.ts`), and ships a new `/dataspaces` page (`src/pages/Dataspaces.vue` + `src/pages/Dataspaces.test.ts`) linked in router/menu/localization. Verification now also includes an HTTP integration test for the new Torii endpoint (`../iroha/crates/iroha_torii/tests/nexus_dataspaces_summary.rs`, command: `cargo test -p iroha_torii --test nexus_dataspaces_summary -- --nocapture`) in addition to helper-unit coverage (`cargo test -p iroha_torii nexus_dataspaces_summary_tests -- --nocapture`) and frontend checks (`pnpm exec vitest run src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/pages/Dataspaces.test.ts src/shared/ui/components/InstructionsTable.test.ts`, `pnpm build`). Follow-up gap closure adds Torii negative-path HTTP coverage (invalid literal/empty literal/invalid `address_format`/missing account/`nexus_disabled`), Dataspaces page edge-case tests (required input, generic failure, in-flight disable, selected-row reset), and locale-key parity checks plus localized (non-English) `dataspaces.*` values across all shipped JSON locale packs. |
| EX-102 | Restore rich poetic programmatic locale layers for `akk`/`egy` and extend them to new UI areas. | COMPLETED | Reinstated the broader Akkadian/Hieroglyphic semantic locale overrides in `src/shared/lib/localization/index.ts` (home/search/widgets/transactions/governance/telemetry/KAIGI/SoraFS/settings) and added poetic `dataspaces.*` overrides so these programmatic locales no longer fall back to plain English for newer pages. Follow-up refinement replaced remaining plain-English-heavy Akkadian account/dataspace phrasing with semantic poetic copy (not phonetic transliteration). Added regression checks in `src/shared/lib/localization/index.test.ts` to ensure core poetic labels remain present. |
| EX-103 | Fill missing `econometrics.*` localization keys across all shipped JSON locales and enforce key parity. | COMPLETED | Backfilled the missing 77 econometrics leaf keys for every shipped JSON locale (`src/shared/lib/localization/*.json`) and completed targeted cleanup for fallback-heavy locales (Amharic/Dzongkha/Divehi/Azerbaijani) so non-technical econometrics labels no longer fall back to English. Added regression coverage in `src/shared/lib/localization/index.test.ts` to enforce `econometrics.*` key parity against `en.json`. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts` and `pnpm build`. |
| EX-104 | Improve failed transaction diagnostics by decoding account-level `FindError` payloads into readable identifiers. | COMPLETED | Extended `src/shared/api/rejection-reason.ts` to decode `FindError::Account` (`publicKey@domain`) and `FindError::PublicKey`, so transaction details show actionable failure reasons (example `c9d2df...`: `Failed to find account: ...@sbp`) instead of `FindError(3)`. Added regression coverage in `src/shared/api/rejection-reason.test.ts` (exact payload + malformed fallback). Verified with `pnpm exec vitest run src/shared/api/rejection-reason.test.ts` and `pnpm typecheck`. |
| EX-105 | Redesign `/telemetry` to an ethstats-style live network board (node cards, latency/ping proxies, propagation graphs, and richer rollups). | COMPLETED | `/telemetry` now includes an ethstats-style “Live network pulse” section in `src/pages/NodesTelemetry.vue`: KPI tiles (live/reporting/lagging nodes, p95 commit latency, propagation spread), rolling sparklines (median commit latency, propagation spread, queue pressure), and compact per-node cards (status, block delta, commit-latency ping proxy, estimated propagation, queue usage bar). Added telemetry math/render helpers (`calculateQueueFillPercent`, `calculatePercentile`, `calculatePropagationSpread`, `buildSparklinePoints`, `formatLatencyMs`) in `src/shared/ui/utils/node-telemetry.ts` with regression coverage in `src/shared/ui/utils/node-telemetry.test.ts`, expanded page checks in `src/pages/NodesTelemetry.test.ts`, and added new telemetry labels in `src/shared/lib/localization/en.json`. Verified with `pnpm exec vitest run src/shared/ui/utils/node-telemetry.test.ts src/pages/NodesTelemetry.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-106 | Close ethstats telemetry gaps with real RTT + observed block propagation data and a map/graph board mode. | COMPLETED | Upstream Torii (`../iroha`) now emits real `/status` RTT metrics (`status_rtt`, avg, p95) in peer status, tracks per-block propagation spread across peers, serves `GET /v2/telemetry/propagation`, and includes propagation in `/v2/telemetry/live` (`first` bootstrap + `propagation` delta kind). Explorer now parses the expanded telemetry schemas (`PeerStatus`, `PeerPropagation`, live SSE union), fetches propagation snapshots in `usePeersTelemetry`, uses true RTT/observed propagation on `/telemetry` with derived fallback, adds a user-selectable history window (24/48/96 points), and upgrades map mode to interactive pan/zoom + hover tooltip + marker-to-card selection. New telemetry strings are now localized natively across every shipped JSON locale pack (no English placeholder keys left for this feature). Verification: `cargo test -p iroha_torii --features telemetry,app_api --lib peers_status_reflects_metrics_updates -- --nocapture`, `cargo test -p iroha_torii --features telemetry,app_api --lib peers_status_computes_propagation_from_first_seen_timestamp -- --nocapture`, `cargo test -p iroha_torii --features telemetry,app_api --lib latency_window_calculates_average_and_percentile -- --nocapture`, `cargo test -p iroha_torii --features telemetry,app_api --lib latency_window_keeps_latest_samples_only -- --nocapture`, `cargo test -p iroha_torii --features telemetry,app_api --test app_api_router_smoke app_api_router_smoke -- --nocapture`, `pnpm exec vitest run src/shared/ui/utils/node-telemetry.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts src/pages/NodesTelemetry.test.ts src/shared/api/index.test.ts src/shared/lib/localization/index.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-107 | Make transaction instruction type immediately obvious on instruction detail links (`/transactions/:hash?instruction=...`). | COMPLETED | Updated `src/shared/ui/components/InstructionsTable.vue` so instruction kind is now surfaced prominently in the detail header (`data-test=\"instruction-detail-kind\"`) and rendered as a high-contrast badge/chip in both detail header and table rows, making the type visible at a glance even when landing via `?instruction=` deep links. Kept detail metadata `Kind` field aligned with the same computed label source, and expanded `src/shared/ui/components/InstructionsTable.test.ts` assertions for the new header-kind indicator (`Register` + `Multisig`). Verified with `pnpm exec vitest run src/shared/ui/components/InstructionsTable.test.ts` and `pnpm build`. |
| EX-108 | Prevent `/telemetry` from showing `0` live nodes when peer-detail telemetry is empty but network metrics still report peers. | COMPLETED | Updated `src/pages/NodesTelemetry.vue` live KPI computation to fall back to `metrics.peers` when the peer-detail rows are empty (`peers_info`/`peers_status` unavailable), so `Live nodes` and `Reporting nodes` reflect network-level peer count instead of `0`. Added regression coverage in `src/pages/NodesTelemetry.test.ts` (`falls back to network peer count when peer details are unavailable`) and normalized telemetry test metrics fixture setup. Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts` and `pnpm build`. |
| EX-109 | Use gossip topology hints (`connected_peers`) to estimate live node counts when only partial peer telemetry is available. | COMPLETED | Updated `src/pages/NodesTelemetry.vue` to include gossip-derived topology in KPI estimation: when partial peer rows exist, `connected_peers` IDs are deduplicated against known peer public keys and used to expand `Live nodes` totals/online counts (with `metrics.peers` still acting as an upper fallback). This ensures a connected node can represent discovered peers from gossip even when full status rows are missing. Added regression coverage in `src/pages/NodesTelemetry.test.ts` (single-row + three gossip peers now renders `Live nodes` as `4 / 4` while `Reporting nodes` remains `1`). Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts` and `pnpm build`. |
| EX-110 | Surface clear failed-transaction error messages end-to-end (Torii-provided human text + frontend fallback decoding for current payloads). | COMPLETED | Reproduced rejected tx `14b6d3a4...` (only encoded Norito bytes in production payload), then patched Torii (`../iroha/crates/iroha_torii/src/explorer.rs`) so transaction detail now emits `rejection_reason.message` with nested validation/instruction context (including repetition/find details). Frontend now parses optional `rejection_reason.message` in `src/shared/api/schemas.ts`, prefers it on `/transactions/:hash` (`src/pages/TransactionDetails.vue`), keeps backward-compatible decoding by extending `src/shared/api/rejection-reason.ts` with repetition-id extraction (`Repeated instruction for id: sbp`) for older Torii responses, and treats opaque Torii tag-only strings (e.g. `InstructionExecutionError(4)`) as low-signal so decoded fallback text is shown instead. Added/updated tests in `../iroha` explorer DTO tests (`transaction_detail_includes_rejection_reason`, `transaction_detail_includes_repetition_error_context_in_message`), `src/shared/api/rejection-reason.test.ts`, `src/shared/api/schemas.test.ts`, and `src/pages/TransactionDetails.test.ts`. Verified with `cargo test -p iroha_torii transaction_detail_includes_rejection_reason -- --nocapture`, `cargo test -p iroha_torii transaction_detail_includes_repetition_error_context_in_message -- --nocapture`, `pnpm exec vitest run src/pages/TransactionDetails.test.ts src/shared/api/rejection-reason.test.ts src/shared/api/schemas.test.ts`, and `pnpm build`. |
| EX-111 | Keep long Norito instruction payloads inside a responsive detail box on `/transactions/:hash?instruction=...`. | COMPLETED | Updated `src/shared/ui/components/InstructionsTable.vue` encoded-payload section to render inside a dedicated responsive box (`instructions-detail__encoded-box`) with `minmax(0, 1fr)` layout, full-width bounded `<pre>`, viewport-capped height, and wrapped/scrollable long Norito strings so payloads stay within screen bounds. Added regression assertions in `src/shared/ui/components/InstructionsTable.test.ts` for the encoded payload container. Verified with `pnpm exec vitest run src/shared/ui/components/InstructionsTable.test.ts` and `pnpm build`. |
| EX-112 | Restore inner table padding on `/sorafs/registry` and `/zk/telemetry` so rows do not render edge-to-edge. | COMPLETED | Updated `src/pages/SorafsRegistry.vue` and `src/pages/ZkTelemetry.vue` table styling to apply explicit horizontal row padding for both BaseTable headers (`.base-table > .content-row`) and body rows (`__table .content-row`), with larger inline spacing on `lg+` breakpoints. Replaced ZK telemetry’s previous `padding: size(1.5) 0` rule that forced zero horizontal inset. Verified with `pnpm exec vitest run src/pages/ZkTelemetry.test.ts` and `pnpm build`. |
| EX-113 | Add horizontal padding for the `/kaigi/relays` domain-metrics empty-state text (“No domain metrics yet”). | COMPLETED | Updated `src/pages/KaigiRelays.vue` to render the fallback with `kaigi-relays-page__empty row-text` and added `&__empty { padding: size(2.5) size(3); }` so the message has the same side inset as table content. Verified with `pnpm build`. |
| EX-114 | Fix remaining table-adjacent empty-state `row-text` fallback(s) lacking horizontal padding. | COMPLETED | Added side insets to every remaining matching fallback: `src/pages/BlockDetails.vue` now uses `display: block; padding-inline: size(4);` for `.block-details__transactions_empty` (replacing margin-left-only), and `src/pages/NodesTelemetry.vue` now applies the same block-level logical side padding for `.nodes-telemetry-page__ethstats-empty`, `__rbc-evictions-empty`, `__nexus-staking-empty`, and `__lane-governance-empty`. Verified with `pnpm build`. |
| EX-115 | Add clearer block producer and Sumeragi-chain details on `/telemetry` (current producer, producer roster, membership snapshot). | COMPLETED | `/telemetry` now shows a Sumeragi chain snapshot (membership height/view/epoch + membership view hash), a block producer board driven by `leader_index` + `npos_election.validator_set` (highlighting the current producer), and a dedicated current-producer hash row in `src/pages/NodesTelemetry.vue`. Added regression coverage in `src/pages/NodesTelemetry.test.ts` (`renders sumeragi chain and block producer details`). Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts` and `pnpm build`. |
| EX-116 | Polish `/telemetry` Sumeragi-chain and block-producer UI layout for better readability and mobile behavior. | COMPLETED | Refined `src/pages/NodesTelemetry.vue` with card-style Sumeragi chain metrics (improved visual hierarchy), producer summary chips (`validator_set` count + leader index), emphasized leader row styling, and horizontal overflow handling for producer tables on narrow screens. Kept the same Torii payload semantics. Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts` and `pnpm build`. |
| EX-117 | Prevent `/telemetry` Nodes table from showing empty while stream is connected but peer telemetry payloads are unavailable. | COMPLETED | Reproduced on production (`https://explorer.soramitsu.io/telemetry` / `https://torii.soramitsu.io`): metrics reported peers while `/v2/telemetry/peers-info` + `/v2/telemetry/propagation` returned empty arrays. Added Torii-root `/peers` fallback in frontend (`fetchOnlinePeers` + `usePeersTelemetry`) that hydrates placeholder node rows (parsed `public_key@host:port` entries, preserves real telemetry rows when available). Added regression coverage in `src/shared/ui/composables/usePeersTelemetry.test.ts` (fallback hydration + non-override behavior) and API coverage in `src/shared/api/index.test.ts` for `/peers`. Verified with `pnpm exec vitest run src/shared/ui/composables/usePeersTelemetry.test.ts src/shared/api/index.test.ts src/pages/NodesTelemetry.test.ts` and `pnpm typecheck`. |
| EX-118 | Add explicit telemetry trust badges on `/telemetry` (source + sample age + freshness) so fallback/snapshot modes are clear. | COMPLETED | `/telemetry` now shows per-section trust badges (Live pulse, Sumeragi consensus, Nodes): stream state, data source (`Live SSE`/`Snapshot`/`Fallback /peers`/`Mixed`), and sample age with freshness labels (`fresh`/`delayed`/`stale`/`unknown`). `usePeersTelemetry` now exposes `peerDataSource`, `lastPeerSampleAtMs`, and `fallbackPeersCount`; `useSumeragiStatus` now exposes `lastUpdatedAtMs`. Added UI/composable regression coverage (`src/pages/NodesTelemetry.test.ts`, `src/shared/ui/composables/usePeersTelemetry.test.ts`, `src/shared/ui/composables/useSumeragiStatus.test.ts`) plus API coverage remains in `src/shared/api/index.test.ts`. Verified with `pnpm exec vitest run src/shared/ui/composables/usePeersTelemetry.test.ts src/shared/ui/composables/useSumeragiStatus.test.ts src/pages/NodesTelemetry.test.ts src/shared/api/index.test.ts` and `pnpm typecheck`. |
| EX-119 | Refine telemetry trust UX clarity (`live` vs `snapshot` source accuracy + explicit fallback warning + freshness scale hint). | COMPLETED | `usePeersTelemetry` now tracks row origin as `live`/`snapshot`/`fallback` (with live precedence over late snapshot refreshes), reports `MIXED` when provenance is combined, and keeps `lastPeerSampleAtMs`/`fallbackPeersCount` aligned with that provenance. `/telemetry` now shows freshness-scale hint (`Fresh <= 15s · Delayed <= 60s`) and an explicit fallback warning when `/peers` data is being used (`FALLBACK`/`MIXED`). Added regression coverage for mixed-source classification in `src/shared/ui/composables/usePeersTelemetry.test.ts` and fallback notice rendering in `src/pages/NodesTelemetry.test.ts`. Verified with `pnpm exec vitest run src/shared/ui/composables/usePeersTelemetry.test.ts src/pages/NodesTelemetry.test.ts src/shared/ui/composables/useSumeragiStatus.test.ts` and `pnpm typecheck`. |
| EX-120 | Show concrete ISI names for `Custom` instructions in transaction details/lists by deriving them from payload wire metadata. | COMPLETED | `src/shared/ui/components/InstructionsTable.vue` now resolves custom instruction labels from structured Norito metadata in priority order: multisig-friendly label, `wire_id` (`json.wire_id`, `payload.wire_id`, `payload.value.wire_id`), then non-placeholder `payload.variant`, with conservative legacy single-key fallback and final fallback to `Custom`. This fixes production tx `04c5deb6...` (`SubmitOfflineToOnlineTransfer` now shown instead of generic `Custom`). Added regression coverage in `src/shared/ui/components/InstructionsTable.test.ts` for `wire_id`-derived + `variant`-derived custom labels while preserving non-ISI payload fallback to `Custom`. Verified with `pnpm exec vitest run src/shared/ui/components/InstructionsTable.test.ts` and `pnpm build`. |
| EX-121 | Add telemetry ops-console interactions on `/telemetry` (quick filters, pin/compare panel, raw-event drawer) with cyberpunk-styled controls. | COMPLETED | `/telemetry` Live network pulse now includes quick filter chips (`All`/`Online`/`Lagging`/`Unsupported`), per-node pin/compare actions with a side-by-side compare panel, and a toggleable raw-events drawer. `src/shared/ui/composables/usePeersTelemetry.ts` now exposes a bounded `recentEvents` feed (snapshot/SSE/fallback event kinds + payloads) consumed by the drawer. Added regression coverage in `src/pages/NodesTelemetry.test.ts` (filters + pin/compare + drawer flow) and `src/shared/ui/composables/usePeersTelemetry.test.ts` (event feed capture/order). Verified with `pnpm exec vitest run src/shared/ui/composables/usePeersTelemetry.test.ts src/pages/NodesTelemetry.test.ts` and `pnpm typecheck`. |
| EX-122 | Emit concrete ISI names from Torii explorer instruction DTOs when instruction kind is currently classified as `Custom`. | COMPLETED | Updated `../iroha/crates/iroha_torii/src/explorer.rs` so unmapped/custom-classified instructions now expose concrete ISI labels derived from `wire_id`: `ExplorerInstructionDto.kind` uses wire-tail labels (e.g., `SubmitOfflineToOnlineTransfer` / `AddSignatory`) while preserving canonical filter classification (`ExplorerInstructionKind::Custom`), and fallback structured payloads now set `payload.variant` to the wire-tail label instead of `Unknown`. Frontend compatibility in this repo: `src/shared/api/schemas.ts` now accepts dynamic instruction `kind` strings, `src/shared/ui/components/InstructionsTable.vue` stream-refresh kind matching checks both top-level `kind` and `box.json.kind`, and schema/table regressions were extended in `src/shared/api/schemas.test.ts` + `src/shared/ui/components/InstructionsTable.test.ts`. Verified with `pnpm exec vitest run src/shared/ui/components/InstructionsTable.test.ts src/shared/api/schemas.test.ts`, `pnpm build`, and Torii tests: `cargo test -p iroha_torii instruction_dto_uses_wire_variant_for_unmapped_isi_kind -- --nocapture`, `cargo test -p iroha_torii fallback_structured_payload_uses_wire_variant_for_unmapped_isi -- --nocapture`. |
| EX-123 | Close telemetry ops UX gaps: localized ops labels, accessibility semantics, event search/copy/export tooling, and persistent/shareable console state. | COMPLETED | `/telemetry` ops console now ships localized search/copy/export actions, ARIA semantics for filters/drawer/event list options, absolute timestamps, and URL+localStorage state sync (`pf/pp/pc/pd/pe/ps`) for shareable/persistent sessions in `src/pages/NodesTelemetry.vue`. Backfilled required telemetry ops keys across all shipped JSON locale packs and added locale regression coverage in `src/shared/lib/localization/index.test.ts`. Expanded page tests in `src/pages/NodesTelemetry.test.ts` for drawer a11y/search/state persistence. Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts src/shared/lib/localization/index.test.ts` and `pnpm typecheck`. |
| EX-124 | Replace telemetry ops English backfills in non-English locale JSON packs with locale-native action labels. | COMPLETED | Updated `telemetry.searchEvents`, `searchEventsPlaceholder`, `copyEventJson`, `exportEvents`, and `eventsExported` in all shipped non-English JSON locales (`src/shared/lib/localization/*.json`). Added regression coverage in `src/shared/lib/localization/index.test.ts` to assert these action labels stay non-English outside `en.json`. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts src/pages/NodesTelemetry.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts` and `pnpm typecheck`. |
| EX-125 | Optimize `/telemetry` raw-event drawer filtering to avoid repeated payload serialization on each search keystroke. | COMPLETED | Added a computed search index in `src/pages/NodesTelemetry.vue` that precomputes lowercased summary/payload fields per event and reuses them for query filtering, reducing per-keystroke JSON stringify work in the drawer. Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts` and `pnpm typecheck`. |
| EX-126 | Localize the full telemetry ops-console label set (filters/source freshness/events metadata) for high-traffic non-English locales. | COMPLETED | Localized all 37 ops-console telemetry labels for `ar`, `az`, `de`, `es`, `fr`, `he`, `id`, `jp`, `ko`, `ru`, `tr`, `uk`, and `zh-TW` (`src/shared/lib/localization/*.json`) and added strict regression coverage in `src/shared/lib/localization/index.test.ts` to keep those labels non-English versus `en.json`. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts src/pages/NodesTelemetry.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts` and `pnpm typecheck`. |
| EX-127 | Localize the remaining telemetry ops-console label set for the rest of shipped non-English locale packs. | COMPLETED | Localized the same 37 telemetry ops labels for the remaining locale JSON packs (`am`, `dv`, `dz`, `hy`, `ka`, `kh`, `my`, `ne`, `si`, `ta`, `th`, `ur`) so all shipped non-English locales now diverge from `en.json` across the ops-console label set. Extended regression coverage in `src/shared/lib/localization/index.test.ts` to enforce this across every non-English JSON locale. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts src/pages/NodesTelemetry.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-128 | Improve raw-events drawer keyboard accessibility with listbox navigation semantics (`ArrowUp/ArrowDown/Home/End` + active descendant). | COMPLETED | Added listbox keyboard handlers (`ArrowUp`/`ArrowDown`/`Home`/`End`) and `aria-activedescendant` wiring in `src/pages/NodesTelemetry.vue`, with option IDs + focus updates tied to selection state. Expanded telemetry page regression coverage in `src/pages/NodesTelemetry.test.ts` to assert keyboard navigation and active descendant updates. Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts src/shared/lib/localization/index.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts` and `pnpm typecheck`. |
| EX-129 | Add keyboard-first search reset in telemetry raw-events drawer (`Esc` clears search query). | COMPLETED | Added `Esc` keyboard handling on the raw-events search input in `src/pages/NodesTelemetry.vue` (`@keydown.esc.stop.prevent`) so operators can clear search instantly from no-result states. Extended `src/pages/NodesTelemetry.test.ts` to verify `Esc` resets the search input and restores matching events. Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts src/shared/lib/localization/index.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts` and `pnpm typecheck`. |
| EX-130 | Add live raw-events match counter (`filtered / total`) in telemetry ops drawer toolbar. | COMPLETED | Added a compact live match counter in the raw-events toolbar (`data-test=\"pulse-events-count\"`) in `src/pages/NodesTelemetry.vue`, bound to `filteredTelemetryEvents / telemetryEvents` so operators can see search impact at a glance. Extended telemetry page tests in `src/pages/NodesTelemetry.test.ts` to validate `2/2 → 1/2 → 0/2 → 2/2` transitions during search + `Esc` reset flow. Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts src/shared/lib/localization/index.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts` and `pnpm typecheck`. |
| EX-131 | Auto-focus telemetry raw-events search input when opening the drawer for faster keyboard workflows. | COMPLETED | Added drawer-open focus behavior in `src/pages/NodesTelemetry.vue` (post-open `nextTick` lookup + focus of `telemetry-pulse-events-search`). Extended `src/pages/NodesTelemetry.test.ts` to assert the focus routine runs on drawer toggle via `document.getElementById('telemetry-pulse-events-search')` invocation. Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts src/shared/lib/localization/index.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts` and `pnpm typecheck`. |
| EX-132 | Remove recurring `baseline-browser-mapping` stale-data warning in local validation runs. | COMPLETED | Registry update path was blocked in this environment (`ENOTFOUND registry.npmjs.org`), so tooling-level mitigation was applied instead in `vite.config.mts`: set `BROWSERSLIST_IGNORE_OLD_DATA` and filter only `[baseline-browser-mapping]` warnings from Node-side build/test output. Verified warning removal with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts src/shared/lib/localization/index.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts` and `pnpm build` (both pass, warning no longer printed). |
| EX-133 | Polish telemetry raw-events keyboard focus visuals for cyberpunk readability. | COMPLETED | Added explicit `:focus-visible` styling for the raw-events list container and option buttons in `src/pages/NodesTelemetry.vue` (primary-tinted outline/glow) so keyboard navigation states are immediately visible and visually consistent with the ops-console glow language. |
| EX-134 | Add explicit “copy view link” action in telemetry raw-events toolbar for sharing current ops-console state. | COMPLETED | Added a one-click share action in `src/pages/NodesTelemetry.vue` (`data-test=\"pulse-copy-link\"`) that copies `window.location.href` through the existing clipboard/notification flow. Extended `src/pages/NodesTelemetry.test.ts` to assert clipboard invocation on click. Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts src/shared/lib/localization/index.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts` and `pnpm typecheck`. |
| EX-135 | Add chain-analysis style transaction tracing workspace with WebGL graph rendering, lazy full-history expansion, risk heuristics, and CSV/JSON bundle import/export. | COMPLETED | Added `/tracing` workspace (`src/pages/TracingWorkspace.vue`) with lazy account/transaction-seeded expansion over explorer instructions (`kind=Transfer`, committed-only), local risk heuristics + manual labels, shortest-path search, and CSV/JSON bundle export/import. Added dedicated WebGL renderer (`src/shared/ui/components/TraceGraphWebGL.vue`) with zoom + pan + node selection for large graphs. Wired navigation and launch paths (`src/app/router.ts`, `src/shared/config.ts`, account tab launcher + transaction launcher). Extended API filters (`asset_id`, `account`) and tracing core helpers/tests (`src/shared/lib/tracing.ts`, `src/shared/lib/tracing.test.ts`, `src/shared/api/index.test.ts`, `src/pages/TracingWorkspace.test.ts`, `src/shared/ui/components/TraceGraphWebGL.test.ts`, `src/pages/TransactionDetails.test.ts`, `src/pages/AccountDetails.test.ts`, `src/features/filter/transactions/model.test.ts`, `src/app/router.test.ts`, `src/shared/config.test.ts`). Hardening updates: one-pass account scan policy prevents cycle re-queue loops, graph snapshots are batched for large-trace performance, and `tracing.*` localization keys now have parity across all shipped locale JSON packs with parity assertions in `src/shared/lib/localization/index.test.ts`. Final verification: `pnpm typecheck`, `pnpm exec vitest run` (56 files / 317 tests passed), `pnpm build`. |
| EX-136 | Add a reset zoom/pan action on `/tracing` graph workspace. | COMPLETED | Added a graph-level reset button in `src/pages/TracingWorkspace.vue` (`data-test=\"trace-reset-viewport\"`) and wired it into `TraceGraphWebGL` through a new `viewportResetNonce` prop so the canvas re-fits viewport pan/zoom to current nodes. Added regression coverage in `src/pages/TracingWorkspace.test.ts` (reset trigger wiring) and `src/shared/ui/components/TraceGraphWebGL.test.ts` (pan away + reset restores click hit). Verified with `pnpm exec vitest run src/pages/TracingWorkspace.test.ts src/shared/ui/components/TraceGraphWebGL.test.ts` and `pnpm typecheck`. |
| EX-137 | Make `/tracing` account-seed mode auto-load direct inflows/outflows and support click-to-expand/collapse nodes. | COMPLETED | Refactored `src/pages/TracingWorkspace.vue` to stop recursive auto-enqueue and instead auto-scan only the seed account on bootstrap (showing direct inflows/outflows), while keeping discovered graph data cached. Added per-node expansion state (`expandedNodeIds`) and wired graph/list node clicks to toggle expand/collapse; expanding a node now enqueues that account for on-demand scan and collapsing hides that node’s expansion contribution from rendered nodes/edges/events without deleting cached data. Added list-item expansion state attributes for UX/tests and kept bundle export on full cached graph. Updated regression coverage in `src/pages/TracingWorkspace.test.ts` for one-hop seed loading and graph-click expand/collapse fetch behavior. Verified with `pnpm exec vitest run src/pages/TracingWorkspace.test.ts src/shared/ui/components/TraceGraphWebGL.test.ts` and `pnpm typecheck`. |
| EX-138 | Improve `/tracing` graph UX with cursor-anchored zoom, node-local overlay actions, and directional in/out flow links. | COMPLETED | `TraceGraphWebGL` now keeps wheel zoom anchored at cursor and emits node-context anchors + viewport-interaction events so `/tracing` can pin an overlay menu to the selected node. `src/pages/TracingWorkspace.vue` now renders a node-local menu (Expand/Collapse action) with directional flow lists (`Inflow`/`Outflow`) and highlights selected-node edge direction in the WebGL graph via curved, color-coded links for chain-analysis readability. Expanded regression coverage in `src/shared/ui/components/TraceGraphWebGL.test.ts` (node-context payload + cursor-zoom hit stability + viewport interaction) and `src/pages/TracingWorkspace.test.ts` (menu-driven expand/collapse + in/out flow rendering). Verified with `pnpm exec vitest run src/pages/TracingWorkspace.test.ts src/shared/ui/components/TraceGraphWebGL.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-139 | Increase `/tracing` graph line/node visual weight and polish canvas presentation. | COMPLETED | `src/shared/ui/components/TraceGraphWebGL.vue` now renders edges as thicker directional triangle ribbons (instead of thin `gl.LINES` strokes), increases node point sizing + halo shading for stronger visibility, and enables alpha blending for smoother layering. Canvas chrome was polished with richer gradients, subtle grid overlay, stronger border/glow treatment, and taller viewport height for clearer exploration. Updated WebGL test mocks in `src/shared/ui/components/TraceGraphWebGL.test.ts` for blending/triangle constants. Verified with `pnpm exec vitest run src/shared/ui/components/TraceGraphWebGL.test.ts src/pages/TracingWorkspace.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-140 | Remove `/tracing` “Top entities” sidebar box and represent top entities directly by node size in the graph. | COMPLETED | Removed the `Top entities` panel from `src/pages/TracingWorkspace.vue` and replaced it with rank-driven node prominence sizing in the graph: `rankedNodes` now feeds `nodeProminenceById`, which is passed into `TraceGraphWebGL` per node. `src/shared/ui/components/TraceGraphWebGL.vue` now uses `prominence` to scale node size (and hit radius) so top entities stand out directly in-canvas. Updated regression coverage in `src/pages/TracingWorkspace.test.ts` to stop relying on the removed list and assert the new UI behavior. Verified with `pnpm exec vitest run src/pages/TracingWorkspace.test.ts src/shared/ui/components/TraceGraphWebGL.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-141 | Investigate production lag where home “Latest Transactions” appears slower to refresh than “Latest Blocks”. | COMPLETED | 2026-03-04 production probe (`https://explorer.soramitsu.io` / `https://torii.soramitsu.io`): `/v2/explorer/blocks?per_page=5` responds in ~`0.09s`, while `/v2/explorer/transactions?per_page=5` takes ~`2.9s`–`4.1s` (observed occasional 5s timeout). Transaction SSE emits promptly, but widget refresh waits on the slower transactions list request. Upstream root cause: `handle_v1_explorer_transactions` materializes full history via `collect_transaction_summaries` before pagination (`../iroha/crates/iroha_torii/src/routing.rs`). |
| EX-142 | Reduce Torii `/v2/explorer/transactions` latency by paginating while scanning (avoid full-history materialization per request). | COMPLETED | Upstream patch landed in `../iroha/crates/iroha_torii/src/routing.rs`: `/v2/explorer/transactions` now counts all matched rows but only materializes DTOs for the requested page window (instead of collecting all DTOs before pagination). Added pagination helper coverage in `routing::tx_query_filter_tests` (`explorer_pagination_window_matches_paginate_semantics`, `explorer_pagination_meta_keeps_page_and_counts`). Verified with `cargo fmt --all -- crates/iroha_torii/src/routing.rs` and targeted tests (`cargo test -p iroha_torii routing::tx_query_filter_tests::explorer_pagination_window_matches_paginate_semantics -- --exact --nocapture`, `cargo test -p iroha_torii routing::tx_query_filter_tests::explorer_pagination_meta_keeps_page_and_counts -- --exact --nocapture`). Production explorer latency will improve once this Torii patch is deployed. |
| EX-143 | Reduce Torii `/v2/explorer/instructions` latency by paginating while scanning (avoid full-history materialization per request). | COMPLETED | 2026-03-04 upstream patch in `../iroha/crates/iroha_torii/src/routing.rs`: `/v2/explorer/instructions` now page-materializes DTOs during scan while preserving full `total_items/total_pages` metadata. Added regression coverage (`routing::explorer_lookup_tests::explorer_instruction_history_collects_requested_page_only`). Verified with `cargo test -p iroha_torii explorer_lookup_tests --features app_api,telemetry`. |
| EX-144 | Speed up explorer transaction/instruction detail lookups by indexing transaction hash to block height (avoid linear chain scans per detail request). | COMPLETED | 2026-03-04 upstream patch in `../iroha/crates/iroha_torii/src/routing.rs`: detail handlers now use `state.committed_transaction_height` as fast path, then bounded fallback scan for index misses/mismatches. Added regression coverage (`routing::explorer_lookup_tests::explorer_detail_lookup_returns_transaction_and_instruction`). Verified with `cargo test -p iroha_torii explorer_lookup_tests --features app_api,telemetry`. |
| EX-145 | Harden production Explorer/Torii availability: investigate persistent `502 Bad Gateway` periods and add resilient fallback behavior. | COMPLETED | 2026-03-04 frontend failover shipped: runtime-configurable Torii candidate list + thresholds (`src/shared/runtime-config.ts`), API failover manager with peer probing (`src/shared/api/index.ts`), Node Settings health/retry UX (`src/features/node-settings/NodeSettings.vue`), outage/degraded notices + retry in latest widgets, and runbook (`docs/roadmap/torii_outage_failover_runbook.md`). Verified with `pnpm exec vitest run src/shared/runtime-config.test.ts src/shared/api/index.test.ts src/widgets/latest-blocks/LatestBlocks.test.ts src/widgets/latest-transactions/LatestTransactions.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-146 | Avoid possible duplicate initial fetches in polling mode (`setupAsyncData`) when `interval` + default immediate fetch are both enabled. | COMPLETED | 2026-03-04 `setupAsyncData` polling startup now uses explicit interval behavior (`immediateCallback: false`) to prevent duplicate first fetch while preserving periodic polling. Added startup call-count tests in `src/shared/utils/setup-async-data.test.ts`. Verified with `pnpm exec vitest run src/shared/utils/setup-async-data.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-147 | Add explicit Explorer endpoint performance telemetry (p50/p95/p99 + error rates) and regression gates. | COMPLETED | 2026-03-04 upstream telemetry landed: new metrics `torii_explorer_requests_total` + `torii_explorer_request_duration_seconds` in `../iroha/crates/iroha_telemetry/src/metrics.rs`, wrapper wiring in `../iroha/crates/iroha_core/src/telemetry.rs`, and endpoint instrumentation in `../iroha/crates/iroha_torii/src/routing.rs` for explorer blocks/transactions/instructions (+ detail endpoints). Added regression coverage (`metrics::test::records_torii_explorer_metrics`, `routing::explorer_endpoint_telemetry_tests::*`). Verified with `cargo test -p iroha_telemetry records_torii_explorer_metrics` and `cargo test -p iroha_torii explorer_endpoint_telemetry_tests --features app_api,telemetry`. |
| EX-148 | Restore `/transactions` rendering when Torii emits `executable = Ivm` in explorer payloads. | COMPLETED | Updated `src/shared/api/schemas.ts` to accept `Ivm` in `Transaction.executable`, preventing list/detail parse failures that previously rendered empty transaction tables. Updated `src/pages/TransactionDetails.vue` to treat `Ivm` as a smart-contract executable (same branch as `Wasm`) and added regression coverage in `src/shared/api/schemas.test.ts` + `src/pages/TransactionDetails.test.ts`. Verified with `pnpm exec vitest run src/shared/api/schemas.test.ts src/pages/TransactionDetails.test.ts` and `pnpm build`. |
| EX-149 | Keep `/dataspaces` functional on nodes that do not expose `/v2/nexus/status` by falling back to root `/status`. | COMPLETED | Updated `src/shared/api/index.ts` `fetchNexusPublicStatus()` to try `/v2/nexus/status` first, then automatically fall back to legacy Torii root `/status` and normalize counters/backlog into `NexusPublicStatus`. Added regression coverage in `src/shared/api/index.test.ts` for both fallback-success and fallback-unavailable paths. Verified with `pnpm exec vitest run src/shared/api/index.test.ts` and `pnpm build`. |
| EX-150 | Resolve merge conflicts from the latest `origin/i23` sync. | COMPLETED | Reconciled `ROADMAP.md` conflict markers, preserved both branches’ completed entries, and renumbered conflicting IDs to keep the roadmap sequence unique. |
| EX-151 | Mitigate slow home “Latest Transactions” rendering under high `/v2/explorer/transactions` latency. | COMPLETED | 2026-03-05 probe still shows production skew (`/v2/explorer/blocks?per_page=5` ~`0.08s` vs `/v2/explorer/transactions?per_page=5` often `4s`-`11s`). Frontend mitigation shipped in `src/widgets/latest-transactions/LatestTransactions.vue`: render path now merges optimistic SSE rows + fetched rows + bounded local cache, writes cache snapshots from successful all-status fetches, and suppresses redundant SSE-triggered refetch while a fetch is pending. Added tested helper module `src/widgets/latest-transactions/model.ts` + `model.test.ts`, plus SSE regression `tests/e2e/latest-transactions-stream.smoke.test.ts` asserting streamed tx row renders before slow fetch completion. Verified with `pnpm exec vitest run src/widgets/latest-transactions/model.test.ts src/widgets/latest-transactions/LatestTransactions.test.ts tests/e2e/latest-transactions-stream.smoke.test.ts` and `pnpm typecheck`. |
| EX-152 | Restore `/transactions/:hash` detail usability when filtered instruction history is temporarily empty and decode nested multisig custom payloads into readable metadata. | COMPLETED | Added instruction-history fallback in `src/shared/ui/components/InstructionsTable.vue`: when `/v2/explorer/instructions?transaction_hash=...` temporarily returns empty, the table now probes detail endpoints (`/instructions/{hash}/{index}`) via `src/shared/lib/instruction-fallback.ts` and renders recovered rows (including deep-link priority index probes). Added multisig custom payload enrichment in `src/shared/lib/multisig-custom.ts` and wired it into `InstructionsTable` so nested `Custom` multisig envelopes now expose readable summary metadata (`variant/account/instructions_count/decoded instruction hints`) in the JSON pane while preserving raw payload. Added regression coverage in `src/shared/lib/instruction-fallback.test.ts`, `src/shared/lib/multisig-custom.test.ts`, and `src/shared/ui/components/InstructionsTable.test.ts` (fallback integration). Verified with `pnpm exec vitest run src/shared/lib/instruction-fallback.test.ts src/shared/lib/multisig-custom.test.ts src/shared/ui/components/InstructionsTable.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-153 | Stabilize `/transactions` list under slow Torii responses (faster first paint + less jumpy SSE refresh). | COMPLETED | `src/shared/ui/components/TransactionsTable.vue` now hydrates the default unfiltered list from a bounded local cache (`transactions_table_cache_v1`) for faster first paint, merges optimistic stream rows with fetched/cache rows on page 1, suppresses duplicate stream-hash reloads, and queues at most one SSE-triggered refetch while a request is already pending (avoids stacked reload races/jumps on slow Torii responses). Expanded regression coverage in `src/shared/ui/components/TransactionsTable.test.ts` for cache hydration, pending-refetch queueing, and duplicate-stream suppression. Verified with `pnpm exec vitest run src/shared/ui/components/TransactionsTable.test.ts` and `pnpm typecheck`. |
| EX-154 | Stabilize home latest block/transaction freshness so refreshes stop flipping between stale and near-head snapshots. | COMPLETED | Failover selection in `src/shared/api/index.ts` now probes all resolved candidates and chooses the freshest healthy head (highest block height, then newest timestamp) instead of first-match; built-in `Nexus/Testus` presets were removed from automatic failover candidate resolution so cross-network auto-switches no longer happen implicitly. Explorer GETs now use `cache: 'no-store'` (`get()` + `/peers`) to avoid stale browser-cache snapshots after refresh. Updated docs/sample config (`docs/roadmap/torii_outage_failover_runbook.md`, `public/config.json.example`) and expanded API regression coverage in `src/shared/api/index.test.ts` (freshest-candidate preference, no preset auto-probe, no-store fetch option). Verified with `pnpm exec vitest run src/shared/api/index.test.ts` and `pnpm typecheck`. |
| EX-155 | Add explicit home-widget freshness badges so latest blocks/transactions expose sample age and stale/fresh state. | COMPLETED | Added shared freshness helper `src/shared/lib/freshness.ts` (`fresh <= 15s`, `delayed <= 60s`, else `stale`) and wired home widgets to show `Sample age` + tone chips via existing telemetry i18n keys in `src/widgets/latest-blocks/LatestBlocks.vue` and `src/widgets/latest-transactions/LatestTransactions.vue`. Added regression coverage in `src/shared/lib/freshness.test.ts`, `src/widgets/latest-blocks/LatestBlocks.test.ts`, and `src/widgets/latest-transactions/LatestTransactions.test.ts` (stale + unknown badge states). Verified with `pnpm exec vitest run src/shared/lib/freshness.test.ts src/widgets/latest-blocks/LatestBlocks.test.ts src/widgets/latest-transactions/LatestTransactions.test.ts` and `pnpm typecheck`. |
| EX-156 | Harden frontend request reliability with timeout/retry controls and failover probe endpoint updates. | COMPLETED | 2026-03-05 completed: added runtime timeout/retry knobs (`toriiRequestTimeoutMs`, `toriiRequestRetryCount`, `toriiRequestRetryBaseDelayMs`) and wired resilient GETs with timeout + transient retry in `src/shared/api/index.ts`; failover candidate probing now uses `/v2/explorer/health` with fresh head metadata parsing fallback. Verified with `pnpm exec vitest run src/shared/runtime-config.test.ts src/shared/api/index.test.ts src/shared/api/schemas.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-157 | Reduce startup bundle cost by lazy-loading non-default locales. | COMPLETED | 2026-03-05 completed: migrated localization bootstrap to dynamic locale imports (`ensureLocaleLoaded`) with only `en` eagerly registered, updated language switching to await async locale load, and preloaded persisted locale in `src/app/main.ts`. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts src/shared/ui/composables/useApplicationLanguage.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-158 | Consolidate explorer transaction/instruction SSE subscriptions into shared composables. | COMPLETED | 2026-03-05 completed: introduced singleton/ref-counted SSE composables (`useExplorerTransactionsEvents`, `useExplorerInstructionsEvents`) and rewired latest/transactions/instructions components to share streams. Added regression tests for both composables plus table/widget wiring. Verified with `pnpm exec vitest run src/shared/ui/composables/useExplorerTransactionsEvents.test.ts src/shared/ui/composables/useExplorerInstructionsEvents.test.ts src/shared/ui/components/TransactionsTable.test.ts src/shared/ui/components/InstructionsTable.test.ts src/widgets/latest-transactions/LatestTransactions.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-159 | Add Torii explorer lightweight health/latest endpoints and stream keepalive behavior for frontend resiliency. | COMPLETED | 2026-03-05 completed in upstream `../iroha`: added `/v2/explorer/health`, `/v2/explorer/transactions/latest`, `/v2/explorer/instructions/latest`, fast-path latest collectors, and SSE idle keepalive comments in explorer streams; frontend now consumes the new latest/health helpers. Added routing regression tests for latest collectors (`explorer_lookup_tests`). Verified with `cargo fmt --all`, `cargo check -p iroha_torii --lib`, and `cargo test -p iroha_torii explorer_lookup_tests:: --features iroha_core/iroha-core-tests`. |
| EX-160 | Remove noisy test-time warnings (`--localstorage-file` Node warning + `vite-svg-loader` fallback warning for header logo). | COMPLETED | 2026-03-09 completed: configured Vitest with `test.execArgv` in `vite.config.mts` to pass a deterministic temp `--localstorage-file=...` path (eliminates Node 22 Web Storage warning spam), updated `src/widgets/header/HeaderLogo.vue` to use an explicit `?url` logo import (`logoSrc`) so `vite-svg-loader` no longer emits fallback warnings, and updated `src/shared/api/index.test.ts` persistence assertions to check stored value changes directly (runtime-stable across storage implementations). Verified with `pnpm exec vitest run src/shared/api/index.test.ts src/shared/ui/composables/useApplicationLanguage.test.ts src/shared/lib/localization/index.test.ts src/features/switch-lang/LangDropdown.test.ts src/widgets/header/TheHeader.test.ts src/pages/HomePage.test.ts` and `pnpm typecheck`. |
| EX-161 | Improve `/tracing` transaction-state exploration UX (state-aware transaction inspector + filters). | COMPLETED | Added a transaction-state inspector in `src/pages/TracingWorkspace.vue` with status/hash filters, per-transaction state summaries (status, block recency, instruction/event counts, participant hints), and richer recent-event rows that surface transaction hash + status + instruction index. Tracing scans now fetch both committed/rejected transfer instructions for state visibility, while graph topology ingestion remains committed-only to avoid flow distortion. Added focused coverage in `src/pages/TracingWorkspace.test.ts` (status/hash filtering + request semantics + hash rendering). Verified with `pnpm exec vitest run src/pages/TracingWorkspace.test.ts src/shared/ui/components/TraceGraphWebGL.test.ts` and `pnpm typecheck`. |
| EX-162 | Add failed-transaction rejection drill-down in `/tracing` grouped by reason/hash/account. | COMPLETED | Extended `src/pages/TracingWorkspace.vue` with rejected-transaction reason hydration (`fetchTransaction` + shared `formatTransactionRejectionReason` decoding), grouped reason buckets (`trace-rejection-groups`) showing transaction hashes and authority accounts, and inline warning/loading states for partial lookup failures. Added regression coverage in `src/pages/TracingWorkspace.test.ts` for grouped rejection drill-down semantics (same reason merged, hash/account evidence present, per-hash detail lookups invoked). Verified with `pnpm exec vitest run src/pages/TracingWorkspace.test.ts src/shared/ui/components/TraceGraphWebGL.test.ts` and `pnpm typecheck`. |
| EX-163 | Align explorer account/asset handling with latest upstream `../iroha` management changes. | COMPLETED | 2026-03-10 hard-cut follow-up: moved frontend account handling to i105-first literals (reject `ih58:` + legacy scoped account literals), updated tracing/search/filter test fixtures to domainless account ids + norito asset objects, switched account-details address UI copy/QR to i105 wording, and updated schema/API handling (`i105_address` canonical field + dataspaces `address_format: i105|compressed`). Also refreshed localization strings from IH58 to I105. Same-day gap hardening: removed `ih58_address` compatibility fallback, normalized rejection-reason account decoding to domainless literals, fixed `BaseHash` two-line rendering for non-`@` i105 hashes, migrated remaining non-legacy test fixtures away from `alice@...` ids, stabilized telemetry-ops localStorage persistence assertions in `src/pages/NodesTelemetry.test.ts`, and updated `tests/e2e/latest-transactions-stream.smoke.test.ts` to cover the `/transactions/latest` path with i105 fixtures. Verified with `pnpm exec vitest run` and `pnpm typecheck`. |
| EX-164 | Remove remaining legacy compatibility code paths from frontend runtime parsers and API helpers. | COMPLETED | 2026-03-10 hard-cut cleanup: removed `/status` fallback from `fetchNexusPublicStatus` (now strict `/v2/nexus/status`), removed old scoped-AccountId decode fallback from `src/shared/api/rejection-reason.ts`, removed legacy KAIGI SSE shape tolerance from `src/shared/lib/kaigi.ts`, and tightened schema compatibility in `src/shared/api/schemas.ts` by dropping `scale -> encoded`, dropping `r#box -> box`, and requiring strict structured instruction/rejection payloads (`.strict()`). Updated regression coverage in `src/shared/api/index.test.ts`, `src/shared/api/schemas.test.ts`, and `src/shared/lib/kaigi.test.ts` to enforce current payloads only. Verified with `pnpm exec vitest run` and `pnpm typecheck`. |
| EX-165 | Add dataspace drill-down detail route with scoped public explorer mode and per-dataspace public-node management. | COMPLETED | 2026-03-10 shipped: added `/dataspaces/:laneId/:dataspaceId` page (`src/pages/DataspaceDetails.vue`) with Nexus backlog + matching Sumeragi lane-governance rendering, refactored `/dataspaces` into overview-only routing to detail rows, introduced route-scope contract helpers (`src/shared/lib/explorer-scope.ts`) and scoped navigation composable (`src/shared/ui/composables/useExplorerScopeNavigation.ts`), wired route-scoped Torii override API (`getConfiguredToriiBaseUrl`, `setRouteScopedToriiBaseUrl`) into `src/shared/api/index.ts` + `src/app/App.vue`, added browser-local dataspace public-node store (`src/shared/lib/dataspace-public-nodes.ts`), and added scoped header control (`src/features/node-settings/ScopedExplorerControl.vue`) that replaces global node settings during scoped sessions with node switching + explicit exit. Scope preservation is now applied in `BaseLink`/`BaseButton` and migrated programmatic navigation call sites (search/home/list-detail/tracing/econometrics/widgets/table rows). Localization expanded with new `dataspaces.*` + `scopedExplorer.*` keys in `en.json` and all shipped locale packs. Verified with `pnpm exec vitest run src/shared/lib/explorer-scope.test.ts src/shared/ui/components/BaseLink.test.ts src/shared/ui/components/BaseButton.test.ts src/shared/lib/dataspace-public-nodes.test.ts src/app/router.test.ts src/pages/Dataspaces.test.ts src/pages/DataspaceDetails.test.ts src/widgets/header/TheHeader.test.ts src/features/search/SearchField.test.ts src/pages/HomePage.test.ts src/pages/Econometrics.test.ts src/pages/TracingWorkspace.test.ts src/pages/TransactionDetails.test.ts src/widgets/latest-blocks/LatestBlocks.test.ts src/shared/api/index.test.ts src/shared/lib/localization/index.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-166 | Close EX-165 follow-up gaps: add dedicated scoped-header control tests and replace locale placeholder carryover for new scoped/dataspace keys. | COMPLETED | 2026-03-10 follow-up completed: added dedicated behavioral coverage in `src/features/node-settings/ScopedExplorerControl.test.ts` for scoped-node switching (route query + scoped Torii override + dataspace store write) and explicit scoped-exit navigation back to `dataspaces-details`. Localized new EX-165 `dataspaces.*` / `scopedExplorer.*` strings across all shipped JSON locales by replacing English carryover values with translated text while preserving placeholders/IDs. Verified with `pnpm exec vitest run src/features/node-settings/ScopedExplorerControl.test.ts src/shared/lib/localization/index.test.ts` and `pnpm typecheck`. |
| EX-167 | Harden locale-pack publication for EX-165/166 keys by removing translation-provider warning artifacts and adding a regression guard. | COMPLETED | 2026-03-10 cleanup: removed accidental `MYMEMORY WARNING` quota strings from shipped locale JSON packs during the dataspaces/scoped-explorer translation sweep by re-translating affected keys and replacing warning payloads. Added regression coverage in `src/shared/lib/localization/index.test.ts` (`does not ship translation quota warning artifacts in locale packs`) to block future commits containing provider warning text. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts` and `pnpm typecheck`. |
| EX-168 | Protect scoped/dataspace localization placeholders from token corruption across shipped locale packs. | COMPLETED | 2026-03-10 hardening: fixed malformed placeholder tokens in localized scoped/dataspace strings (`ar`, `az`, `dz`, `he`, `jp`) where `{value}` / `{node}` / `{dataspace}` / `{lane}` were partially replaced by temporary markers, and added localization regression tests in `src/shared/lib/localization/index.test.ts` to enforce placeholder-token parity (`dataspaces.updatedAt`, `scopedExplorer.activeNode`, `scopedExplorer.dataspaceContext`) plus non-English localization checks for core scoped/dataspace labels. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts`, `pnpm exec vitest run src/shared/lib/explorer-scope.test.ts src/shared/ui/components/BaseLink.test.ts src/shared/ui/components/BaseButton.test.ts src/shared/lib/dataspace-public-nodes.test.ts src/app/router.test.ts src/pages/Dataspaces.test.ts src/pages/DataspaceDetails.test.ts src/widgets/header/TheHeader.test.ts src/features/node-settings/ScopedExplorerControl.test.ts src/features/search/SearchField.test.ts src/pages/HomePage.test.ts src/pages/Econometrics.test.ts src/pages/TracingWorkspace.test.ts src/pages/TransactionDetails.test.ts src/widgets/latest-blocks/LatestBlocks.test.ts src/shared/api/index.test.ts src/shared/lib/localization/index.test.ts`, and `pnpm typecheck`. |
| EX-169 | Migrate `/telemetry` query-sync writes to shared scoped navigation helper and add scope-preservation regression coverage. | COMPLETED | 2026-03-10 hardening: updated `src/pages/NodesTelemetry.vue` to use `useScopedExplorerNavigation().replace` for telemetry ops query sync (instead of direct `router.replace`) so scoped explorer contract remains centralized. Added regression coverage in `src/pages/NodesTelemetry.test.ts` asserting `torii` + `dataspaceLaneId` + `dataspaceId` survive telemetry filter query updates while ops keys change. Verified with `pnpm exec vitest run src/pages/NodesTelemetry.test.ts src/shared/lib/localization/index.test.ts` and `pnpm typecheck`. |
| EX-170 | Request i105 account rendering on explorer transaction/instruction endpoints so authority values stay searchable as account routes. | COMPLETED | 2026-03-10 completed: added `address_format=i105` on transaction/instruction fetchers in `src/shared/api/index.ts` (`fetchTransactions`, `fetchLatestTransactions`, `fetchTransaction`, `fetchInstructions`, `fetchLatestInstructions`, `fetchInstructionDetail`) so authority literals are requested in i105 format. Expanded regression coverage in `src/shared/api/index.test.ts` to assert query propagation on list/latest/detail routes. Verified with `pnpm exec vitest run src/shared/api/index.test.ts` and `pnpm typecheck`. |
| EX-171 | Preserve transaction/instruction API compatibility on older Torii nodes by falling back from `address_format=i105` to legacy `ih58` when required. | COMPLETED | 2026-03-10 completed: added `getWithAddressFormatFallback` in `src/shared/api/index.ts` so transaction/instruction fetchers request `address_format=i105` first and retry once with `address_format=ih58` only when the 400 body indicates an address-format compatibility rejection. Added per-node runtime caching of the detected legacy mode to avoid repeated two-step retries. Expanded regression coverage in `src/shared/api/index.test.ts` for fallback success (`fetchTransaction`, `fetchInstructions`), cached legacy reuse, and non-fallback behavior on unrelated 400/404 responses. Verified with `pnpm exec vitest run src/shared/api/index.test.ts` and `pnpm typecheck`. |
| EX-172 | Clarify transaction authority wording in UI to signer-centric terminology for operator readability. | COMPLETED | 2026-03-10 completed: switched transaction detail authority field title to `transactions.authority` (`src/pages/TransactionDetails.vue`) and updated English localization copy to `Signer account` (`src/shared/lib/localization/en.json`) so users see signer semantics instead of ambiguous account-id wording. Verified with `pnpm exec vitest run src/pages/TransactionDetails.test.ts src/shared/ui/components/TransactionsTable.test.ts src/shared/lib/localization/index.test.ts` and `pnpm typecheck`. |
| EX-173 | Finalize i105 rollout UX/compat: persist address-format detection across reloads, apply format to explorer SSE streams, and normalize legacy account payload display to a single i105 account literal. | COMPLETED | 2026-03-11 completed: persisted per-node address-format preferences in `src/shared/api/index.ts` (`torii_address_format_preference_v1`) so `i105`/legacy `ih58` fallback detection survives reloads; added exported `useToriiAddressFormatPreference()` and wired `address_format` query hints into shared explorer SSE streams (`src/shared/ui/composables/useExplorerTransactionsEvents.ts`, `useExplorerInstructionsEvents.ts`). Normalized legacy account payload handling in `src/shared/api/schemas.ts` so `ih58 + compressed` responses map to a single canonical i105 literal for display/navigation, and simplified account details address UI to show one i105 value (`src/pages/AccountDetails.vue`). Transaction/instruction/operator-facing labels now use account-id wording instead of “authority” (`src/pages/TransactionDetails.vue`, `src/shared/ui/components/InstructionsTable.vue`, `src/shared/ui/components/TransactionsTable.vue`). Verified with `pnpm exec vitest run src/shared/api/index.test.ts src/shared/api/schemas.test.ts src/shared/ui/composables/useExplorerTransactionsEvents.test.ts src/shared/ui/composables/useExplorerInstructionsEvents.test.ts src/shared/ui/components/TransactionsTable.test.ts src/pages/TransactionDetails.test.ts src/pages/AccountDetails.test.ts` and `pnpm typecheck`. |
| EX-174 | Prioritize i105-compressed compatibility fallback over legacy ih58 and remove legacy account examples from explorer account filter UX. | COMPLETED | 2026-03-11 completed: updated `src/shared/api/index.ts` fallback chain from `i105 -> ih58` to `i105 -> compressed -> ih58`, generalized compatibility-retry detection by attempted format, expanded persisted per-node preference support to include `compressed` in `torii_address_format_preference_v1`, and applied the same fallback helper to account list/detail requests so `/accounts` views prefer i105/compressed outputs too. Updated explorer English transaction filter copy in `src/shared/lib/localization/en.json` to use account wording plus an i105-style (`sora...`) placeholder sample. Added/updated regression coverage in `src/shared/api/index.test.ts` for compressed-first fallback, compressed preference caching/reload, account endpoint `address_format` propagation, and ih58 final fallback only when both `i105` and `compressed` are rejected. Verified with `pnpm exec vitest run src/shared/api/index.test.ts src/shared/lib/localization/index.test.ts src/pages/AccountDetails.test.ts src/shared/ui/composables/useExplorerTransactionsEvents.test.ts src/shared/ui/composables/useExplorerInstructionsEvents.test.ts`, and `pnpm typecheck`. |
| EX-175 | Remove compressed Sora-only account-address promotion from account pages so account detail uses canonical/id output and avoids compressed-first rendering. | COMPLETED | 2026-03-11 completed: changed account payload normalization in `src/shared/api/schemas.ts` to keep canonical account display as `i105_address || id` (no longer promoting `compressed_address` to primary), switched account list/detail API helpers in `src/shared/api/index.ts` to canonical fallback path (`i105 -> ih58`) instead of compressed-first compatibility fallback, and updated account-page assertions in `src/pages/AccountDetails.test.ts` to ensure compressed Sora-only literals are not surfaced as primary account addresses. Added/updated regression coverage in `src/shared/api/schemas.test.ts` and `src/shared/api/index.test.ts` for account fallback/address-format behavior. Verified with `pnpm exec vitest run src/shared/api/index.test.ts src/shared/api/schemas.test.ts src/pages/AccountDetails.test.ts` and `pnpm typecheck`. |
| EX-176 | Remove unused compressed-account localization keys from shipped locale packs and programmatic locale overrides after account-page compressed UI removal. | COMPLETED | 2026-03-11 completed: removed `accounts.accountAddressCompressedLabel` / `accountAddressCompressedWarning` / `accountAddressCopyCompressed` / `accountAddressCopyCompressedToast` from every shipped locale JSON pack plus `en.json`, removed matching programmatic-locale fields from `src/shared/lib/localization/index.ts` (`AccountsLocaleCopy` key pick and Akkadian/Hieroglyphic account-copy overrides), and updated account/localization regression checks in `src/shared/lib/localization/index.test.ts` + `src/pages/AccountDetails.test.ts` to assert against remaining address keys/labels only. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts src/pages/AccountDetails.test.ts` and `pnpm typecheck`. |
| EX-177 | Stop truncating account IDs with middle ellipsis on `/accounts/:id` so operators can read the full literal directly. | COMPLETED | 2026-03-12 completed: updated `src/pages/AccountDetails.vue` so the Account ID `DataField` always renders with `type=\"full\"` (removed adaptive hash truncation on this field), and added regression coverage in `src/pages/AccountDetails.test.ts` (`renders account id field in full mode (no middle ellipsis)`). Verified with `pnpm exec vitest run src/pages/AccountDetails.test.ts` and `pnpm typecheck`. |
| EX-178 | Improve `/transactions/:hash` clarity when instruction history is empty by showing an in-context transaction overview in the instructions section. | COMPLETED | 2026-03-12 completed: `src/shared/ui/components/InstructionsTable.vue` now emits list-state updates (`isLoading`, `totalItems`, `itemsCount`), and `src/pages/TransactionDetails.vue` consumes that signal to show an instructions-section fallback overview (No data + executable + status badge + account + rejection reason when present) whenever non-smart-contract transactions have no instruction rows. Added regression coverage in `src/pages/TransactionDetails.test.ts` for empty-list overview visibility and loading suppression. Verified with `pnpm exec vitest run src/pages/TransactionDetails.test.ts src/shared/ui/components/InstructionsTable.test.ts` and `pnpm typecheck`. |
| EX-179 | Expand empty-instructions transaction overview with richer context (hash/block/timestamp/nonce/signature) so users can identify what the transaction is about without instruction rows. | COMPLETED | 2026-03-12 completed: extended `src/pages/TransactionDetails.vue` empty-instructions fallback panel to include transaction hash, block, timestamp, nonce, signature, and metadata in addition to existing executable/status/account/rejection context, all using existing localization keys/components. Updated `src/pages/TransactionDetails.test.ts` assertions to validate the richer overview payload appears only when instruction list is empty and loaded. Verified with `pnpm exec vitest run src/pages/TransactionDetails.test.ts src/shared/ui/components/InstructionsTable.test.ts` and `pnpm typecheck`. |
| EX-180 | Make transaction instruction `Value` JSON visibly scrollable/readable in table/detail views when payloads are large. | COMPLETED | 2026-03-12 completed: updated `src/shared/ui/components/InstructionsTable.vue` to tag row/detail JSON renderers with dedicated classes and enforce bounded scroll panes (`max-height`, `overflow: auto`, `scrollbar-gutter`, `overscroll-behavior`) plus wrapped long JSON values for readability. Added regression assertions in `src/shared/ui/components/InstructionsTable.test.ts` for row/detail JSON container presence. Verified with `pnpm exec vitest run src/shared/ui/components/InstructionsTable.test.ts src/pages/TransactionDetails.test.ts` and `pnpm typecheck`. |
| EX-181 | Fix low-contrast `Hide details` action in transaction instruction detail header for dark mode readability. | COMPLETED | 2026-03-12 completed: updated `src/shared/ui/components/InstructionsTable.vue` detail-header button styles so `instructions-detail__close` now has explicit contrast-safe border/background/text tokens (plus hover state) instead of inheriting low-contrast defaults in dark mode. Added regression assertion in `src/shared/ui/components/InstructionsTable.test.ts` to ensure the close action exists with the expected `Hide details` label when instruction details are open. Verified with `pnpm exec vitest run src/shared/ui/components/InstructionsTable.test.ts src/pages/TransactionDetails.test.ts` and `pnpm typecheck`. |
| EX-182 | Restore `/assets` rendering when Torii returns non-legacy asset-definition IDs (for example `aid:...`) that do not include `name/domain` fields. | COMPLETED | 2026-03-17 completed: added `src/shared/lib/asset-definition-id.ts` fallback helpers and wired `/assets`, `/assets/:id`, `/accounts/:id`, and `/domains/:id` asset rows to render literal IDs safely when `name/domain` are unavailable (domain column now shows `-` without broken links). Added regression coverage in `src/shared/lib/asset-definition-id.test.ts` and `src/pages/AssetsList.test.ts` for both legacy and literal `aid:...` IDs. Verified with `pnpm exec vitest run src/shared/lib/asset-definition-id.test.ts src/pages/AssetsList.test.ts src/pages/AccountDetails.test.ts`, `pnpm exec vitest run --maxWorkers=1` (full suite: 70 files / 444 tests passed), `pnpm build`, and `pnpm typecheck`. |
| EX-183 | Stabilize default parallel `vitest` runs (remove intermittent Node v25 `EPIPE` worker crash) so `pnpm exec vitest run` is reliable without `--maxWorkers=1`. | COMPLETED | 2026-03-17 completed: switched Vitest worker pool from forked processes to threads in `vite.config.mts` (`test.pool = 'threads'`) to avoid child-process IPC `EPIPE` crashes observed under Node v25. Verified with `pnpm exec vitest run` (default parallel mode; 70 files / 444 tests passed), plus `pnpm typecheck` and `pnpm build`. |
| EX-184 | Run a validation-led bug hunt and fix the first verified frontend regression uncovered by tests or code review. | COMPLETED | 2026-03-18 completed: fixed `useCurrentExplorerScope()` in `src/shared/ui/composables/useExplorerScopeNavigation.ts` to read `vue-router` via safe injection (`inject(routerKey, null)`) instead of `useRouter()` try/catch, which previously still emitted `Symbol(router)` warnings whenever shared link/button primitives rendered outside a router context (seen in LangDropdown/InstructionsTable tests). Added `src/shared/ui/composables/useExplorerScopeNavigation.test.ts` covering the no-router regression and normal scoped-route parsing. Verified with targeted warning-heavy suites (`pnpm exec vitest run src/shared/ui/composables/useExplorerScopeNavigation.test.ts src/features/switch-lang/LangDropdown.test.ts src/shared/ui/components/InstructionsTable.test.ts src/shared/ui/components/TransactionsTable.test.ts src/shared/ui/components/BaseButton.test.ts src/shared/ui/components/BaseLink.test.ts`), plus full `pnpm exec vitest run` (71 files / 446 tests passed), `pnpm typecheck`, and `pnpm build`. |
| EX-185 | Restore `pnpm lint` usability by excluding local virtualenv artifacts from repo lint/git scans. | COMPLETED | 2026-03-18 completed: updated `eslint.config.mjs` ignores to exclude `.venv*` directories and added `.venv` / `.venv-*` to `.gitignore`, so local virtualenv artifacts no longer break lint or show up in repo hygiene. Verified with `pnpm lint`: the prior `.venv-l10n/.../emscripten_fetch_worker.js` failures disappeared, leaving only first-party repo lint issues for follow-up. |
| EX-186 | Remove reactive side effects from filter `computed()` getters on account/asset/domain pages so filter validation stays pure and lint-clean. | COMPLETED | 2026-03-18 completed: added pure shared helpers in `src/shared/lib/optional-filter.ts` + tests, then rewired `/accounts`, `/assets`, `/assets/:id`, and `/domains/:id` filter parsing to derive `{ value, error }` from computed state instead of mutating refs inside computed getters. This removed the `vue/no-side-effects-in-computed-properties` violations on those pages and simplified redundant `shallowRef` state for holder/asset filters. Verified with `pnpm exec eslint --ext .ts,.vue src/shared/lib/optional-filter.ts src/shared/lib/optional-filter.test.ts src/pages/AccountsList.vue src/pages/AssetsList.vue src/pages/AssetDetails.vue src/pages/DomainDetails.vue`, targeted tests (`pnpm exec vitest run src/shared/lib/optional-filter.test.ts src/pages/AssetsList.test.ts`), full `pnpm exec vitest run` (72 files / 450 tests passed), and `pnpm build`. Full `pnpm lint` still has unrelated repo issues but dropped from 228 problems to 208 after this pass. |
| EX-187 | Close remaining repo hardening gaps from bug hunt: tracing async safety, page-level filter validation tests, complexity hotspots, and full lint cleanup. | COMPLETED | 2026-03-18 completed: hardened `TracingWorkspace` async triggers to avoid discarded-promise/unhandled-rejection paths, added direct invalid-filter page tests for `/accounts`, `/assets`, `/assets/:id`, and `/domains/:id`, refactored `Econometrics`, `NodesTelemetry`, `ZkTelemetry`, `GovernanceDashboard`, tracing, KAIGI, and WebGL helpers to clear the remaining lint-blocking complexity/max-params issues, and extracted node-board projection/sorting helpers with tests. Verified with `pnpm lint` (now green; 74 existing warnings remain but 0 errors), full `pnpm exec vitest run` (75 files / 460 tests passed), `pnpm typecheck`, and `pnpm build`. |
| EX-188 | Reduce remaining ESLint warning debt by fixing app-code warnings and scoping low-signal Vue test-stub rules. | COMPLETED | 2026-03-18 completed: fixed the remaining production-side warnings in `SorafsRegistry`, shared UI props, telemetry composables, and async-data tests; added a test-file ESLint override for low-signal Vue stub rules (`vue/one-component-per-file`, test stub prop defaults/types) so lint output no longer gets buried by inline `defineComponent` helpers. Verified with `pnpm lint` (clean aside from the upstream `@stylistic/eslint-plugin-ts` deprecation banner), full `pnpm exec vitest run` (75 files / 460 tests passed), `pnpm typecheck`, and `pnpm build`. |
| EX-189 | Restore `/accounts` rendering when Torii returns nullable legacy address fields (`ih58_address`, `compressed_address`). | COMPLETED | 2026-03-25 completed: reproduced the live `https://taira-explorer.sora.org/accounts` empty state in a browser and traced it to the `Account` schema rejecting `null` `ih58_address`/`compressed_address` values returned by `/v1/explorer/accounts`. Updated `src/shared/api/schemas.ts` to accept nullable address variants while preserving canonical i105/id fallback, and added regression coverage in `src/shared/api/schemas.test.ts` plus `src/shared/api/index.test.ts` so account listings continue parsing when Torii omits legacy address strings. Verified with `pnpm exec vitest run src/shared/api/schemas.test.ts src/shared/api/index.test.ts`. |
| EX-190 | Remove the remaining frontend `ih58` compatibility/runtime paths so explorer account transport is i105/compressed only. | COMPLETED | 2026-03-25 completed: removed `ih58_address` from the runtime `Account` schema in `src/shared/api/schemas.ts`, removed `address_format=ih58` fallback/retry support from `src/shared/api/index.ts` so account/transaction/instruction requests now stop at `i105 -> compressed`, and deleted the last non-test `ih58` reject special-case from `src/shared/lib/account-literal.ts` because the generic forbidden-character guard already rejects prefixed literals. Updated active fixtures/assertions in `src/shared/api/index.test.ts`, `src/shared/api/schemas.test.ts`, `src/shared/lib/account-literal.test.ts`, `src/pages/AccountDetails.test.ts`, and `tests/e2e/accounts-data.smoke.test.ts` to reflect i105/compressed-only handling. Verified with `pnpm exec vitest run src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/shared/lib/account-literal.test.ts src/pages/AccountDetails.test.ts tests/e2e/accounts-data.smoke.test.ts` and `pnpm typecheck`. |
| EX-191 | Ignore generated `.playwright-cli` artifacts and remove existing tracked files from git. | COMPLETED | 2026-03-25 completed: added `.playwright-cli/` to `.gitignore` and removed the tracked Playwright CLI capture YAML files from the git index (`git rm --cached -r -- .playwright-cli`). Verified with `git ls-files -- '.playwright-cli' '.playwright-cli/**'` returning no tracked paths and `git status --short` showing only the ignore/roadmap edits plus staged deletions for the previously tracked captures. |
| EX-192 | Force account-list surfaces to render and navigate with canonical `i105_address` values instead of generic `id` fallbacks. | COMPLETED | 2026-03-25 completed: added `src/shared/lib/account-id.ts` as the shared preferred-account-id helper, rewired `/accounts` and the domain account table to render/link/click through `i105_address` when present, and added regression coverage in `src/shared/lib/account-id.test.ts`, `src/pages/AccountsList.test.ts`, and `src/pages/DomainDetails.test.ts` using mismatched `id` vs `i105_address` fixtures. Verified with `pnpm exec vitest run src/shared/lib/account-id.test.ts src/pages/AccountsList.test.ts src/pages/DomainDetails.test.ts` and `pnpm typecheck`. |
| EX-193 | Surface upstream RWAs as a first-class explorer asset family (schemas, routes, search, list/detail UI, and owner/domain views) aligned with `../iroha`. | COMPLETED | 2026-03-26 completed: added frontend RWA schemas/fetchers plus `src/shared/lib/rwa-id.ts`, exposed `/rwas` and `/rwas/:id` routes, extended asset-area tabs/search/home/nav so canonical `64-hex$domain` RWA IDs resolve to the new list/detail views, and rendered filtered explorer RWA rows from Torii’s `/v1/explorer/rwas` + `/v1/explorer/rwas/{rwa_id}` payloads. Added regression coverage in `src/shared/lib/rwa-id.test.ts`, `src/shared/api/schemas.test.ts`, `src/shared/api/index.test.ts`, `src/features/search/SearchField.test.ts`, `src/pages/AssetsList.test.ts`, `src/pages/HomePage.test.ts`, and `src/pages/RWADetails.test.ts`. Verified with `pnpm exec vitest run src/shared/lib/rwa-id.test.ts src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/features/search/SearchField.test.ts src/pages/AssetsList.test.ts src/pages/HomePage.test.ts src/pages/RWADetails.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-194 | Enforce canonical account/asset selector semantics everywhere: account IDs are canonical Katakana i105 only, account aliases are `name@domain.dataspace` or `name@dataspace`, asset-definition IDs are base58, and asset aliases are `name#domain.dataspace` or `name#dataspace`. | COMPLETED | 2026-03-26 completed: replaced legacy SDK account/asset-definition parsing with local upstream-aligned validators, normalized routes/search/filter inputs to accept only canonical Katakana i105 IDs or on-chain aliases for accounts and base58 IDs or aliases for asset definitions, updated display helpers so canonical base58 IDs and alias metadata render correctly, and resolved tracing/econometrics/account-detail flows so aliases resolve to canonical on-screen IDs. Added regression coverage across schema/API/page/component tests and updated legacy fixtures/samples/localized placeholder copy to remove old Base58/compressed account literals and `rose#wonderland` examples. Verified with `pnpm typecheck`, `pnpm exec vitest run src/shared/lib/asset-definition-id.test.ts src/shared/lib/account-literal.test.ts src/shared/lib/asset-definition-literal.test.ts src/shared/lib/econometrics.test.ts src/shared/lib/tracing.test.ts src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/features/search/SearchField.test.ts src/pages/HomePage.test.ts src/pages/AccountsList.test.ts src/pages/AssetsList.test.ts src/pages/AssetDetails.test.ts src/pages/DomainDetails.test.ts src/pages/Econometrics.test.ts src/pages/TracingWorkspace.test.ts src/pages/RWADetails.test.ts`, and `pnpm exec vitest run src/pages/AccountDetails.test.ts src/pages/TransactionDetails.test.ts src/pages/NodesTelemetry.test.ts src/widgets/latest-transactions/LatestTransactions.test.ts src/widgets/latest-transactions/model.test.ts src/shared/lib/instruction-fallback.test.ts src/shared/lib/rwa-provenance.test.ts src/shared/lib/account-id.test.ts src/shared/lib/multisig-custom.test.ts src/shared/lib/optional-filter.test.ts src/shared/ui/composables/useGovernanceMetrics.test.ts src/shared/ui/composables/useSumeragiStatus.test.ts src/shared/ui/components/TransactionsTable.test.ts src/shared/ui/components/TraceGraphWebGL.test.ts src/shared/ui/components/BaseHash.test.ts src/shared/ui/components/InstructionsTable.test.ts`. |
| EX-195 | Show RWA provenance as a graph on `/rwas/:id`, using upstream parent refs from `../iroha` explorer payloads instead of flat detail cards only. | COMPLETED | 2026-03-26 completed: extended `../iroha/crates/iroha_torii/src/explorer.rs` so explorer RWAs now expose quantitative `parents`, then added `src/shared/lib/rwa-provenance.ts` to recursively fetch ancestor lots and render a DAG on `src/pages/RWADetails.vue` with placeholder nodes for missing ancestors. Added regression coverage in `src/shared/lib/rwa-provenance.test.ts`, `src/shared/api/schemas.test.ts`, `src/shared/api/index.test.ts`, and `src/pages/RWADetails.test.ts`. Verified the web app with `pnpm exec vitest run src/shared/lib/rwa-provenance.test.ts src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/pages/RWADetails.test.ts`, `pnpm typecheck`, and `pnpm build`. Attempted upstream Torii verification, but `cargo test -p iroha_torii rwas_page_filters_by_owner_and_domain --lib` is currently blocked by unrelated pre-existing compile errors in the dirty sibling repo outside `explorer.rs` (`routing.rs`, `lib.rs`). |
| EX-196 | Deduplicate the account identifier on `/accounts/:id` so the canonical i105 value is not rendered twice when the account ID and address are the same. | COMPLETED | 2026-03-26 completed: `src/pages/AccountDetails.vue` now suppresses the standalone `Account ID` field when the address card already shows the same canonical i105 string, while still falling back to the full-width `Account ID` field if no i105 address block is available. Added regression coverage in `src/pages/AccountDetails.test.ts` and verified with `pnpm exec vitest run src/pages/AccountDetails.test.ts` plus `pnpm typecheck`. |
| EX-197 | Fix `/transactions` pagination so changing pages refreshes the rendered rows instead of keeping the latest-page cache visible. | COMPLETED | 2026-03-26 completed: `src/shared/ui/components/TransactionsTable.vue` now limits the cache/stream merge path to the latest default-feed page only, so older paginated views render the fetched page rows directly. Added a warm-cache pagination regression in `src/shared/ui/components/TransactionsTable.test.ts` and verified with `pnpm exec vitest run src/shared/ui/components/TransactionsTable.test.ts` plus `pnpm typecheck`. |
| EX-198 | Review surrounding pagination/account-details surfaces for additional regressions after the latest fixes. | COMPLETED | 2026-03-26 completed: follow-up review found two concrete issues to address next: `TransactionsTable` still writes older paginated default-feed rows into the latest-feed local cache, and `AccountDetails` renders the QR caption twice while the QR code is still pending. Findings reported to the user with file references. |
| EX-199 | Fix the follow-up transactions-cache and account-QR regressions found after the latest pagination/account-details changes. | COMPLETED | 2026-03-26 completed: `src/shared/ui/components/TransactionsTable.vue` now writes `transactions_table_cache_v2` only from the live latest default-feed page, preventing older paginated rows from polluting the latest-feed bootstrap cache; `src/pages/AccountDetails.vue` no longer renders the QR caption twice while the QR image is pending. Added regressions in `src/shared/ui/components/TransactionsTable.test.ts` and `src/pages/AccountDetails.test.ts`, verified with `pnpm exec vitest run src/shared/ui/components/TransactionsTable.test.ts src/pages/AccountDetails.test.ts`, and `pnpm typecheck`. |
| EX-200 | Show smart-contract deployment code in a decompiled/source-like way on transaction details, with verified-source upload stored in SoraFS on Sora Nexus. | COMPLETED | 2026-03-28 shipped end-to-end, then revised so the explorer decompiles locally in the browser instead of fetching pre-rendered decompile output from Torii. The frontend now reconstructs pseudo-source directly from the loaded transaction instruction payloads in `src/shared/lib/contract-view.ts`, renders that reusable panel in `src/shared/ui/components/ContractCodeViewPanel.vue`, shows it inside instruction details (`src/shared/ui/components/InstructionsTable.vue`) and on smart-contract transaction pages (`src/pages/TransactionDetails.vue`), and keeps verified-source submission as an explicit upload action via `src/shared/api/index.ts`. Added regression coverage in `src/shared/lib/contract-view.test.ts`, `src/shared/ui/components/ContractCodeViewPanel.test.ts`, `src/shared/ui/components/InstructionsTable.test.ts`, and `src/pages/TransactionDetails.test.ts`. Verified with `pnpm exec vitest run src/shared/lib/contract-view.test.ts src/shared/ui/components/ContractCodeViewPanel.test.ts src/shared/ui/components/InstructionsTable.test.ts src/pages/TransactionDetails.test.ts`, then `pnpm typecheck` and `pnpm build`. |
| EX-201 | Replace smart-contract pseudo-source body stubs with real browser opcode disassembly and fetch the full transaction instruction history for contract decoding. | COMPLETED | 2026-03-28 completed: `src/shared/lib/contract-view.ts` now performs real browser-side IVM wide-word disassembly with labels, syscall naming, and memory/syscall analysis instead of stubbed entrypoint bodies, while `src/shared/lib/transaction-instructions.ts` pages through the full transaction instruction history so both `src/pages/TransactionDetails.vue` and `src/shared/ui/components/InstructionsTable.vue` can resolve companion contract bytecode/manifest instructions across paginated history. Added/updated regression coverage in `src/shared/lib/transaction-instructions.test.ts`, `src/shared/lib/contract-view.test.ts`, `src/shared/ui/components/ContractCodeViewPanel.test.ts`, `src/shared/ui/components/InstructionsTable.test.ts`, and `src/pages/TransactionDetails.test.ts`. Verified with `pnpm exec vitest run src/shared/lib/transaction-instructions.test.ts src/shared/lib/contract-view.test.ts src/shared/ui/components/ContractCodeViewPanel.test.ts src/shared/ui/components/InstructionsTable.test.ts src/pages/TransactionDetails.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-202 | Move browser smart-contract decompilation off the main thread so contract panels do not block UI rendering on large artifacts. | COMPLETED | 2026-03-28 completed: added `src/shared/lib/contract-view.worker.ts` plus `src/shared/lib/contract-view-worker-client.ts`, so `src/shared/ui/components/ContractCodeViewPanel.vue` now renders contract views asynchronously in a dedicated Web Worker with a synchronous fallback when `Worker` is unavailable or errors. Added regression coverage in `src/shared/lib/contract-view-worker-client.test.ts` and updated `src/shared/ui/components/ContractCodeViewPanel.test.ts` for the async render path. Verified with `pnpm exec vitest run src/shared/lib/transaction-instructions.test.ts src/shared/lib/contract-view.test.ts src/shared/lib/contract-view-worker-client.test.ts src/shared/ui/components/ContractCodeViewPanel.test.ts src/shared/ui/components/InstructionsTable.test.ts src/pages/TransactionDetails.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-203 | Ship a full-bleed Kotodama Studio at `/studio` with a Blockly-inspired smart-contract builder, an n8n-inspired on-chain workflow canvas, source generation, and child-friendly workspace UX. | COMPLETED | 2026-03-28 completed: added a dedicated full-bleed `/studio` route and navigation entry, a Blockly-powered contract builder plus Vue Flow workflow canvas under `src/features/studio/`, persisted studio documents with JSON import/export under `src/shared/lib/kotodama-studio-*.ts`, preview compile/deploy draft hooks, and the combined `src/pages/KotodamaStudio.vue` workspace with localized copy. Added regression coverage in `src/features/studio/blockly.test.ts`, `src/pages/KotodamaStudio.test.ts`, `src/shared/lib/kotodama-studio-document.test.ts`, `src/shared/lib/kotodama-studio-source.test.ts`, `src/shared/lib/kotodama-studio-deploy.test.ts`, plus route/button assertions. Verified with `pnpm typecheck`, `pnpm exec vitest run src/shared/lib/kotodama-studio-document.test.ts src/shared/lib/kotodama-studio-source.test.ts src/shared/lib/kotodama-studio-deploy.test.ts src/features/studio/blockly.test.ts src/shared/ui/components/BaseButton.test.ts src/app/router.test.ts src/pages/KotodamaStudio.test.ts`, and `pnpm build`. |
| EX-205 | Retheme Kotodama Studio so the visual language feels more jinja-inspired while preserving the existing builder/workflow interaction model. | COMPLETED | 2026-03-28 completed: shifted the `/studio` art direction toward shrine/jinja cues by retuning the studio shell in `src/pages/KotodamaStudio.vue` to use vermilion lacquer, warm washi paper, cedar/gold accents, and plaque-like templates; restyled `src/features/studio/ContractStudioBuilder.vue` and `src/features/studio/blockly.ts` so Blockly surfaces/categories fit the same palette; and updated `src/features/studio/WorkflowStudioCanvas.vue` plus `src/features/studio/StudioFlowNode.vue` so workflow chips, nodes, and edges feel closer to ema/ofuda objects than generic app cards. Verified with `pnpm typecheck`, `pnpm exec vitest run src/pages/KotodamaStudio.test.ts src/features/studio/blockly.test.ts`, and `pnpm build`. |
| EX-206 | Replace Kotodama Studio’s preview/endpoint compile flow with a browser-local TypeScript compiler that emits chain-valid IVM artifacts for the Studio language subset. | COMPLETED | 2026-03-28 completed: removed the remote compiler/preview fallback from `src/shared/lib/kotodama-studio-deploy.ts` and `/studio`, added a browser-local TypeScript compiler in `src/shared/lib/kotodama-studio-compiler.ts` that emits `IVM` + `CNTR` + `LTLB` artifacts for the Studio-generated Kotodama subset, switched hashing to pure-JS BLAKE2b via `@noble/hashes`, and hard-gated deploy on real local diagnostics instead of preview state. Added regression coverage in `src/shared/lib/kotodama-studio-compiler.test.ts`, updated `src/shared/lib/kotodama-studio-deploy.test.ts` and `src/pages/KotodamaStudio.test.ts`, and verified with `pnpm typecheck`, `pnpm exec vitest run src/shared/lib/kotodama-studio-compiler.test.ts src/shared/lib/kotodama-studio-deploy.test.ts src/pages/KotodamaStudio.test.ts`, and `pnpm build`. This is intentionally limited to the Studio-emitted language subset, not full upstream Kotodama parity. |
| EX-207 | Build a full browser-local TypeScript Kotodama compiler with parser, semantic analysis, manifest generation, and bytecode parity against the upstream Rust compiler. | COMPLETED | 2026-03-28 started: audited upstream `../iroha/crates/kotodama_lang` lexer/parser/semantic/compiler modules and reset the roadmap to distinguish Studio-subset compilation from full-language parity. Follow-up compiler slices broadened `src/shared/lib/kotodama-studio-compiler.ts` to lower local bindings, local reassignment, query-result bindings, `while` loops, integer arithmetic expressions (`+ - * / %`), underscored integer literals, unary negation, and less blockly-specific `if` / `while` condition syntax without mandatory parentheses or `else`. Subsequent work added typed helper functions and entrypoints with params/returns, inline call lowering, `return` statements, ternary expressions, both `name: Type` and `Type name` parameter syntax, `poseidon2`, `assert`/`assert_eq`, `mint_asset`, and `burn_asset`, plus far-jump-friendly `if` / `while` / ternary lowering so larger inlined helper bodies still compile inside the browser. A later slice added boolean/logical control-flow support for unary `!`, `&&`, `||`, and loop-control statements `break` / `continue`. Another tranche added tuple type parsing, tuple literals, tuple params/returns, numeric tuple member access (`.0`, `.1`, ...), recursive tuple binding lowering for inline calls and returns, and embedded `CNTR` param/return type metadata strings. A later tranche added parsed `struct` declarations, resolved struct type references, struct-constructor calls (`StructName(...)`), named member access on structs, nested aggregate lowering across tuples/structs, and unknown-field diagnostics. The next tranche added upstream-style top-level `#[access(...)]` attribute skipping, `state Name: Map<K, V>` parsing, indexed durable map reads and writes (`State[key]`), `contains`, `get_or_insert_default`, `isqrt`, optional assert messages, and real durable-path lowering through `BUILD_PATH_MAP_KEY`, `BUILD_PATH_KEY_NORITO`, `POINTER_TO_NORITO`, and `POINTER_FROM_NORITO` syscalls so `irohaswap`-style contracts now compile in the browser. A later tranche added call-style pointer constructors used by upstream samples (`account_id(...)`, `asset_definition(...)`, `name(...)`, `json(...)`, `blob(...)`, `norito_bytes(...)`), non-mutating durable-map `get_or(...)`, `current_time_ms()`, and additional arithmetic parity for `abs`, `min`, `max`, `div_ceil`, `gcd`, and `mean`. The latest tranche adds trigger/runtime builtin coverage for `trigger_event()`, `resolve_account_alias(...)`, `execute_instruction(...)`, and JSON field getters `json_get_int/json_get_json/json_get_name/json_get_account_id/json_get_asset_definition_id`, matching the upstream syscall surface well enough for trigger-bridge helper contracts to compile locally in the browser. A follow-up slice now adds typed local `let map: Map<K, V> = Map::new()` bindings, local-map aliasing, one-slot in-memory `contains` / `get_or` / `get_or_insert_default` lowering, indexed local-map reads and writes, and explicit diagnostics when `Map::new()` lacks a declared map type or a local map is used without indexing. The newest metadata tranche now parses function-level `#[access(...)]` annotations instead of skipping them, derives durable-state read/write keys transitively across helper-function calls for the browser-supported subset, emits contract-level `access_set_hints`, fills entrypoint `read_keys` / `write_keys` / `access_hints_complete` / `access_hints_skipped`, and embeds declared state descriptors into the `CNTR` section so unused state still survives as artifact metadata. A trigger-follow-up broadened top-level trigger syntax to accept the upstream `trigger` alias, `call namespace::entrypoint`, `repeats`, and `metadata { ... }` blocks for the browser-supported subset, embeds those trigger descriptors into `CNTR` entrypoint metadata instead of leaving `triggers: []`, and adds a semantic-style diagnostic when a trigger targets a non-public helper. The next trigger-metadata slice corrected embedded Norito enum/payload wrapping for trigger descriptors, added trigger `authority` encoding, lowered supported `on data ...` and `on pipeline ...` filters into `EventFilterBox` metadata, and emits explicit diagnostics for unsupported `rwa` data triggers and non-canonical trigger authority literals. The newest manifest/access tranche now exports compiler-derived manifest metadata directly from the browser compiler, feeds that structure through `src/shared/lib/kotodama-studio-deploy.ts` instead of rebuilding a downgraded summary manifest in the UI, and upgrades dynamic host-effect access hints so `transfer_asset`, `mint_asset`, `burn_asset`, `set_account_detail`, and `execute_instruction(...)` conservatively emit `*` read/write wildcard coverage instead of incomplete skipped-hint metadata. A 2026-03-29 follow-up landed bounded Norito literal access decoding in the browser compiler so static `execute_instruction(norito_bytes(...))` payloads can now derive exact `domain` / `asset_def` / `nft` / `trigger` detail keys plus `role` / `perm.role` and `trigger.repetitions` effects where the payload is fully decodable, while `execute_query(...)` now derives bounded asset-definition reads where possible and otherwise falls back to upstream-style wildcard coverage instead of reporting a skipped hint. A later 2026-03-29 slice moved canonical single-key i105 rendering into `src/shared/lib/account-literal.ts` and expanded static literal access decoding to cover account-targeted `set/remove_key_value`, `grant/revoke` permission and role bindings, and `FindAssetById` asset-bucket queries, including dataspace-scoped asset IDs, so exact `account`, `account.detail`, `perm.account`, `role.binding`, and `asset` keys are emitted when the payload is fully decodable. The next 2026-03-29 tranche adds transparent `SetAssetKeyValue` / `RemoveAssetKeyValue` Norito envelope decoding, derives exact `asset.detail` metadata access for static asset-balance edits, widens `FindAssetById` query hints to include the same bounded related reads the browser compiler can recover (`account`, `asset`, `asset_def`, synthetic `domain:aid`), and corrects metadata `set/remove_key_value` hints for `domain` / `account` / `asset_def` / `nft` so their detail keys are recorded as read-write instead of write-only. The newest 2026-03-29 slice now decodes static boxed `iroha.transfer`, `iroha.mint`, and `iroha.burn` payloads for the supported asset/domain/asset-definition/NFT/trigger variants, deriving exact bounded access for domain transfers, asset-definition transfers, numeric asset transfers, NFT transfers, asset mint/burn, and trigger-repetition mint/burn instead of collapsing those fully decodable `execute_instruction(norito_bytes(...))` calls to wildcard host effects. Expanded regression coverage in `src/shared/lib/account-literal.test.ts` and `src/shared/lib/kotodama-studio-compiler.test.ts` now covers canonical public-key-to-i105 rendering, malformed/unsupported multihash rejection, exact account detail access, exact account grant/revoke access, exact account-scoped asset query access, exact transparent asset-metadata instruction access, exact boxed transfer/mint/burn access, the map-state success path, constructor/builtin success cases, trigger/runtime builtin success, explicit access metadata, state descriptor embedding, access-attribute parse errors, non-map index diagnostics, local-map success and failure paths, pointer-map `get_or` default errors, illegal value use of `execute_instruction(...)`, trigger metadata embedding, trigger authority/data/pipeline metadata success and failure cases, compiler-exported manifest metadata, wildcard host-access hints, exact decoded instruction/query access-hint cases, and non-public trigger target diagnostics; `src/shared/lib/kotodama-studio-deploy.test.ts` now also verifies the deploy draft consumes compiler-derived manifest data. Reverified the affected compiler/deploy/studio regression slices with `pnpm exec vitest run src/shared/lib/kotodama-studio-compiler.test.ts src/shared/lib/kotodama-studio-deploy.test.ts src/pages/KotodamaStudio.test.ts` and targeted ESLint on the touched compiler files. A later pass restored repository-wide `pnpm typecheck --pretty false` and `pnpm build`; the final 2026-05-31 audit also passed `pnpm run typecheck` and `pnpm run build` against the reusable SDK package boundary. 2026-05-31 completion audit: EX-207 is closed. The compiler now lives in the upstream reusable JS SDK package at `../iroha/javascript/iroha_js/src/kotodamaCompiler`, Explorer consumes it through the `@iroha/iroha-js/kotodama-compiler` package boundary, all current upstream `.ko` corpus sources are represented by refreshable Rust fixtures, expected drift lists are empty, successful fixtures match Rust manifests, debug rows, `CNTR`/`LTLB` sections, and full artifacts byte-for-byte, and upstream SDK tests cover the tracked corpus plus package-dist imports. Remaining future Kotodama language expansion should be opened as new roadmap items when upstream adds new contracts or APIs. |
| EX-208 | Fix the `/studio` production page freeze so Kotodama Studio mounts reliably from a cold load. | COMPLETED | 2026-03-28 completed after reproducing the live `https://taira-explorer.sora.org/studio` failure. Fixed two client-side reactive feedback loops: `src/pages/KotodamaStudio.vue` now persists only metadata/workspace/workflow changes instead of mutating a deep-watched document inside its own callback, and `src/features/studio/WorkflowStudioCanvas.vue` now suppresses no-op `v-model` echoes by comparing normalized workflow snapshots before syncing or emitting. Added regressions in `src/pages/KotodamaStudio.test.ts` and new coverage in `src/features/studio/WorkflowStudioCanvas.test.ts`. Verified with `pnpm exec vitest run src/pages/KotodamaStudio.test.ts src/features/studio/WorkflowStudioCanvas.test.ts`, `pnpm exec eslint --quiet src/pages/KotodamaStudio.vue src/pages/KotodamaStudio.test.ts src/features/studio/WorkflowStudioCanvas.vue src/features/studio/WorkflowStudioCanvas.test.ts`, `pnpm typecheck`, `pnpm build`, and a local `pnpm preview` + Playwright load of `http://127.0.0.1:4173/studio` showing the full Studio content again. |
| EX-209 | Restore `/accounts` rendering when Torii returns canonical mixed Base58 + kana i105 account IDs (for example `sorau...`) that include ASCII payload symbols. | COMPLETED | 2026-03-28 completed: reproduced `https://taira-explorer.sora.org/accounts` in a real browser, confirmed `/v1/explorer/accounts?page=1&per_page=10` returns data, and traced the blank page to the frontend `Account` schema rejecting live mixed-script `sorau...` IDs. A follow-up live sweep confirmed the same deployed-bundle failure pattern on `/transactions` (`authority`) and `/assets` / `/domains` (`owned_by`) because those schemas reuse `AccountIdSchema`. Updated `src/shared/lib/account-literal.ts` to accept both the existing legacy kana-only literals and the upstream mixed Base58 + Iroha-poem canonical alphabet from `../iroha/crates/iroha_data_model/src/account/address.rs`, relaxed the schema error copy in `src/shared/api/schemas.ts`, and added regression coverage in `src/shared/lib/account-literal.test.ts` plus `src/shared/api/schemas.test.ts` for live-style account IDs and invalid non-Base58 ASCII characters. Verified with `pnpm exec vitest run src/pages/AccountsList.test.ts src/shared/lib/account-literal.test.ts src/shared/api/schemas.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-210 | Add explicit `/transactions` mixed-i105 regression coverage and document the current production deploy gap. | COMPLETED | 2026-03-28 completed after reproducing `https://taira-explorer.sora.org/transactions` in Playwright and confirming the live deployed bundle still throws `ZodError` on mixed Base58 + kana `authority` values even though `/v1/explorer/transactions` returns rows. Added a transaction-specific schema regression in `src/shared/api/schemas.test.ts` for paginated transaction payloads with live-style mixed `authority` IDs, plus a routed smoke test in `tests/e2e/transactions-data.smoke.test.ts` that mounts `/transactions` and verifies the page renders those rows. Verified with `pnpm exec vitest run src/shared/api/schemas.test.ts tests/e2e/transactions-data.smoke.test.ts`. `pnpm typecheck` is currently blocked by unrelated pre-existing `src/shared/lib/kotodama-studio-compiler.ts` errors already present in this worktree. |
| EX-211 | Refresh the Kotodama Studio hero headline so it pitches dApps instead of toy-like problems. | COMPLETED | 2026-03-28 completed: updated the English `studio.title` copy in `src/shared/lib/localization/en.json` from "Build Kotodama problems like colorful toys" to "Build Kotodama dApps" so the `/studio` hero matches the requested product framing. Verified with `pnpm exec vitest run src/pages/KotodamaStudio.test.ts`. |
| EX-212 | Retune light mode with a softer neumorphic palette and global-illumination surface treatment. | COMPLETED | 2026-03-28 completed: refreshed the shared light-theme palette/shadow tokens in `src/shared/ui/styles/theme.scss`, softened global illumination in `src/app/App.vue`, and retuned the main chrome surfaces in `src/widgets/header/TheHeader.vue`, `src/features/search/SearchField.vue`, `src/shared/ui/components/BaseButton.vue`, `src/shared/ui/components/BaseContentBlock.vue`, `src/shared/ui/components/BaseInnerBlock.vue`, `src/shared/ui/components/BaseDropdownWindow.vue`, `src/pages/HomePage.vue`, and `src/widgets/home-page-info/HomePageInfo.vue` so light mode reads as a warmer neumorphic surface system instead of flat white cards. Verified with `pnpm build:vite` and `pnpm exec vitest run src/pages/HomePage.test.ts src/widgets/header/TheHeader.test.ts src/shared/ui/components/BaseButton.test.ts src/widgets/latest-blocks/LatestBlocks.test.ts src/widgets/latest-transactions/LatestTransactions.test.ts`. |
| EX-213 | Make the `/studio` contract builder feel like a real Blockly puzzle editor with typed sockets and immediately visible draggable pieces. | COMPLETED | 2026-03-28 completed after reviewing the live `/studio` builder complaint. Tightened Blockly connection checks in `src/features/studio/blockly.ts` so state, entrypoint, trigger, action, and reporter sockets only accept compatible block families, and updated `src/features/studio/ContractStudioBuilder.vue` to preselect the Actions toolbox on mount/mode changes so draggable pieces are visible immediately instead of leaving the canvas looking static. Added regression coverage in `src/features/studio/blockly.test.ts` for statement compatibility, value compatibility, and toolbox inventory. Verified with `pnpm exec vitest run src/features/studio/blockly.test.ts src/pages/KotodamaStudio.test.ts`, `pnpm typecheck`, `pnpm build`, and a local `pnpm preview` + Playwright snapshot of `http://127.0.0.1:4174/studio` showing the Actions flyout open with draggable blocks. |
| EX-214 | Add a `/soracloud` Explorer page that surfaces upstream Torii Soracloud status/control-plane stats. | COMPLETED | 2026-03-29 completed: added `src/pages/SoracloudPage.vue` on `/soracloud`, wired navigation + scoped-route preservation, added Torii `fetchSoracloudStatus()` support against `GET /v1/soracloud/status`, modeled the Soracloud status/control-plane schemas, and added tested helper transforms for Soracloud summaries/action labels/service ordering. Verified with `pnpm exec vitest run src/pages/SoracloudPage.test.ts src/shared/lib/soracloud.test.ts src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/shared/lib/explorer-scope.test.ts src/app/router.test.ts src/shared/config.test.ts`, `pnpm exec eslint --quiet src/pages/SoracloudPage.vue src/pages/SoracloudPage.test.ts src/shared/lib/soracloud.ts src/shared/lib/soracloud.test.ts src/shared/api/index.ts src/shared/api/index.test.ts src/shared/api/schemas.ts src/shared/api/schemas.test.ts src/shared/config.ts src/shared/config.test.ts src/shared/lib/explorer-scope.ts src/shared/lib/explorer-scope.test.ts src/app/router.ts src/app/router.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-215 | Add a `/vpn` Explorer page that surfaces upstream SoraNet VPN relay stats from the selected node. | COMPLETED | 2026-03-29 completed: added `/vpn` with nav wiring plus a VPN overview/breakdown/countries UI backed by the selected node's raw `/metrics` Prometheus payload and peer-country summaries from `GET /v1/telemetry/peers-info`. Added `src/shared/lib/vpn.ts` parser/grouping helpers, raw metrics text fetching in `src/shared/api/index.ts`, shared byte formatting, VPN locale keys across all shipped locale packs, and targeted coverage in `src/shared/lib/vpn.test.ts`, `src/pages/VpnStats.test.ts`, `src/shared/api/index.test.ts`, `src/app/router.test.ts`, `src/shared/config.test.ts`, `src/shared/ui/utils/formatters.test.ts`, and `src/shared/lib/localization/index.test.ts`. Verified with `pnpm exec vitest run src/pages/VpnStats.test.ts src/shared/lib/vpn.test.ts src/shared/api/index.test.ts src/app/router.test.ts src/shared/config.test.ts src/shared/ui/utils/formatters.test.ts src/shared/lib/localization/index.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-216 | Keep Kotodama Studio’s Blockly workspace sized to the actual contract canvas so the block editor does not collapse into a blank pane on larger layouts. | COMPLETED | 2026-03-29 completed after reproducing `/studio` with Playwright and finding the contract card at roughly `868x608` while Blockly’s SVG stayed around `488x150`, leaving the pane visually blank. Updated `src/features/studio/ContractStudioBuilder.vue` to schedule `Blockly.svgResize(...)` after mount, on toolbox/template updates, on window resize, and through a `ResizeObserver`, and gave the inner workspace a real `height: 100%` so it fills the card. Added `src/features/studio/ContractStudioBuilder.test.ts` coverage for resize-on-mount and resize-on-layout-change behavior. Verified with `pnpm exec vitest run src/features/studio/ContractStudioBuilder.test.ts src/features/studio/blockly.test.ts src/pages/KotodamaStudio.test.ts` and `pnpm exec eslint --quiet src/features/studio/ContractStudioBuilder.vue src/features/studio/ContractStudioBuilder.test.ts`. Fresh browser reload verification is currently blocked by an unrelated dirty-worktree router import to missing `src/pages/SoracloudPage.vue`. |
| EX-217 | Make Kotodama Studio’s workflow editor reliable for dragging, selecting, and linking nodes. | COMPLETED | 2026-03-29 completed after reproducing `/studio` in Playwright and confirming the workflow viewport was over-fitted so only one starter node stayed visible while connection handles were still the theme-default 6px targets. Updated `src/features/studio/WorkflowStudioCanvas.vue` to stop relying on `fit-view-on-init`, refit the viewport after real layout via `ResizeObserver` + `fitView()`, auto-refit again after new nodes are added, reject invalid/self-loop/over-capacity connections before they hit the edge list, and delegate node/edge state transitions to Vue Flow’s `applyNodeChanges`, `applyEdgeChanges`, and `addEdge` helpers so dragging/linking stays consistent. Updated `src/features/studio/StudioFlowNode.vue` to give source/target handles larger, easier hit targets. Added regression coverage in `src/features/studio/WorkflowStudioCanvas.test.ts` for viewport fitting, add-node refits, dragging, deduplication, and semantic connection rejection. Verified with `pnpm exec vitest run src/features/studio/ContractStudioBuilder.test.ts src/features/studio/WorkflowStudioCanvas.test.ts src/features/studio/blockly.test.ts src/pages/KotodamaStudio.test.ts`, `pnpm exec eslint --quiet src/features/studio/WorkflowStudioCanvas.vue src/features/studio/WorkflowStudioCanvas.test.ts src/features/studio/StudioFlowNode.vue`, `pnpm typecheck --pretty false`, and a local Playwright pass of `http://127.0.0.1:4175/studio` showing the starter workflow nodes and a newly added contract node inside the visible canvas while an invalid output-to-trigger drag left the rendered edge count unchanged at two. |
| EX-274 | Show asset-definition aliases on `/econometrics` when the selected asset has an alias binding. | COMPLETED | 2026-04-18 completed: preserved alias metadata from `fetchAssetDefinition()` inside `src/pages/Econometrics.vue`, render the alias in the computed snapshot alongside the canonical definition ID, and show alias + canonical ID together in the asset-definition picker rows so base58-only URLs still reveal the bound alias when present. Added regression coverage in `src/pages/Econometrics.test.ts` for both the canonical-id query flow and alias rendering in the picker list. Verified with `pnpm exec vitest run src/pages/Econometrics.test.ts`, `pnpm exec eslint --quiet src/pages/Econometrics.vue src/pages/Econometrics.test.ts`, and `pnpm typecheck --pretty false`. |
| EX-218 | Show asset aliases in the first `/assets` column and canonical asset IDs in the second column. | COMPLETED | 2026-03-29 completed: updated `src/pages/AssetsList.vue` so the asset definitions and holders tables/mobile cards render alias-or-`-` first and the canonical asset definition ID second, while keeping links/copy affordances on the ID column via `BaseHash`. Added regression coverage in `src/pages/AssetsList.test.ts` for both alias-bound and alias-missing rows on the definitions and holders views. Verified with `pnpm exec vitest run src/pages/AssetsList.test.ts` and `pnpm exec eslint --quiet src/pages/AssetsList.vue src/pages/AssetsList.test.ts`. |
| EX-219 | Harden the `/vpn` page’s runtime-state parsing, coverage, and locale quality after the initial launch. | COMPLETED | 2026-03-29 completed: tightened `soranet_vpn_runtime_status` parsing in `src/shared/lib/vpn.ts` so only a single binary `1` state resolves to `active`/`stubbed`/`disabled`, added malformed-state coverage plus raw `/metrics` non-OK/network-failure tests in `src/shared/lib/vpn.test.ts` and `src/shared/api/index.test.ts`, and replaced the placeholder `vpn.*` copy across all shipped JSON locale packs while adding explicit poetic `akk`/`egy` VPN overrides in `src/shared/lib/localization/index.ts`. Verified with `pnpm exec vitest run src/shared/lib/vpn.test.ts src/shared/api/index.test.ts src/shared/lib/localization/index.test.ts src/pages/VpnStats.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-220 | Expand `/soracloud` into a polling, localized operations dashboard covering the full upstream Soracloud status surface. | COMPLETED | 2026-03-29 completed: refactored `src/pages/SoracloudPage.vue` into a polling multi-section Soracloud operations dashboard with stale-data retention, section-local errors, and explicit inspector inputs for all current Torii Soracloud status GET endpoints (`/service/config/status`, `/service/secret/status`, `/training/job/status`, `/model/weight/status`, `/model/artifact/status`, `/model/upload/status`, `/model/compile/status`, `/model/run-status`, `/hf/status`, `/model-host/status`, `/agent/status`, `/agent/mailbox/status`, `/agent/autonomy/status`). Added Soracloud schemas/query types/fetch helpers, service/apartment suggestion + query-readiness helpers, full shipped JSON locale parity for the expanded `soracloud` namespace, and routed/component/API/localization/e2e smoke coverage. Verified with `pnpm exec eslint --quiet src/pages/SoracloudPage.vue src/pages/SoracloudPage.test.ts src/shared/lib/soracloud.ts src/shared/lib/soracloud.test.ts src/shared/api/index.ts src/shared/api/index.test.ts src/shared/api/schemas.ts src/shared/api/schemas.test.ts src/shared/lib/localization/index.test.ts tests/e2e/app-routing.smoke.test.ts tests/e2e/soracloud-data.smoke.test.ts`, `pnpm exec vitest run src/pages/SoracloudPage.test.ts src/shared/lib/soracloud.test.ts src/shared/api/index.test.ts src/shared/api/schemas.test.ts src/shared/lib/localization/index.test.ts tests/e2e/app-routing.smoke.test.ts tests/e2e/soracloud-data.smoke.test.ts`, `pnpm typecheck`, and `pnpm build`. |
| EX-221 | Split the browser Kotodama compiler into a package boundary and add the first Rust-refreshable parity fixture harness. | COMPLETED | 2026-05-28 completed: the browser Kotodama compiler now lives behind the reusable upstream SDK subpath `@iroha/iroha-js/kotodama-compiler`, Explorer consumes that package boundary through a thin re-export, and `tests/fixtures/kotodama-parity/` remains refreshable from sibling `../iroha` via `pnpm refresh:kotodama-parity`. The parity harness now checks success/failure kind parity, normalized diagnostics, exact Rust `source_map`/`budget_report` rows for every current success fixture, selected artifact/contract-view behavior, and an empty strict debug-row drift list. Remaining broader compiler work is tracked under EX-207, including full semantic coverage and raw artifact/hash parity beyond debug rows. |
| EX-222 | Harden Kotodama Studio workflow import/runtime validation and add a real browser drag-connect regression. | COMPLETED | 2026-03-29 completed: added shared workflow sanitization/reconciliation helpers so persisted docs and the live canvas use the same connection rules, and `src/shared/lib/kotodama-studio-document.ts` now reports dropped invalid workflow edges during import/restore parsing. `src/pages/KotodamaStudio.vue` now clears stale contract bindings when Blockly entrypoints disappear, reports sanitized import counts, widens the starter workflow spacing, and stacks the contract/workflow canvases so the workflow editor gets the full center-column width on large screens. `src/features/studio/WorkflowStudioCanvas.vue` now sanitizes mirrored props, keeps a lower-but-still-readable auto-fit zoom floor, and focuses newly added nodes so they stay in-frame; `src/features/studio/StudioFlowNode.vue` enlarged the handle hit target again. Added targeted coverage in `src/shared/lib/kotodama-studio-workflow.test.ts`, `src/shared/lib/kotodama-studio-document.test.ts`, `src/pages/KotodamaStudio.test.ts`, and the first real browser Playwright smoke in `tests/playwright/kotodama-studio-workflow.pw.ts` with `playwright.config.ts` and `@playwright/test`. Verified with `pnpm exec vitest run src/shared/lib/kotodama-studio-workflow.test.ts src/shared/lib/kotodama-studio-document.test.ts src/features/studio/WorkflowStudioCanvas.test.ts src/pages/KotodamaStudio.test.ts src/features/studio/ContractStudioBuilder.test.ts src/features/studio/blockly.test.ts`, `pnpm exec eslint --quiet src/shared/lib/kotodama-studio-workflow.ts src/shared/lib/kotodama-studio-workflow.test.ts src/shared/lib/kotodama-studio-document.ts src/shared/lib/kotodama-studio-document.test.ts src/features/studio/WorkflowStudioCanvas.vue src/features/studio/WorkflowStudioCanvas.test.ts src/features/studio/StudioFlowNode.vue src/pages/KotodamaStudio.vue src/pages/KotodamaStudio.test.ts playwright.config.ts tests/playwright/kotodama-studio-workflow.pw.ts`, `pnpm typecheck --pretty false`, and `pnpm exec playwright test tests/playwright/kotodama-studio-workflow.pw.ts`. |
| EX-223 | Add an RWA tab to `/accounts/:id` so operators can browse account-owned RWAs alongside NFTs. | COMPLETED | 2026-03-29 completed: scoped the change to `src/pages/AccountDetails.vue` so the account page now uses an account-only `Assets / NFTs / RWAs` tab set without widening the shared domain/account asset-tab model. Added an RWA branch backed by `/rwas?owned_by=...`, including row/mobile rendering and row navigation to `/rwas/:id`, while keeping the existing numeric-assets and NFTs behavior unchanged. Expanded `src/pages/AccountDetails.test.ts` so the mocked `useParamScope` harness can drive all per-section scopes and added a regression that switches to the RWA tab, renders account-owned RWA data, and asserts navigation to the RWA detail page. Verified with `pnpm exec vitest run src/pages/AccountDetails.test.ts`, `pnpm exec eslint --quiet src/pages/AccountDetails.vue src/pages/AccountDetails.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build`. |
| EX-224 | Accept TAIRA `testu...` account routes/filters in the Explorer UI while translating them to the Torii-compatible account selector for API calls. | COMPLETED | 2026-03-29 completed: verified the live TAIRA explorer endpoints still reject `testu...` account selectors with `ERR_UNEXPECTED_NETWORK_PREFIX` while the same account resolves when the selector is normalized to `sorau...`. Added `normalizeToriiAccountSelectorLiteral()` in `src/shared/lib/account-literal.ts`, rewired `src/shared/api/index.ts` so `/accounts/:id`, owner filters (`owned_by`), instruction/transaction account filters, and Nexus dataspace account summary requests normalize encoded account IDs before hitting Torii while leaving aliases intact, and then added a display-side formatter so TAIRA/Testus account hashes, copied values, QR payloads, links, and scoped row navigation render `testu...` while preserving canonical `sorau...` API traffic (`src/shared/lib/account-id.ts`, `src/shared/ui/components/BaseHash.vue`, `src/shared/ui/components/BaseLink.vue`, `src/shared/ui/composables/useExplorerScopeNavigation.ts`, `src/pages/AccountDetails.vue`). Expanded regression coverage in `src/shared/lib/account-literal.test.ts`, `src/shared/lib/account-id.test.ts`, `src/shared/ui/components/BaseHash.test.ts`, `src/shared/ui/components/BaseLink.test.ts`, `src/shared/ui/composables/useExplorerScopeNavigation.test.ts`, `src/pages/AccountDetails.test.ts`, plus related page checks in `src/pages/AccountsList.test.ts` and `src/pages/DomainDetails.test.ts`. Verified with `pnpm exec vitest run src/shared/lib/account-literal.test.ts src/shared/api/index.test.ts src/shared/lib/account-id.test.ts src/shared/ui/components/BaseHash.test.ts src/shared/ui/components/BaseLink.test.ts src/shared/ui/composables/useExplorerScopeNavigation.test.ts src/pages/AccountDetails.test.ts src/pages/AccountsList.test.ts src/pages/DomainDetails.test.ts` and `pnpm typecheck --pretty false`. |
| EX-225 | Finish the remaining Kotodama Studio workflow hardening gaps from the EX-222 follow-up audit. | COMPLETED | 2026-03-29 completed: added shared readable node-ID allocation in `src/shared/lib/kotodama-studio-workflow.ts` so new workflow nodes now use the highest existing category suffix instead of `nodes.length`, and rewired `src/features/studio/WorkflowStudioCanvas.vue` to use that helper when adding nodes. Expanded unit coverage in `src/shared/lib/kotodama-studio-workflow.test.ts` and `src/features/studio/WorkflowStudioCanvas.test.ts` to lock the non-colliding ID path. Hardened the browser harness by switching `playwright.config.ts` to bundled Chromium without the system Chrome channel, serving `/studio` from `pnpm build:vite && pnpm preview`, raising the server startup timeout, and adding `playwright:install` in `package.json`. Expanded `tests/playwright/kotodama-studio-workflow.pw.ts` so the real browser suite now checks dynamic add-node visibility, sanitized JSON imports, and silent stale-binding cleanup from persisted localStorage hydration. Verified with `pnpm exec vitest run src/shared/lib/kotodama-studio-workflow.test.ts src/features/studio/WorkflowStudioCanvas.test.ts src/shared/lib/kotodama-studio-document.test.ts src/pages/KotodamaStudio.test.ts`, `pnpm exec eslint --quiet src/shared/lib/kotodama-studio-workflow.ts src/shared/lib/kotodama-studio-workflow.test.ts src/features/studio/WorkflowStudioCanvas.vue src/features/studio/WorkflowStudioCanvas.test.ts playwright.config.ts tests/playwright/kotodama-studio-workflow.pw.ts package.json`, `pnpm typecheck --pretty false`, and `pnpm exec playwright test tests/playwright/kotodama-studio-workflow.pw.ts`. |
| EX-226 | Finish the remaining `/soracloud` follow-up gaps: split the page, add real locale copy, tighten structured-field validation, and add an env-gated live browser check. | COMPLETED | 2026-03-29 completed: confirmed the Soracloud status surface against sibling `../iroha`, split the former 3.2k-line `src/pages/SoracloudPage.vue` into feature-scoped section components/composables under `src/features/soracloud/`, and moved the polling/request orchestration into a shared Soracloud dashboard layer while preserving stale-data retention and section-local errors. Added structured-field validation for `account_id`, `lease_term_ms`, `bundle_root`, and `compile_profile_hash` with inline field errors and disabled-submit behavior, covered by `src/features/soracloud/validation.test.ts` plus new route-level assertions in `src/pages/SoracloudPage.test.ts`. Replaced the mirrored-English Soracloud namespace across all shipped JSON locale packs, added explicit `akk`/`egy` Soracloud overrides in `src/shared/lib/localization/index.ts`, tightened `src/shared/lib/localization/index.test.ts` so Soracloud locale packs cannot stay byte-identical to English, and added the opt-in live browser check in `tests/playwright/soracloud-live.pw.ts` using `PLAYWRIGHT_SORACLOUD_TORII_URL` + `localStorage['torii_base_url']`. Verified with `pnpm exec eslint --quiet src/pages/SoracloudPage.vue src/pages/SoracloudPage.test.ts src/features/soracloud/*.ts src/features/soracloud/*.vue src/features/soracloud/validation.test.ts tests/playwright/soracloud-live.pw.ts src/shared/lib/localization/index.ts src/shared/lib/localization/index.test.ts`, `pnpm exec vitest run src/features/soracloud/validation.test.ts src/pages/SoracloudPage.test.ts src/shared/lib/soracloud.test.ts src/shared/lib/localization/index.test.ts tests/e2e/soracloud-data.smoke.test.ts tests/e2e/app-routing.smoke.test.ts`, `pnpm typecheck --pretty false`, `pnpm build`, and `pnpm exec playwright test tests/playwright/soracloud-live.pw.ts` (skipped cleanly without `PLAYWRIGHT_SORACLOUD_TORII_URL`). |
| EX-227 | Repair Kotodama Studio’s typography, layout polish, and editor validity so `/studio` stops producing broken programs and flaky workflow links. | COMPLETED | 2026-03-29 completed: unified the Studio page/nodes/Blockly chrome on the global Sora stack, removed the remaining serif overrides from the Studio surface, and retuned the workflow node styling so the editor reads as one coherent workspace. Hardened Blockly by replacing the stale free-text state/entrypoint references with reference-aware dropdown-backed blocks, identifier sanitizers, and workspace reconciliation that auto-corrects renamed references and disables/warns on blocks that would otherwise generate invalid programs. Hardened the workflow canvas by moving node/edge transitions onto Vue Flow’s helper APIs, adding non-overlapping grid slot placement for new nodes, and extending the real browser regression to verify links survive a node drag after connect. Verified with `pnpm exec vitest run src/features/studio/blockly.test.ts src/features/studio/WorkflowStudioCanvas.test.ts src/features/studio/ContractStudioBuilder.test.ts src/shared/lib/kotodama-studio-workflow.test.ts src/pages/KotodamaStudio.test.ts`, `pnpm exec eslint --quiet src/features/studio/blockly.ts src/features/studio/blockly.test.ts src/features/studio/ContractStudioBuilder.vue src/features/studio/ContractStudioBuilder.test.ts src/features/studio/WorkflowStudioCanvas.vue src/features/studio/WorkflowStudioCanvas.test.ts src/features/studio/StudioFlowNode.vue src/shared/lib/kotodama-studio-workflow.ts src/shared/lib/kotodama-studio-workflow.test.ts src/pages/KotodamaStudio.vue tests/playwright/kotodama-studio-workflow.pw.ts`, `pnpm typecheck --pretty false`, `pnpm exec playwright test tests/playwright/kotodama-studio-workflow.pw.ts --reporter=line`, and `pnpm build:vite`. |
| EX-228 | Add live Kotodama Studio semantic checks plus explicit workflow editing so `/studio` catches conceptual errors before compile. | COMPLETED | 2026-03-29 completed: added `src/shared/lib/kotodama-studio-semantics.ts` so `/studio` now combines live Blockly/workflow diagnostics before compile, including duplicate definitions, malformed literal blocks, unreachable workflow steps, invalid contract bindings, missing logic labels, and bad schedule config. `src/features/studio/blockly.ts` now validates the remaining risky literal fields (permission/name/json key sanitizers, trimmed account/asset literals) and reports raw query blocks for manual review, while `src/features/studio/ContractStudioBuilder.vue` emits workspace issues into the page. `src/pages/KotodamaStudio.vue` now treats workflow bindings as public-entrypoint-only, exposes category-specific workflow node editors plus explicit link add/edit/remove controls, clearly labels the workflow as storyboard-only relative to compile/deploy, and blocks compile/deploy when live semantic errors remain. `src/features/studio/WorkflowStudioCanvas.vue` now gives new nodes sensible default config and auto-selects them for editing, and `src/features/studio/StudioFlowNode.vue` renders config badges so nodes communicate real behavior at a glance. Added coverage in `src/shared/lib/kotodama-studio-semantics.test.ts`, `src/features/studio/blockly.test.ts`, `src/features/studio/WorkflowStudioCanvas.test.ts`, `src/features/studio/ContractStudioBuilder.test.ts`, and `src/pages/KotodamaStudio.test.ts`. Verified with `pnpm exec vitest run src/features/studio/blockly.test.ts src/features/studio/ContractStudioBuilder.test.ts src/features/studio/WorkflowStudioCanvas.test.ts src/shared/lib/kotodama-studio-semantics.test.ts src/pages/KotodamaStudio.test.ts`, `pnpm exec eslint --quiet src/features/studio/blockly.ts src/features/studio/blockly.test.ts src/features/studio/ContractStudioBuilder.vue src/features/studio/ContractStudioBuilder.test.ts src/features/studio/WorkflowStudioCanvas.vue src/features/studio/WorkflowStudioCanvas.test.ts src/features/studio/StudioFlowNode.vue src/pages/KotodamaStudio.vue src/pages/KotodamaStudio.test.ts src/shared/lib/kotodama-studio-semantics.ts src/shared/lib/kotodama-studio-semantics.test.ts src/shared/lib/localization/en.json`, `pnpm exec playwright test tests/playwright/kotodama-studio-workflow.pw.ts --reporter=line`, and `pnpm build:vite`; repository-wide `pnpm typecheck --pretty false` is still blocked by the pre-existing unrelated `src/shared/lib/account-literal.ts` error already present in the worktree. |
| EX-229 | Align explorer account/asset/contract surfaces with the 2026-04-03 upstream account/domain and address-first smart-contract model, and add a deployed smart contracts inventory page. | COMPLETED | 2026-04-04 completed: reviewed `../iroha` follow-ups (`cc7c15b460 update account handling`, `d2b86fc5b9 update smart contracts`) and confirmed the explorer’s account/asset selectors already matched the current canonical ID model, so the actual frontend drift was in smart-contract payload handling. Added shared activation parsing in `src/shared/lib/smart-contracts.ts`, migrated local contract-view activation comments in `src/shared/lib/contract-view.ts` to address-first payloads, normalized governance deploy proposal parsing in `src/shared/api/schemas.ts`, and updated `/governance` rendering in `src/pages/GovernanceDashboard.vue` to surface `contract_address` plus optional alias/dataspace. Added the new `/contracts` inventory route/page in `src/pages/SmartContractsPage.vue`, wired it into `src/app/router.ts` and `src/shared/config.ts`, and covered the new behavior in `src/shared/lib/smart-contracts.test.ts`, `src/shared/lib/contract-view.test.ts`, `src/shared/api/schemas.test.ts`, `src/pages/SmartContractsPage.test.ts`, `src/app/router.test.ts`, and `src/shared/config.test.ts`. Verified with `pnpm exec vitest run src/shared/lib/smart-contracts.test.ts src/shared/lib/contract-view.test.ts src/pages/SmartContractsPage.test.ts src/shared/api/schemas.test.ts src/app/router.test.ts src/shared/config.test.ts src/pages/GovernanceDashboard.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-230 | Rework Kotodama Studio deploy drafts around address-first Torii `/v1/contracts/deploy` semantics instead of legacy manual activation payloads. | COMPLETED | 2026-04-04 completed: `src/shared/lib/kotodama-studio-deploy.ts` now emits a Torii deploy envelope (`deployMode='torii_contracts_deploy_v1'`, `toriiRequest.endpoint='/v1/contracts/deploy'`) instead of fake register/activate instruction previews, with `dataspace` normalized to the endpoint’s `universal` default when appropriate. `src/shared/lib/kotodama-studio-document.ts`, `src/pages/KotodamaStudio.vue`, and `src/shared/lib/kotodama-studio-source.ts` now carry an explicit Studio `dataspace` field, while `namespace` / `contractId` remain document/source annotations rather than public activation inputs. Added regression coverage in `src/shared/lib/kotodama-studio-deploy.test.ts`, `src/shared/lib/kotodama-studio-document.test.ts`, `src/shared/lib/kotodama-studio-source.test.ts`, and refreshed Studio page/playwright fixtures. Verified with `pnpm exec vitest run src/shared/lib/kotodama-studio-deploy.test.ts src/shared/lib/kotodama-studio-document.test.ts src/shared/lib/kotodama-studio-source.test.ts src/pages/KotodamaStudio.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-231 | Harden Kotodama Studio against outdated or capability-limited external signer bridges after the Torii deploy-draft migration. | COMPLETED | 2026-04-04 completed: `src/pages/KotodamaStudio.vue` now accepts either `signAndSubmitDraft(...)` or `signAndSubmitToriiRequest(...)` bridge methods, warns when bridge capabilities explicitly omit `torii_contracts_deploy_v1`, and normalizes legacy `txInstructions`-style failures into an actionable “update the bridge” error. Added deploy-path coverage in `src/pages/KotodamaStudio.test.ts` for legacy draft submission, direct Torii-request submission, unsupported-capability warnings, and outdated-bridge errors. Verified with `pnpm exec vitest run src/pages/KotodamaStudio.test.ts src/shared/lib/kotodama-studio-deploy.test.ts src/shared/lib/kotodama-studio-document.test.ts src/shared/lib/kotodama-studio-source.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-232 | Let Kotodama Studio deploy directly to Torii with an operator-supplied private key when no compatible external bridge is available. | COMPLETED | 2026-04-04 completed: added typed direct-deploy request/response schemas in `src/shared/api/schemas.ts`, a shared `submitContractDeployRequest(...)` helper in `src/shared/api/index.ts`, and a non-persisted password field in `src/pages/KotodamaStudio.vue` so `/studio` can post compiled bundles to `POST /v1/contracts/deploy` itself when the operator provides a private key. Direct deploy now clears the secret after a successful submission, and Studio document persistence/export stays secret-free because the private key lives only in page state. Added regression coverage in `src/shared/api/schemas.test.ts`, `src/shared/api/index.test.ts`, and `src/pages/KotodamaStudio.test.ts`. Verified with `pnpm exec vitest run src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/pages/KotodamaStudio.test.ts src/shared/lib/kotodama-studio-deploy.test.ts src/shared/lib/kotodama-studio-document.test.ts src/shared/lib/kotodama-studio-source.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-233 | Remove the remaining legacy Kotodama Studio deploy/metadata surface now that direct Torii deploy is the canonical path. | COMPLETED | 2026-04-04 completed: removed the external `IrohaConnectStudio` bridge submission path from `src/pages/KotodamaStudio.vue`, so `/studio` now deploys only through the direct Torii private-key flow added in EX-232 and keeps the deploy action disabled until a private key is provided. Dropped stale Studio `namespace` / `contractId` metadata from `src/shared/lib/kotodama-studio-document.ts`, `src/shared/lib/kotodama-studio-source.ts`, and `src/shared/lib/kotodama-studio-deploy.ts`, switched export filenames to a title-derived slug, and kept backward-compatible import normalization so older saved JSON files still open by mapping legacy `namespace` to `dataspace`. Updated `src/pages/KotodamaStudio.test.ts`, `src/shared/lib/kotodama-studio-document.test.ts`, `src/shared/lib/kotodama-studio-deploy.test.ts`, `src/shared/lib/kotodama-studio-source.test.ts`, `tests/playwright/kotodama-studio-workflow.pw.ts`, and `src/shared/lib/localization/en.json` to lock the cleanup. Verified with `pnpm exec vitest run src/shared/lib/kotodama-studio-document.test.ts src/shared/lib/kotodama-studio-deploy.test.ts src/shared/lib/kotodama-studio-source.test.ts src/pages/KotodamaStudio.test.ts`, `pnpm typecheck --pretty false`, `pnpm build:vite`, and `pnpm exec playwright test tests/playwright/kotodama-studio-workflow.pw.ts`. |
| EX-234 | Remove the final Studio cleanup gaps by dropping legacy import shims and shipping real locale packs for `/studio`. | COMPLETED | 2026-04-04 completed: removed the remaining `metadata.namespace -> dataspace` fallback from `src/shared/lib/kotodama-studio-document.ts`, so legacy namespace-only Studio documents now fall back to the current default dataspace instead of silently inheriting the obsolete identifier model. Machine-translated the full `studio` namespace into every shipped JSON locale pack under `src/shared/lib/localization/*.json`, including `zh-TW`, and added Studio-specific parity/localization assertions to `src/shared/lib/localization/index.test.ts` so `/studio` no longer depends on English fallback or English-copy locale packs. Updated `src/shared/lib/kotodama-studio-document.test.ts` to lock the hard-drop behavior. Verified with `pnpm exec vitest run src/shared/lib/kotodama-studio-document.test.ts src/pages/KotodamaStudio.test.ts src/shared/lib/localization/index.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-235 | Polish the roughest Kotodama Studio locale copy after the bulk locale import so the main deploy flow reads naturally in high-traffic languages. | COMPLETED | 2026-04-04 completed: manually refined the most visible `/studio` strings in `src/shared/lib/localization/es.json`, `de.json`, `fr.json`, `jp.json`, `zh-TW.json`, and `ru.json` to replace literal machine-translation artifacts in the compile/deploy path, metadata card, workflow hints, and generated-source labels. Kept the Studio locale parity/localization guardrails in `src/shared/lib/localization/index.test.ts` and verified the polished packs with `pnpm exec vitest run src/shared/lib/localization/index.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-236 | Continue the Kotodama Studio locale polish pass across the remaining high-traffic shipped JSON packs. | COMPLETED | 2026-04-04 completed: manually refined the most visible `/studio` strings in `src/shared/lib/localization/ar.json`, `id.json`, `ko.json`, `tr.json`, `uk.json`, `th.json`, and `he.json`, replacing literal machine-translation artifacts across the contract canvas, workflow guidance, deploy/compile messaging, and key field labels while keeping the existing Studio parity/localization guardrails unchanged. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-237 | Keep reducing bulk-translation artifacts in the remaining Kotodama Studio locale packs. | COMPLETED | 2026-04-04 completed: manually refined the next visibly rough `/studio` locale tranche in `src/shared/lib/localization/am.json`, `az.json`, `si.json`, `ta.json`, and `ur.json`, replacing awkward bulk-import phrasing across the compile/deploy workflow, contract/workflow hints, and operator-facing field labels while keeping the Studio localization guardrails unchanged. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-238 | Finish the Kotodama Studio locale polish pass across the remaining shipped JSON packs. | COMPLETED | 2026-04-04 completed: refined the final untouched `/studio` locale packs in `src/shared/lib/localization/dv.json`, `dz.json`, `hy.json`, `kh.json`, `ka.json`, `my.json`, and `ne.json`, reducing mixed-English artifacts and awkward deploy/compile or contract/workflow phrasing so every shipped JSON locale pack now has a manually touched Studio namespace. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-239 | Reduce the remaining code-mixed English carryovers in already polished Kotodama Studio locale packs. | COMPLETED | 2026-04-04 completed: rescanned the non-English `studio` namespaces for leftover raw English carryovers and tightened the remaining high-signal strings in `he`, `jp`, `zh-TW`, `ru`, `uk`, `ko`, `th`, `si`, `ta`, `ur`, `am`, `hy`, `ka`, `kh`, `my`, and `ne`. Replaced mixed-language `Studio` / `Explorer` / `deploy` / `bundle` / `commit` / `Storyboard` wording inside the Studio canvas, validation, and compile/deploy messaging while preserving intentional technical tokens such as `Kotodama`, `Torii`, `JSON`, `NFT`, `ABI`, and endpoint paths. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-240 | Prevent negative relative timestamps in Explorer widgets when a block/sample timestamp is ahead of the viewer clock. | COMPLETED | 2026-04-04 completed: clamped future/invalid deltas to zero in `src/shared/lib/time.ts` so shared `TimeStamp`/`useTimeAgo` consumers stop rendering negative `ago` values, added regression coverage in `src/shared/lib/time.test.ts` for future timestamps in both normal and detailed-second modes, and refreshed `src/widgets/latest-blocks/LatestBlocks.test.ts` so the widget regression slice includes the newer scoped-navigation dependency on `getToriiBaseUrl()`. Verified with `pnpm exec vitest run src/shared/lib/time.test.ts src/widgets/latest-blocks/LatestBlocks.test.ts` and `pnpm typecheck --pretty false`. Live probe at `https://taira-explorer.sora.org/v1/explorer/blocks?per_page=5` during the fix returned current-looking `2026-04-04T10:27Z`-`10:31Z` block timestamps, so the originally reported future block appears transient or client-clock-related; the UI is now hardened either way. |
| EX-241 | Add deployed SoraFS file links plus a wallet-assisted CID blacklist proposal flow on `/sorafs/registry`. | COMPLETED | 2026-04-04 partial completion: `/sorafs/registry` now fetches `/v1/sorafs/storage/manifest/{digest}`, decodes `ManifestV1.root_cid` out of `manifest_b64`, and renders direct public file links under `/sorafs/cid/<cid>/...` with a new optional `sorafsPublicBaseUrl` runtime-config override. Latest tranche: added typed Torii Connect status/session helpers plus browser-side SID/deeplink utilities, and the selected-manifest detail now includes an IrohaConnect wallet panel that checks `/v1/connect/status`, creates `/v1/connect/session` QR/deeplink invites, rewrites the launcher to `irohaconnect://…`, and lets operators copy the SID/launch link. The same panel now generates a fuller `AgendaProposalV1`-shaped CID denylist draft with the documented enumerated tags (`csam`, `malware`, `fraud`, `harassment`, `impersonation`, `policy-escalation`, `terrorism`, `spam`), optional PGP fingerprint + duplicate proposal references, richer evidence generation (`sorafs-cid` + public URL when available), and a CLI-ready handoff path: suggested export filename plus download/copy actions for the draft JSON and the documented `cargo xtask ministry-agenda validate` / `impact` commands. Final 2026-04-06 completion came through EX-246/EX-247: `/sorafs/registry` now drafts Ministry agenda submissions against Torii, drives the approved-wallet Connect sign-and-submit loop to terminal status, fetches the confirmed Ministry record, keeps the export/download fallback intact, and ships fully localized SoraFS moderation/connect/proposal copy plus localized generated proposal payload text across every shipped locale. Verified with `pnpm vitest --run src/shared/lib/localization/index.test.ts src/shared/lib/sorafs-blacklist-proposal.test.ts src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/pages/SorafsRegistry.test.ts`, `pnpm typecheck`, and upstream `../iroha/javascript/iroha_js npm run build:dist`. |
| EX-242 | Extend Torii SoraFS CID lookup with gateway moderation visibility for CID/manifest denylist matches. | COMPLETED | 2026-04-06 completed: updated the upstream `../iroha` SoraFS gateway read model so `GET /v1/sorafs/cid/{cid}` bypasses denylist enforcement while still returning a nullable `moderation` object with `clear/local/global/mixed` status, `public_links_enabled`, and per-match provenance (`scope`, `match_kind`, `policy_tier`, timestamps, governance refs, pack IDs, pack manifest CID, and Merkle root). The denylist loader now threads source-pack metadata into loaded entries, pack-level governance provenance satisfies permanent-entry validation, and content-serving `/sorafs/cid/...` routes remain denylist-enforced. Added regression coverage in `../iroha/crates/iroha_torii/src/sorafs/api.rs`, `../iroha/crates/iroha_torii/src/sorafs/gateway/denylist.rs`, and `../iroha/crates/iroha_torii/src/lib.rs`. Verified with `timeout 180 cargo test -p iroha_torii --features app_api cid_lookup_reports_moderation_without_unblocking_gateway_fetches`, `timeout 120 cargo test -p iroha_torii --features app_api --lib pack_metadata_flows_into_loaded_entries`, `timeout 120 cargo test -p iroha_torii --features app_api --lib entry_preserves_source_pack_metadata`, and `timeout 120 cargo test -p iroha_torii --features app_api --lib entry_without_governance_provenance_remains_local`. |
| EX-243 | Surface gateway moderation state on `/sorafs/registry` and switch denylist drafting between add/remove actions. | COMPLETED | 2026-04-06 completed: `src/pages/SorafsRegistry.vue` now fetches the extended CID lookup in parallel with the storage manifest, renders a moderation banner that keeps blocked manifests and file inventory visible, disables only the current gateway’s public file links, and drops the root-CID public link whenever `public_links_enabled=false`. The denylist draft helper in `src/shared/lib/sorafs-blacklist-proposal.ts` now supports both `add-to-denylist` and `remove-from-denylist`, requiring a reviewer note for removals and auto-prefilling duplicate/review references from the active moderation matches. Added typed CID lookup parsing in `src/shared/api/schemas.ts` / `src/shared/api/index.ts` and regression coverage in `src/shared/lib/sorafs-blacklist-proposal.test.ts`, `src/shared/api/schemas.test.ts`, `src/shared/api/index.test.ts`, and `src/pages/SorafsRegistry.test.ts`. Verified with `pnpm exec vitest run src/shared/lib/sorafs-blacklist-proposal.test.ts src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/pages/SorafsRegistry.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-244 | Localize the new SoraFS moderation, IrohaConnect, and denylist-draft UI copy on `/sorafs/registry`. | COMPLETED | 2026-04-06 completed: moved the 2026-04 SoraFS moderation/connect/proposal tranche off hardcoded English and onto `src/shared/lib/localization/en.json`, including moderation summaries, blocked-link notices, wallet-session actions, draft-form labels/placeholders, proposal notifications, and localized formatting of stable proposal-validation issue codes. Also fixed the pre-existing generic `clipboard.error` English copy in `en.json` so copy toasts resolve through i18n instead of a broken literal. Verified with `pnpm exec vitest run src/shared/lib/sorafs-blacklist-proposal.test.ts src/pages/SorafsRegistry.test.ts src/shared/lib/localization/index.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-245 | Propagate full SoraFS locale-key parity across shipped JSON packs and lock it with tests. | COMPLETED | 2026-04-06 completed: bulk-merged the full `en.sorafs` namespace into every shipped JSON locale pack under `src/shared/lib/localization/*.json`, preserving each locale’s existing translated leaves while filling missing SoraFS keys so `/sorafs/registry` no longer depends on English fallback for absent entries. Added a `sorafs` key-parity guard to `src/shared/lib/localization/index.test.ts` to prevent future namespace drift. Newly filled leaves still inherit English copy until per-language polish happens, but the runtime now has explicit SoraFS coverage in every shipped JSON locale. Verified with `pnpm exec vitest run src/shared/lib/localization/index.test.ts src/pages/SorafsRegistry.test.ts`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-246 | Replace the disabled SoraFS wallet submit state with a live Ministry draft, Connect signing, signed Torii submit, status polling, and confirmed record lookup flow. | COMPLETED | 2026-04-06 completed: landed the upstream `SubmitAgendaProposal` instruction, Ministry agenda draft/get Torii endpoints, JS SDK helpers, browser Connect app-session client, and updated Ministry docs in `../iroha`. The explorer now links against `@iroha/iroha-js`, drafts CID denylist/removal agenda submissions against `/v1/ministry/agenda/proposals/draft`, waits for approved wallet authority, requests detached wallet signatures through the Connect app session, finalizes and submits the signed Norito transaction to `/v1/pipeline/transactions`, polls `/v1/pipeline/transactions/status` to terminal state, resolves duplicate `proposal_id` conflicts from 409 preflight responses, fetches the confirmed Ministry record, and renders explicit `idle` / `waiting_for_wallet` / `approved` / `signing` / `submitted` / `wallet_rejected` / `session_closed` / `submission_failed` UI states while keeping the export/download fallback path intact. Added direct schema/API/page regression coverage for the Ministry draft/get, signed submit, terminal status polling, successful add/remove flows, duplicate handling, and Connect rejection/close cases. Verified with `pnpm vitest --run src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/pages/SorafsRegistry.test.ts`, `pnpm typecheck`, and upstream `../iroha/javascript/iroha_js npm run build:dist`. |
| EX-247 | Finish full SoraFS locale-quality completion, including localized proposal payload templates and explicit poetic-locale overrides. | COMPLETED | 2026-04-06 completed: replaced the remaining English filler under `src/shared/lib/localization/*.json` for the shipped SoraFS moderation/connect/proposal surface, repaired placeholder-bearing translations, and localized the generated proposal payload templates so `buildSorafsBlacklistProposal()` now emits locale-normalized BCP-47 `language` tags plus locale-specific title/impact/evidence strings instead of hardcoded English. Extended the poetic `akk` and `egy` locale layer with explicit `sorafs.connect`, `sorafs.proposal`, and `sorafs.moderation` overrides rather than inheriting the core submit/moderation copy from English. Added locale-quality guardrails on top of structural parity in `src/shared/lib/localization/index.test.ts`, including curated anti-English SoraFS sentinel checks for every non-English JSON locale and explicit poetic-locale sentinel coverage while exempting branded/code literals via the curated key list. Verified with `pnpm vitest --run src/shared/lib/localization/index.test.ts src/shared/lib/sorafs-blacklist-proposal.test.ts src/pages/SorafsRegistry.test.ts`, `pnpm typecheck`, and upstream `../iroha/javascript/iroha_js npm run build:dist`. |
| EX-248 | Close the post-ship SoraFS follow-up gaps: keep localized proposal payload templates authoritative when notes are present, cover Ministry draft-endpoint failure states, and rerun upstream validation. | COMPLETED | 2026-04-07 completed: `buildSorafsBlacklistProposal()` now keeps the localized motivation and target-reason templates authoritative while appending reviewer notes instead of replacing the generated copy, and the explorer has an explicit regression for Ministry draft-endpoint failures entering the `submission_failed` state without attempting signing or submit. Verified locally with `pnpm vitest --run src/shared/lib/sorafs-blacklist-proposal.test.ts src/pages/SorafsRegistry.test.ts` and `pnpm typecheck`. Reran upstream validation in `../iroha`: `IROHA_JS_DISABLE_NATIVE=1 node --test test/connect.browser.test.js test/transaction.test.js test/instructionBuilders.test.js test/toriiClient.test.js` passed with `630` passing / `111` skipped / `0` failed, `cargo test -p iroha_core submit_agenda_proposal --lib` passed, and `cargo test -p iroha_torii ministry_agenda_ --lib` passed after fixing the block execution path to populate `current_tx_hash` for regular transaction execution and correcting the Torii draft test to decode bare `TransactionPayload` bytes with the proper Norito entrypoint. |
| EX-249 | Fix Taira account-detail lookups and owner filters so `test...` i105 selectors are sent to Torii without being rewritten to `sora...`. | COMPLETED | 2026-04-07 completed after reproducing `https://taira-explorer.sora.org/accounts/testuロ1NrpスモaMメフNhziルZfvWn9ルリvFqxセmUモマ2ハキヘhqzセ71P2D3` and confirming the live root cause against `https://taira.sora.org`: `/v1/explorer/accounts/{test...}` and `/v1/explorer/assets?...&owned_by=test...` return `200`, while the frontend’s old `sora...` rewrite returns `400 invalid account_id ... ERR_UNEXPECTED_NETWORK_PREFIX`. Updated `src/shared/lib/account-literal.ts` so Torii-bound account selectors follow the selected node’s network prefix (`test` for Taira/Testus, `sora` otherwise), wired `src/shared/api/index.ts` to pass the active Torii base into that normalization for both detail fetches and owner/authority query params, and added regressions in `src/shared/lib/account-literal.test.ts` plus `src/shared/api/index.test.ts` for the Taira runtime. Verified with `pnpm exec vitest run src/shared/lib/account-literal.test.ts src/shared/api/index.test.ts`, `pnpm exec eslint --quiet src/shared/lib/account-literal.ts src/shared/lib/account-literal.test.ts src/shared/api/index.ts src/shared/api/index.test.ts`, and `pnpm typecheck --pretty false`. |
| EX-250 | Remove all account network-prefix rewrites so Explorer preserves the literal prefix it receives in routes, display, and Torii requests. | COMPLETED | 2026-04-08 completed per product direction: account IDs now keep the same `sora...`/`test...` prefix everywhere instead of being rewritten for display or API calls. Simplified `src/shared/lib/account-literal.ts` and `src/shared/lib/account-id.ts` so account selectors/display helpers preserve encoded i105 literals verbatim while still canonicalizing aliases, and updated `src/shared/api/index.ts` to stop passing the selected Torii base into account-selector normalization. Flipped the old rewrite assumptions in `src/shared/lib/account-literal.test.ts`, `src/shared/lib/account-id.test.ts`, `src/shared/api/index.test.ts`, `src/shared/ui/components/BaseHash.test.ts`, `src/shared/ui/components/BaseLink.test.ts`, and `src/shared/ui/composables/useExplorerScopeNavigation.test.ts` so display, routes, scoped links, and API requests all preserve the incoming prefix. Verified with `pnpm exec vitest run src/shared/lib/account-literal.test.ts src/shared/lib/account-id.test.ts src/shared/api/index.test.ts src/shared/ui/components/BaseHash.test.ts src/shared/ui/components/BaseLink.test.ts src/shared/ui/composables/useExplorerScopeNavigation.test.ts`, `pnpm exec eslint --quiet src/shared/lib/account-literal.ts src/shared/lib/account-id.ts src/shared/api/index.ts src/shared/lib/account-literal.test.ts src/shared/lib/account-id.test.ts src/shared/api/index.test.ts src/shared/ui/components/BaseHash.test.ts src/shared/ui/components/BaseLink.test.ts src/shared/ui/composables/useExplorerScopeNavigation.test.ts`, and `pnpm typecheck --pretty false`. |
| EX-251 | Review PR `#112` against the authoritative upstream Torii routes in `../iroha` and document regressions. | COMPLETED | 2026-04-15 completed as a review-only pass. Compared the frontend route/version assumptions against `../iroha` branch `i23-features` (`01c15080e9de93e8d52750efd8bba380c40cadf3`) and found production mismatches where the explorer still targets `/v2/*` for routes that upstream currently serves under `/v1/*`, including `nexus`, `zk`, governance SSE, and KAIGI relay SSE. No code changes were applied in this pass; findings were reported back to the user with file/line references. |
| EX-275 | Render transaction instruction metadata fully inline in the instruction details drawer instead of inside a nested scroll panel. | COMPLETED | 2026-04-16 completed after reproducing the `/transactions/:hash?instruction=...` complaint from Taira Explorer. Updated `src/shared/ui/components/InstructionsTable.vue` so the instruction-detail metadata JSON uses the full `BaseJson` rendering path and no longer applies the nested max-height/overflow scroller, while the table-row preview stays compact. Added regression coverage in `src/shared/ui/components/InstructionsTable.test.ts` to lock the compact row preview vs full detail-drawer rendering split. Verified with `pnpm exec vitest run src/shared/ui/components/InstructionsTable.test.ts src/pages/TransactionDetails.test.ts`, `pnpm exec eslint --quiet src/shared/ui/components/InstructionsTable.vue src/shared/ui/components/InstructionsTable.test.ts`, and `pnpm typecheck --pretty false`. |
| EX-204 | Preload persisted verified smart-contract source on page load so accepted source bundles are shown without requiring a new upload in the current browser session. | COMPLETED | 2026-03-28 completed: `src/shared/ui/components/ContractCodeViewPanel.vue` now fetches persisted verified source records after the local browser decompile resolves, first via the instruction-scoped contract-view endpoint and then via the code-hash contract-view endpoint as fallback, replacing the local decompile only when the backend reports `rendered_source_kind='verified_source'`. Added coverage for both preload paths in `src/shared/ui/components/ContractCodeViewPanel.test.ts`. Verified the smart-contract regression set with `pnpm exec vitest run src/shared/lib/transaction-instructions.test.ts src/shared/lib/contract-view.test.ts src/shared/lib/contract-view-worker-client.test.ts src/shared/ui/components/ContractCodeViewPanel.test.ts src/shared/ui/components/InstructionsTable.test.ts src/pages/TransactionDetails.test.ts`. Repository-wide `pnpm typecheck` and `pnpm build` are currently blocked by pre-existing unrelated `src/features/studio/*` TypeScript errors. |
| EX-252 | Migrate asset surfaces off legacy `/v1/explorer/*` endpoints onto the upstream `/v1/assets/*` + `/v1/accounts/{id}/assets` app API so Taira asset pages work again. | COMPLETED | 2026-04-18 completed after reproducing the live regression on `https://taira-explorer.sora.org/assets` and confirming the upstream drift against `https://taira.sora.org`: legacy `/v1/explorer/assets` and `/v2/explorer/assets` paths are gone, while the current app API serves asset definitions from `/v1/assets/definitions`, per-definition holders from `/v1/assets/{definition_id}/holders`, and per-account balances from `/v1/accounts/{account_id}/assets`. Updated `src/shared/api/index.ts` to prefer those routes, adapted `src/shared/api/schemas.ts` for the new `{ items, total }` payloads and `quantity`/`asset` fields plus nullable asset counters, and relaxed `src/pages/AssetDetails.vue` so holder tables still load when supply counters are absent. Added regression coverage in `src/shared/api/index.test.ts` and `src/shared/api/schemas.test.ts`, then verified with `pnpm exec vitest run src/shared/api/schemas.test.ts src/shared/api/index.test.ts src/pages/AssetDetails.test.ts src/pages/AssetsList.test.ts`, `pnpm exec eslint --quiet src/shared/api/index.ts src/shared/api/index.test.ts src/shared/api/schemas.ts src/shared/api/schemas.test.ts src/pages/AssetDetails.vue`, `pnpm typecheck --pretty false`, and `pnpm build`. |
| EX-253 | Audit non-asset explorer pages against the current Taira `/v1/*` app API and migrate the next broken surfaces off removed `/v1/explorer/*` routes. | COMPLETED | 2026-04-18 follow-up from EX-252: live probing showed the core explorer pages are still largely served by `https://taira.sora.org/v1/explorer/*`, so the real breakage was schema/display drift rather than wholesale route removal. Fixed four regressions across the remaining surfaces: (1) accepted upstream `ContractCall` executables in `src/shared/api/schemas.ts` and treated them as smart-contract transactions in `src/pages/TransactionDetails.vue`, which unblocked account-detail transaction tables; (2) normalized reversed pagination when Torii returns `page: 0` for the latest page in `src/shared/ui/components/BasePagination.vue`, which fixed block-detail ranges like `-9-0 of 1`; (3) preserved routed `testu...` account literals in `src/pages/AccountDetails.vue` so Taira account pages and QR payloads no longer jump back to canonical `sorau...`; and (4) relaxed telemetry `PeerInfo` parsing in `src/shared/api/schemas.ts` so live SSE payloads with `config: {}` and missing `location` fields no longer spam `ZodError` warnings on ordinary pages. Added regression coverage in `src/shared/api/schemas.test.ts`, `src/pages/TransactionDetails.test.ts`, `src/shared/ui/components/BasePagination.test.ts`, `src/pages/AccountDetails.test.ts`, `src/shared/ui/composables/useTelemetryMetrics.test.ts`, `src/shared/ui/composables/usePeersTelemetry.test.ts`, and `src/pages/NodesTelemetry.test.ts`. Verified with `pnpm exec vitest run src/shared/api/index.test.ts src/shared/api/schemas.test.ts src/pages/AccountDetails.test.ts src/pages/AssetDetails.test.ts src/pages/AssetsList.test.ts src/pages/TransactionDetails.test.ts src/pages/NodesTelemetry.test.ts src/shared/ui/components/BasePagination.test.ts src/shared/ui/components/TransactionsTable.test.ts src/shared/ui/composables/useTelemetryMetrics.test.ts src/shared/ui/composables/usePeersTelemetry.test.ts`, `pnpm typecheck --pretty false`, `pnpm build`, plus Playwright spot checks against the Taira-backed dev server for `/accounts/{id}`, `/blocks/885`, and `/telemetry`. |
| EX-254 | Restore the older light-mode styling while keeping the current dark-mode styling unchanged. | COMPLETED | 2026-04-18 completed: reverted the light-theme palette, shadows, and light-only chrome/homepage overrides in `src/shared/ui/styles/theme.scss`, `src/app/App.vue`, `src/widgets/header/TheHeader.vue`, `src/pages/HomePage.vue`, and `src/widgets/home-page-info/HomePageInfo.vue` back toward the 2026-02-14 styling baseline while leaving dark-mode overrides intact. Verified with `pnpm exec eslint --quiet src/app/App.vue src/pages/HomePage.vue src/widgets/header/TheHeader.vue src/widgets/home-page-info/HomePageInfo.vue`, `pnpm exec vitest run src/pages/HomePage.test.ts src/widgets/header/TheHeader.test.ts src/shared/ui/components/BaseButton.test.ts`, `pnpm build`, and Playwright screenshots of `/` and `/assets` in light mode plus `/` in dark mode via `http://127.0.0.1:4175`. |
| EX-255 | Switch Explorer account-literal handling to the upstream halfwidth-only i105 canonical format and reject legacy/fullwidth variants. | COMPLETED | 2026-04-18 completed after confirming `../iroha/crates/iroha_data_model/src/account/address.rs` now builds the canonical i105 alphabet from Base58 + halfwidth Iroha-poem kana and explicitly rejects both fullwidth-sentinel and legacy fullwidth-kana inputs. Updated `src/shared/lib/account-literal.ts` to accept only canonical halfwidth i105 literals, removed the old fullwidth-normalization path, tightened schema copy in `src/shared/api/schemas.ts`, and flipped the focused account/API/schema regressions so Torii halfwidth payloads are preserved while fullwidth inputs fail. Followed through with a repo-wide sweep of sample account IDs in tests, localization placeholders, home-page examples, and parity fixtures so the Explorer no longer ships invalid fullwidth examples. Verified with `pnpm exec vitest run $(git diff --name-only -- '*.test.ts' '*.test.tsx' | tr '\n' ' ')` and `pnpm typecheck --pretty false`. |
| EX-256 | Fix the currently failing Soracloud/accounts/latest-transactions Vitest regressions after the halfwidth account + filter copy updates. | COMPLETED | 2026-04-18 completed: updated `src/pages/SoracloudPage.test.ts` and `tests/e2e/accounts-data.smoke.test.ts` to assert against current localized validation/filter copy plus encoded account navigation, refreshed `tests/e2e/latest-transactions-stream.smoke.test.ts` to use canonical halfwidth i105 SSE fixtures and explicit stream cleanup, and stabilized `src/pages/NodesTelemetry.test.ts` by unmounting wrappers so timer/storage watchers no longer leak between cases. Verified with `pnpm exec vitest run src/pages/SoracloudPage.test.ts tests/e2e/accounts-data.smoke.test.ts tests/e2e/latest-transactions-stream.smoke.test.ts`, `pnpm exec vitest run src/pages/NodesTelemetry.test.ts`, and `pnpm exec vitest run`. |
| EX-257 | Add focused unit coverage for still-untested Explorer helpers and Soracloud composables. | COMPLETED | 2026-04-18 completed: added new regression suites for `src/shared/api/query.ts`, `src/shared/utils/transform-error-response.ts`, and `src/features/soracloud/display.ts`, plus direct composable coverage for `src/features/soracloud/useSoracloudSection.ts` and `src/features/soracloud/useSoracloudDashboard.ts`. The new tests cover query-param normalization (including preserved `0`/`false` values), 404 vs unknown error shaping, Soracloud inline formatting fallbacks, section readiness/refetch triggers, translated not-found handling, dashboard draft sanitization, optional-field validation, overview-card derivation, and refresh gating. Verified with `pnpm exec vitest run src/shared/api/query.test.ts src/shared/utils/transform-error-response.test.ts src/features/soracloud/display.test.ts src/features/soracloud/useSoracloudSection.test.ts src/features/soracloud/useSoracloudDashboard.test.ts`, `pnpm exec eslint --quiet src/shared/api/query.test.ts src/shared/utils/transform-error-response.test.ts src/features/soracloud/display.test.ts src/features/soracloud/useSoracloudSection.test.ts src/features/soracloud/useSoracloudDashboard.test.ts`, `pnpm typecheck --pretty false`, and a full `pnpm exec vitest run` pass (`112` files / `814` tests). |
| EX-258 | Remove site-specific host and project-name references from Explorer deployment documentation. | COMPLETED | 2026-04-21 completed: generalized the nginx deployment section in `README.md`, renamed the nginx include to `ops/nginx/explorer.subpath.locations.conf`, and scrubbed existing roadmap wording so local docs do not carry site-specific names. Verified with a repository content and filename search. |
| EX-259 | Fix `/transactions` pagination against the current 1-based Torii explorer contract so page clicks swap the rendered rows again. | COMPLETED | 2026-04-21 completed after verifying the current upstream `../iroha` explorer transaction pagination is 1-based while `src/shared/ui/components/TransactionsTable.vue` still used the older reversed `page=0` model. Switched the transactions table to normal 1-based paging, kept the cache/SSE merge path scoped to page 1 only, removed the stale reversed-pagination prop, and added a focused regression asserting the table now fetches page 1/latest rows without reversed mode. Verified with `pnpm exec vitest run src/shared/ui/components/TransactionsTable.test.ts`, `pnpm exec vitest run src/pages/BlocksList.test.ts src/shared/ui/components/BasePagination.test.ts`, `pnpm exec eslint --quiet src/shared/ui/components/TransactionsTable.vue src/shared/ui/components/TransactionsTable.test.ts`, and `pnpm typecheck --pretty false`. |
| EX-260 | Rebuild Kotodama Studio as a graph-first visual programming model for deployable contracts. | COMPLETED | 2026-04-24 completed: replaced the `/studio` primary creation path with a typed Vue Flow graph editor backed by `KotodamaStudioGraphDocumentV2`, v1 import normalization, source-map ranges, graph validation, and graph-to-Kotodama generation for state/map state, helpers, entrypoints, triggers, guards, branches, loops, formulas, durable writes, returns, and Iroha effects. Added graph templates for `stablecoin`, `threshold_escrow`, `asset_ops`, `subscription`, and `irohaswap_reduced`; the generated source now feeds the existing browser compiler and direct Torii `/v1/contracts/deploy` flow, with compiler diagnostics mapped back to graph nodes. Added `src/features/studio/ContractGraphCanvas.vue`, `src/features/studio/KotodamaGraphNode.vue`, `src/shared/lib/kotodama-studio-graph.ts`, refreshed `/studio`, and fixed the browser compiler's remaining Node-only `Buffer` usage. Added coverage in `src/shared/lib/kotodama-studio-graph.test.ts`, `src/features/studio/ContractGraphCanvas.test.ts`, `src/pages/KotodamaStudio.test.ts`, and `tests/playwright/kotodama-studio-workflow.pw.ts`. Verified with `pnpm exec vitest run src/shared/lib/kotodama-studio-graph.test.ts src/pages/KotodamaStudio.test.ts src/features/studio/ContractGraphCanvas.test.ts src/shared/lib/kotodama-studio-compiler.test.ts src/shared/lib/kotodama-studio-deploy.test.ts`, `pnpm exec vue-tsc --noEmit --pretty false`, and `pnpm exec playwright test tests/playwright/kotodama-studio-workflow.pw.ts --reporter=line`. |
| EX-261 | Close the remaining graph-first Kotodama Studio gaps: typed ports, structured builders, edge editing, stronger template coverage, lossless legacy import records, and Blockly isolation. | COMPLETED | 2026-04-24 completed: graph nodes now derive typed ports from params/state/maps/formulas/effects, connection validation enforces typed flow labels and duplicate branch/loop outputs, the inspector has structured parameter editing plus effect/formula field builders and branch/loop edge-label editing, v1 imports preserve Blockly workspace/workflow payloads in an explicit `legacy` record, and Blockly is documented as legacy-only outside the primary `/studio` creation path. All shipped graph templates now compile through the browser compiler tests, and the Vue Flow canvas sync path ignores internal dimension/selection churn so browser palette insertion no longer locks the page. Verified with `pnpm exec vitest run src/shared/lib/kotodama-studio-compiler.test.ts src/shared/lib/kotodama-studio-deploy.test.ts src/shared/lib/kotodama-studio-graph.test.ts src/pages/KotodamaStudio.test.ts src/features/studio/ContractGraphCanvas.test.ts --reporter=dot`, `pnpm exec vue-tsc --noEmit --pretty false`, and `pnpm exec playwright test tests/playwright/kotodama-studio-workflow.pw.ts --reporter=line`. |
| EX-262 | Align Explorer routing, proxying, streams, and active docs with current upstream `../iroha` `/v1` Torii routes. | COMPLETED | 2026-04-25 completed after confirming upstream `../iroha` HEAD `681ad33ecd` exposes Explorer and app API routes under `/v1`, root node status under `/status`, and no live Torii `/v2` app route surface for the Explorer. Collapsed frontend Torii route normalization to `/v1`, rewrote explicit `/v2/...` inputs to `/v1/...`, repointed governance/KAIGI/ZK streams, fetched Nexus public status from `/status`, updated Vite/nginx proxies for `/v1` plus `/status`/`/metrics`/`/peers`, and refreshed active docs plus `AGENTS.md` to reference `../iroha`. Added `@noble/ciphers` plus a narrow Vite alias for the linked upstream `@iroha/iroha-js` browser export so production builds resolve `@noble/ciphers/chacha` without mutating the sibling repo. Verified with `pnpm exec vitest run src/shared/api/index.test.ts src/shared/ui/composables/useGovernanceEvents.test.ts src/shared/ui/components/KaigiRelayEvents.test.ts src/pages/KaigiRelays.test.ts src/pages/ZkTelemetry.test.ts src/pages/Dataspaces.test.ts src/pages/DataspaceDetails.test.ts`, `pnpm exec eslint --quiet vite.config.mts src/shared/api/index.ts src/shared/api/index.test.ts src/shared/ui/composables/useGovernanceEvents.ts src/shared/ui/composables/useGovernanceEvents.test.ts src/pages/KaigiRelays.vue src/pages/KaigiRelays.test.ts src/pages/ZkTelemetry.test.ts src/shared/lib/kaigi.ts`, `pnpm typecheck --pretty false`, and `pnpm build`. |
| EX-263 | Resolve the current `origin/i23` merge conflict in the roadmap without dropping either completed work stream. | COMPLETED | 2026-04-26 completed: removed the `ROADMAP.md` merge markers, preserved both conflicting `EX-261` rows by keeping the graph-first Studio follow-up as `EX-261` and renumbering the later `/v1` route-alignment entry to `EX-262`, then recorded this conflict-resolution pass as `EX-263`. |
| EX-264 | Make transaction instruction details unambiguous when transaction and instruction statuses are questioned. | COMPLETED | 2026-04-29 verified the live BPNG transaction `4c06791dbb9950401ef85c8da44590163eab9945479ee31e162c0c1ee39c3947`: Torii reports the transaction and instruction `0` as `Committed` with no rejection reason. Fixed instruction-detail `DataField` slot rendering, added explicit instruction status in the drawer, and covered the populated drawer metadata in tests. |
| EX-265 | Add explicit no-fallback/no-hack API integration guidance to `AGENTS.md`. | COMPLETED | 2026-05-03 completed: updated the agent brief to prohibit fallback paths, compatibility shims, mock payloads, and quick hacks, and to require calling authoritative APIs with the correct routes, transports, codecs, and data formats. |
| EX-266 | Send required Torii negotiation/version headers on every Explorer API fetch path. | COMPLETED | 2026-05-04 completed: `src/shared/api/index.ts` now sends upstream Torii negotiation/version headers (`x-iroha-api-version: 1.1`, explicit `Accept`, and required `Content-Type`) through shared JSON GET/POST helpers, failover peer/health probes, raw `/peers`, root `/status`, raw `/metrics`, and Norito transaction submission. Added regression coverage in `src/shared/api/index.test.ts` for JSON reads, failover probes, root endpoints, JSON POSTs, metrics text, and binary Norito submission. Verified with `pnpm exec vitest run src/shared/api/index.test.ts`, `pnpm exec eslint --quiet src/shared/api/index.ts src/shared/api/index.test.ts`, and `pnpm typecheck --pretty false`. |
| EX-267 | Align the Explorer with the latest `../iroha` Torii API and move supported calls onto the JS SDK/browser client without frontend fallbacks. | COMPLETED | 2026-05-28 completed: added an upstream browser-safe `ToriiBrowserClient` export in `../iroha/javascript/iroha_js` for current `/v1/explorer/*`, `/v1/accounts/{id}/assets`, `/v1/assets/*`, Sumeragi, KAIGI, and contract deploy routes; updated the Node `ToriiClient` block/account-asset helpers to current route/query contracts; and rewired Explorer API reads/deploys in `src/shared/api/index.ts` through the SDK client while preserving existing failover/version-header behavior and removing frontend fallback shims. The full-suite pass also picked up upstream fixture drift, so the Explorer now exposes `fileURLToPath` from its browser `node:url` shim and treats Kotodama `String` annotations as string-literal JSON values for the refreshed `threshold_escrow.ko` sample. The upstream JS native checksum manifest now matches the checked-in `darwin-arm64` `iroha_js_host.node`, unblocking the Node-side SDK suite. Verified with `pnpm exec vitest run src/shared/api/index.test.ts src/shared/api/schemas.test.ts`, `pnpm exec vitest run`, `pnpm typecheck --pretty false`, `pnpm build:vite`, `pnpm exec eslint --quiet src/shared/api/index.ts src/shared/lib/node-url-browser.ts src/shared/lib/node-url-browser.test.ts src/shared/lib/kotodama-studio-compiler/internal.ts`, upstream `npm run build:dist`, upstream `node --test test/toriiBrowserClient.test.js`, and upstream `node --test test/toriiClient.test.js` (`652` tests). |
| EX-268 | Move the browser Kotodama compiler into `../iroha` as a reusable JavaScript SDK module and consume it from Explorer. | COMPLETED | 2026-05-28 completed: moved the browser Kotodama compiler implementation, literal helpers, parity fixture normalizers, TypeScript declarations, package export metadata, dist output, README usage docs, and SDK smoke coverage into `../iroha/javascript/iroha_js` behind the browser-safe `@iroha/iroha-js/kotodama-compiler` subpath. The SDK now exposes the reusable `compileKotodamaProgram` alias while retaining `compileKotodamaStudioProgram`, uses Iroha-marked BLAKE2b hashes for artifact hashes and literal checksums, and has `build:dist` coverage for the compiler output. Explorer now re-exports the SDK package from `src/shared/lib/kotodama-studio-compiler.ts`, removes the local compiler implementation files, and keeps parity/deploy/compiler tests pointed at the SDK boundary. Verified with upstream `npm run build:dist`, upstream `node --test test/kotodamaCompiler.test.js`, upstream targeted ESLint, Explorer focused compiler/parity/deploy Vitest, `pnpm typecheck --pretty false`, Explorer targeted ESLint, and `pnpm build:vite`. |
| EX-269 | Restore the Kotodama compiler test lint gate after the latest review. | COMPLETED | 2026-06-01 completed: changed the permission-instruction test helper options from an object-shaped `type` to an `interface`, extracted parity diagnostic alignment into a helper to keep callback nesting within ESLint limits, and verified the review fixes with targeted ESLint, targeted compiler/parity Vitest, full `pnpm lint` (0 errors; existing warnings remain), and `pnpm typecheck --pretty false`. |
| EX-270 | Make PR #112 mergeable under `master` signed-commit and linear-history protection. | COMPLETED | 2026-06-10 completed: GitHub reported PR #112 as approved and mergeable but `BLOCKED`; branch protection requires signed commits plus linear history, while the PR branch contained merge commits and four GitHub-reported unsigned commits. Rebuilt `i23` as a signed linear commit on top of `origin/master`, preserving the feature tree, with local backup branch `backup/i23-before-pr112-linearize-20260610`. Verified with `git diff --check`, `pnpm typecheck --pretty false`, and `pnpm build:vite`. |
| EX-271 | Migrate Kotodama Studio to the current canonical Rust compiler-service API exposed by the exact sibling `../iroha` JavaScript SDK. | COMPLETED | 2026-07-16 completed against the exact linked package at `../iroha/javascript/iroha_js`: Studio now awaits `compileKotodamaProgram`, consumes the current `KotodamaCompiled*` result model, maps structured Rust diagnostics back to graph nodes, surfaces transport/service failures separately, and cannot compile until an operator explicitly configures a trusted compiler-service base URL through runtime config or `VITE_KOTODAMA_COMPILER_URL`. No Torii/same-origin compiler URL is inferred. Browser deployment remains unavailable because the exact linked SDK has no approved browser signing and submission API. The obsolete independent-JavaScript-compiler and normalization/parity suites were retired, while focused tests retain the exact package link, browser-service request contract, HTTPS trust gate, canonical Rust native bridge, and Rust fixture-refresh boundary. Verified with focused compiler/config/Studio Vitest (`40` tests), full Vitest (`117` files / `787` tests), the exact upstream SDK compiler suite (`42` tests), `pnpm typecheck --pretty false`, targeted ESLint, `pnpm build:vite`, exact package realpath checks, and `git diff --check`. |
| EX-272 | Fail closed on unsupported browser deployment and make remote Studio compilation race-safe. | COMPLETED | 2026-07-16 completed: removed the retired server-deploy request/response schemas, API helper, deploy-draft builder, raw signing-key field, submission handler, route fixtures, and stale locale copy. Studio compile and graph JSON export remain available, while deployment is permanently disabled with an explicit explanation until the exact linked Iroha JavaScript SDK gains an approved browser signing/submission design. Each remote compile now carries a monotonic generation plus exact source, graph, and compiler-URL snapshots; any graph/config edit or newer request invalidates the old generation, and stale success, diagnostic, and transport-error responses are discarded without notification or authoritative hashes. Adversarial tests cover edits during an awaited compile, out-of-order responses, compiler-service config changes, missing compiler configuration, and absence of the signing-key/server-deploy surface. Verified with focused Vitest (`146` tests), localization integrity Vitest (`32` tests), `pnpm typecheck --pretty false`, targeted ESLint, `pnpm build:vite`, and `git diff --check`. |
| EX-273 | Decode nested multisig Norito instructions in transaction details. | COMPLETED | 2026-07-21 completed after reproducing transaction `0301b76b...624f`: replaced the lossy UTF-8/wire-id preview with the linked browser-safe `@iroha/iroha-js` `noritoDecodeInstruction` codec, so nested multisig entries now expose their full canonical instruction object while preserving the raw payload. The reported entry renders `Transfer.Asset` source, destination, and amount `100000`; malformed base64 and valid non-Norito bytes fail closed per entry without hiding other decoded entries. Added exact production-payload coverage in `src/shared/lib/multisig-custom.test.ts` and row/deep-link drawer coverage in `src/shared/ui/components/InstructionsTable.test.ts`. Verified with focused Vitest (`22` tests), full Vitest (`117` files / `787` tests), targeted ESLint, `pnpm typecheck --pretty false`, `pnpm build:vite`, and `git diff --check`. |
| EX-276 | Repair roadmap identifiers, archive stale task notes, and enforce roadmap integrity in CI. | COMPLETED | 2026-07-21 completed: reassigned the duplicate historical EX-256/EX-258 rows to EX-274/EX-275, archived 635 lines of stale Kotodama progress notes verbatim, added EX-276 through EX-290, and added a tested roadmap validator plus `pnpm check:roadmap`. Verified with focused Vitest, the validator, and `git diff --check`. |
| EX-277 | Replace implicit loading/error handling with explicit reusable resource states and race-safe refresh behavior. | COMPLETED | 2026-07-21 completed: added typed resource snapshots and a reusable state renderer; async requests are generation-safe, preserve ready data during refresh, suppress stale completions, avoid overlapping polls and hidden retries, and expose explicit retry actions. Block, transaction, account, domain, asset-definition, NFT, RWA, account subviews, search, and contract views now render truthful terminal/refresh states. |
| EX-278 | Make Explorer filters, pagination, panels, and exact global search URL-addressable and scope-preserving. | COMPLETED | 2026-07-21 completed: added typed route-query codecs and migrated primary list pagination/filters; added the exact-precedence search classifier (64-hex before numeric height), scoped `/search?q=`, parallel block/transaction resolution, truthful both/partial/not-found/error states, race suppression, and explicit retry; moved the catch-all route last and restored desktop header search. Verified with 45 focused tests, targeted lint, production build, and `git diff --check`. |
| EX-279 | Establish WCAG 2.2 AA page, table, navigation, dialog, focus, and reduced-motion foundations. | COMPLETED | 2026-07-21 completed: added application landmarks, a skip link with route-change focus management, keyboard/ARIA tabs and pagination, live notifications, and reduced-motion rules. Shared tables expose table/row-group/row/cell semantics with keyboard parity; block navigation and account copy actions use native buttons; primary account/domain entity rows use explicit links. Desktop/mobile keyboard behavior is covered by hermetic Playwright. |
| EX-280 | Add authoritative browser SDK parity for account history/permissions, contract activity/events, ledger evidence, and Norito block proofs. | COMPLETED | 2026-07-21 completed in `../iroha/javascript/iroha_js`: added strict browser-safe account history/effective-permission, contract activity/event/SSE, ledger header/state/state-proof, and canonical Norito BlockProof APIs; added typed terminal stream-gap errors, exact declarations/exports/docs/dist, and local Merkle verification. Relevant browser/Norito tests and declaration portability checks pass. |
| EX-281 | Add Iroha Connect raw signing plus an exact canonical-request authentication adapter for read-only browser requests. | COMPLETED | 2026-07-21 completed in `../iroha/javascript/iroha_js`: added exact `SignRequestRaw` tag-1 transport, domain-bound signature verification against the approved identity, a single-flight approval gate, and canonical-request auth without exposing private keys. Six focused Connect tests plus SDK lint/declaration checks pass. |
| EX-282 | Render exact semantic instruction presentations, including nested multisig instructions, while retaining canonical raw payloads. | COMPLETED | 2026-07-21 completed: added an exact `{variant, value}` presentation registry for every authoritative Explorer instruction family; preserved exact amount strings and validated entity links; decoded nested multisig Register/Propose/Approve/Cancel instructions; retained raw JSON/Norito evidence; and removed wire-ID, first-key, entity-guessing, fallback, and sequential-probing paths. Verified with 61 focused tests, typecheck, production build, and `git diff --check`. |
| EX-283 | Add authoritative account history and effective-permission views with visibility and provenance semantics. | COMPLETED | 2026-07-21 completed: `/accounts/:id` has URL-backed Overview, Activity, and Effective permissions views over the exact SDK contracts. Activity preserves exact amount strings and index evidence; permissions state effective direct-plus-role semantics without inventing unavailable per-entry provenance; both distinguish permission, visibility, not-found, empty, loading, and error states. |
| EX-284 | Add a read-only, Iroha Connect-authenticated multisig specification and proposal workspace. | COMPLETED | 2026-07-21 completed: added URL-backed chain/status/cursor state, Connect `sign_raw` canonical authentication, signed spec/proposal reads, exact server-supplied roles/thresholds/TTL/approvals, semantic nested instructions plus raw JSON, resolved-ID mismatch checks, and stale-session invalidation. The workspace contains no mutation, approval, or submission controls. |
| EX-285 | Add contract deployment, activity, event-history, and explicitly gap-aware live-event views. | COMPLETED | 2026-07-21 completed: Smart Contracts now has URL-addressable Deployments, Activity, and Events views with strict filters/pagination, decoded exact payloads and semantic links. Live events use one abortable direct fetch per explicit start, never auto-reconnect, stop on EOF/error/gap, preserve stale evidence, and require an explicit resync/history reload. 39 focused tests pass. |
| EX-286 | Add locally checked transaction Merkle evidence plus clearly labelled node-provided state and finality evidence. | COMPLETED | 2026-07-21 completed: transaction details load proof/state/QC evidence independently, decode exact Norito BlockProofs, verify Merkle paths and result pairs locally, preserve partial evidence, and label local checks separately from node-provided state-root and finality/QC claims. QC/BLS is explicitly marked unverified because no official browser verifier exists. |
| EX-287 | Add a version-pinned deterministic Mochi seed/profile for Explorer integration tests. | COMPLETED | 2026-07-21 completed: added a full-revision-pinned, profile-scoped Mochi wrapper, local-only seed verifier, native Torii CORS config for explicit loopback browser origins, runtime-artifact ignores, documentation, and focused tests. Cold live runs exposed and repaired upstream launcher/package, consensus-config, Kura-root, snapshot/lane-lifecycle, config-bound genesis, canonical Torii WebSocket/HTTP route, Norito envelope, and feature-dependent MCP-discovery contracts. Focused Kagami (4/4), Mochi supervisor (61/61), integration-mock (7/7), canonical stream, readiness, MCP, wrapper (7/7), and seed tests pass. A clean regenerated real peer reached readiness, the seed verifier observed chain `iroha-explorer-e2e-v1`, domain `wonderland.universal`, height 19, and 8 transactions, Torii returned the exact native preflight headers, and live Chromium loaded the seeded domain successfully (1/1). |
| EX-288 | Add hermetic and live Playwright/Jenkins quality gates across desktop and mobile browser profiles. | COMPLETED | 2026-07-21 completed: added deterministic desktop/mobile Chromium projects and opt-in live Mochi coverage. Jenkins now gates frozen install, roadmap integrity, lint, all unit tests, typecheck, production build, bundle budgets, and hermetic Playwright; the integrated hermetic matrix passes 10/10. |
| EX-289 | Split heavy browser dependencies and enforce route-aware gzip bundle budgets. | COMPLETED | 2026-07-21 completed: heavy SDK, QR, JSON, Vue Flow, and vendor dependencies are split; contract analysis is lazy, reducing the transaction route increment from about 212 KiB to 27.8 KiB gzip. Tested budgets cover entry/static closures, route increments, named chunks, and emitted workers (including the 183.9 KiB contract-view worker). |
| EX-290 | Roll out the audited Explorer slices with pinned upstream compatibility, smoke gates, and per-slice rollback points. | IN PROGRESS | 2026-07-23 in progress: advanced the coupled runtime/browser-SDK pin to `a457d6b60846923441fea549edcae30626c5e939`, installed the sibling SDK as a dependency-aware `file:` package, and repeated the clean paired-checkout production build/budget plus real Mochi readiness, seed, MCP, and live Chromium gates. The local compatibility gate is complete; repository-owner commit/deploy rollback points still require the later release workflow. |
| EX-291 | Correct the Explorer-facing Hyperledger Iroha brand spelling and prevent regressions. | COMPLETED | 2026-07-21 completed: corrected the misspelled home-page title in the five affected locale packs and added a shipped-locale regression check for the canonical English title plus the forbidden typo. Verified with localization Vitest (33 tests), targeted ESLint, roadmap validation, and a repository-wide spelling scan. |
| EX-292 | Make Mochi/Kagami genesis regeneration transactional and independently verified. | TODO | Follow-up hardening from the pinned live-gate audit: load one immutable peer-config snapshot and reject chain/discriminant mismatches; stage and sync the signed block, bound manifest, and peer configs as one generation; decode the Norito block and verify its genesis key, signatures, topology/transaction semantics, and consensus metadata against the persisted manifest; switch generations only after validation; and preserve the prior usable sandbox when generation or publication fails. The current EX-287 repair uses the authoritative config-bound Kagami path, persists the exact signed manifest, keeps private keys off argv in owner-only files, and is being validated through real-Kagami and live-launch gates; EX-292 records the broader crash/concurrent-mutation hardening without weakening those gates. |
| EX-293 | Resolve release-blocking review findings in the pinned SDK workflow, account-history fanout contract, transaction evidence binding, state agreement, and table accessibility semantics. | COMPLETED | 2026-07-23 completed: pinned the coupled runtime/browser SDK to `a457d6b60846923441fea549edcae30626c5e939`, changed the sibling SDK from an unmanaged `link:` to a dependency-aware `file:` package, inlined that file package in Vitest to keep strict SDK typed-array checks in the jsdom realm, and gave the debug Mochi runtime a tested 32 MiB worker-stack floor. Added strict index/fanout account-history envelopes; bound transaction proof validity to the requested hash, proof/reference heights, and the reference block's transaction root; bound state-root/state-proof agreement to the requested reference block; and exposed each visual desktop column as its own header/cell while mobile renders a list. Verified 1,001 unit tests, integrated typecheck, a clean exact-pin production build and bundle budget, Mochi `ready`/`mcp_ready`, deterministic seed data, live Chromium 1/1, and hermetic desktop/mobile Chromium 12/12. |
| EX-294 | Make Tracing Workspace scans resilient to transient Torii capacity responses without hiding terminal failures. | COMPLETED | 2026-07-30 completed after a live local Torii scan reproduced a terminal workspace failure on the first HTTP 429. Tracing instruction reads now honor the canonical Torii error envelope's `details.retry_after_seconds`, retry at most three times with bounded exponential backoff, pace sequential scans at 125 ms, count every real request, and retain a failed cursor for explicit resume after exhaustion; non-canonical/terminal failures are not retried. Added focused recovery, pacing, bounded-exhaustion, and resume coverage. Verified with focused Vitest (14/14), targeted ESLint, `pnpm typecheck --pretty false`, `pnpm check:roadmap`, and `git diff --check`; the exact production component (`54be229e…87ec0`) was synced to the Nexus, BPNG, and CBSI prepared explorer workdirs. |
| EX-295 | Align strict Explorer instruction-box validation with Torii's canonical framed-instruction digest field. | COMPLETED | 2026-07-30 completed after the paced live trace reached block 1 without throttling and strict parsing rejected the authoritative `box.framed_sha256` field. A read-only live `/v1/explorer/instructions` response, the exact pinned Torii `ExplorerInstructionBoxDto`, and OpenAPI schema all confirm this is a required `0x`-prefixed lowercase 32-byte SHA-256 digest. The strict frontend schema now models that exact field and rejects missing, unprefixed, uppercase, truncated, and unrelated input without passthrough or fallback behavior; typed test fixtures were aligned with the current API contract. Verified with nine focused Vitest files (212/212), targeted ESLint, `pnpm typecheck --pretty false`, `pnpm check:roadmap`, and `git diff --check`; the exact production schema (`fc3e2d4d…a3004`) was synced to the Nexus, BPNG, and CBSI prepared explorer workdirs. |
| EX-296 | Investigate apparently all-zero per-block transaction counts in the local Explorer. | COMPLETED | 2026-08-02 completed as a read-only diagnosis: live Torii data showed 26 external transactions across 20 non-empty blocks, with total/rejected counts matching the transaction index exactly. The newest roughly 120 blocks were genuinely empty (`transactions_hash: null`, `transactions_total: 0`), which made the home widget and first 11 list pages look uniformly zero; older block 174 rendered `1 (0 rejected)`, block 172 rendered `1 (1 rejected)`, and genesis rendered 7. The frontend passes Torii's authoritative `transactions_total` through its strict schema and renders it unchanged, so no count mapping, fallback, or UI correction was appropriate. |
| EX-297 | Fix the upstream Iroha root cause of sustained empty-block production. | IN PROGRESS | 2026-08-02: tracing the Sumeragi proposal/timer path in `../iroha` against the live local four-peer behavior, then implementing a deterministic consensus-level repair with regression coverage and live verification. |

## Archived Task Notes

Historical Kotodama compiler progress notes were moved verbatim to
[`docs/roadmap/archive/kotodama-compiler-progress-2026-04-21-to-2026-05-31.md`](docs/roadmap/archive/kotodama-compiler-progress-2026-04-21-to-2026-05-31.md).
**Process:** Before starting work, check this table, pick the highest-priority `TODO`, and update the status/notes as soon as progress is made. Add new rows if scope expands.
